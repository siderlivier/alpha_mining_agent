# 使用教學與功能解說

> 2026-09-20：目前優先挖礦與回測正確性，總經／HMM 暫緩，尚未刪檔。audit 預設只回饋 validation；舊 [audit] 經驗不再注入 prompt，新驗證經驗標 [audit:validation]。backtest 若遇 MissingReturnError，代表固定股票池存在未知報酬；須處理估值，不能刪股票或補零繞過。long_short 沿用前置只扣多頭成本的相容口徑，不是完整多空淨績效。最新處置與未結案項見 TODO 及評估報告第 14 節。

本文件是 `README.md` 的展開版：解釋**每個功能為什麼存在**、**參數怎麼調**、
**出事了怎麼辦**。設計理由的完整版在 `SPEC_架構設計規格書.md`。

---

## 目錄

1. [系統怎麼運作（一輪發生什麼事）](#1-系統怎麼運作一輪發生什麼事)
2. [三種 LLM 呼叫](#2-三種-llm-呼叫)
3. [記憶系統](#3-記憶系統)
4. [整理回合](#4-整理回合)
5. [預算計量與控制](#5-預算計量與控制)
6. [逾時與失敗處理](#6-逾時與失敗處理)
7. [運算子提案：從提案到上線](#7-運算子提案從提案到上線)
8. [因子組合實驗室（回測與合成）](#8-因子組合實驗室回測與合成)
9. [過擬合診斷：這個 IR 是真的嗎](#9-過擬合診斷這個-ir-是真的嗎)
10. [市場狀態模型（HMM）](#10-市場狀態模型hmm)
11. [config.yaml 完整參數說明](#11-configyaml-完整參數說明)
12. [日常操作指令速查](#12-日常操作指令速查)
13. [疑難排解](#13-疑難排解)

> 新增功能索引：[參考因子](#參考因子stage-2-的盲區修補)、[佇列消費機制](#佇列消費機制)、
> [運算子解鎖（第五步）](#批准後怎麼實作五步驟)、[三層前瞻檢測](#前瞻偏差檢測三層)、
> [留一法找拖累因子](#8-因子組合實驗室回測與合成)、
> [六項檢定的綜合結論](#綜合結論怎麼下)

---

## 1. 系統怎麼運作（一輪發生什麼事）

```
                    ┌─ memory/learnings.md（經驗庫）
                    ├─ memory/library.json（已入庫因子摘要，不含績效）
                    └─ fields.yaml + DSL 運算子卡片
                              │
                    ① Generate（LLM，約 300~560s）
                              ↓  15 個候選：hypothesis / prediction / formula
                    ② Evaluate（本地 pandas，約 50s，不花 token）
                              ↓  四階段漏斗 + 八項診斷 → verdict
                    ③ Distill（LLM，約 200~390s）
                              ↓  逐候選歸因：diagnosis / failure_type / lesson
                    ④ attempts 落盤（15 筆，只進不出）
                    ⑤ passed 因子入庫（含密封 test 指標）
                    ⑥ learnings.md 追加（經驗 ≤3 條、佇列 ≤3 條）
                    ⑦ DSL 受限訊號記錄（整理回合的提案素材）
                              ↓
                    ⑧ 記帳 → budget.json
                    ⑨ 每 10 輪觸發整理回合
```

**關鍵設計：② 是純本地計算。**LLM 只負責「提假設」和「解釋為什麼失敗」，
所有績效數字都由本地 Python 算，LLM 從頭到尾看不到 test 期指標。這是防止
「LLM 對著測試集調參」的根本機制。

### 四階段漏斗（Stage 1~4）

| 階段 | 檢查 | 門檻（`config.funnel`） |
|---|---|---|
| Stage 1 | 訊號存在性 | `\|IC\| ≥ 0.02` 且方向與 `direction` 一致 |
| Stage 2 | 與**因子庫＋參考因子**去相關 | 對任一因子 `\|ρ\| ≤ 0.5`（見下方「參考因子」） |
| Stage 3 | 與**同輪其他候選**去相關 | `\|ρ\| ≤ 0.7` |
| Stage 4 | 穩健性 | sub-train ICIR ≥ 0.45、validation ICIR ≥ 0.30、衰減 ≤ 50%、多頭腿超額 > 0、覆蓋率 ≥ 0.60、月換手 ≤ 0.40 |
| Stage 4b | 產業限定通道 | 全池未達標時，檢查單一產業是否達到更嚴的門檻（0.60 / 0.35） |

### 參考因子（Stage 2 的盲區修補）

2026-09-19 起，正式參考池改為 `registered_base_fields_v1`：候選是 `fields.yaml` 登錄的全部 32 個基礎欄位，不使用曾依 test 選取的上游 DFS survivors 或舊候選 CSV。目前 18 個通過，舊 25 個 DFS 參考因子完整保存在提交前備份。

```powershell
python -B src/seed_reference.py --dry-run
python -B src/seed_reference.py --list
python -B src/seed_reference.py --apply
python -B src/seed_reference.py --clear
```

`--list`／`--dry-run` 會重算訓練與驗證指標，但不提交。`--apply` 一次替換參考池並備份；不需要先 clear。`--clear` 也走同一備份交易。舊 `--from-upstream`／`--all` 路徑已移除，避免繞回 test 篩選。

預設資格：訓練期 |t| > 2、|ICIR| > 0.3；驗證期同向且 |ICIR| > 0.2；衰減 ≤ 50%；有效月份至少 36／12。每個評估區間的最後一月不列入，避免標籤價格跨越區間。方向只由 sub_train 決定，負向欄位儲存前取負，名稱與 metadata 標明反向。ICIR 使用月 IC 均值／標準差，沒有乘年化係數。

去重使用 sub_train＋validation 的月內排名池化相關，預設 |ρ| > 0.95 剔除較弱者；不讀 test 值來決定保留。test 期值仍保存在正式值檔，供日後人類評估，但不參與資格或方向判定。可用 `--min-icir`、`--max-decay`、`--max-ref-corr` 指定規則，請勿依 test 結果調參。

`memory/transactions/<交易ID>/` 保留提交前完整 library／values；新 R-ID 不重用歷史 ID。中斷鎖須先確認工作已結束，再執行 `python -B src/factor_scope.py --recover`。這是故障回復，尚非任意歷史版本的一鍵切換。

mock 挖礦會把因子庫、attempts、經驗、預算及日誌放在系統暫存目錄；mock 整理同樣隔離，不覆寫正式經驗。啟動時印出挖礦 sandbox 路徑。

歷史「26 個 own 對 25 個 DFS」績效仍是舊版本研究，不適用於新的 18 個參考因子，也不因流程修正而成為全新 holdout。

---

## 2. 三種 LLM 呼叫

全系統只有三種 LLM 呼叫，全部走 `claude -p`（headless）：

| 呼叫 | 模板 | 實測耗時 | 逾時上限 |
|---|---|---|---|
| Generate | `prompts/generate.md` | 中位 418s、最大 561s | `timeout_sec`（1200s） |
| Distill | `prompts/distill.md` | 中位 250s、最大 469s | `timeout_sec`（1200s） |
| Consolidate | `prompts/consolidate.md` | 約 300~530s | `consolidate_timeout_sec`（1800s） |

**耗時主要由「輸出長度」決定，不是輸入長度。**實測把 `learnings.md` 從
24,459 字元壓回 4,400 字元，Generate 只快了 4%（遠低於 98s 的標準差）。
想讓一輪變快，該調的是 `candidates_per_round`（每個候選都要寫一整段中文假設），
不是清經驗庫。

### 進度輸出

每通呼叫前後都會 log，避免終端機看起來像當掉：

```
Generate: prompt 28,431 字元，timeout 1200s，呼叫中...
Generate: 回應 8,204 字元，耗時 485s，227,414 tokens / $1.4069
```

任何一通耗時超過 timeout 的 80% 會額外警告，讓你在真的爆掉之前先看到。

---

## 3. 記憶系統

### 兩層結構

| 檔案 | 性質 | 會不會進 prompt |
|---|---|---|
| `memory/attempts/A-XXXX.json` | 逐筆嘗試，**只進不出、永不刪改** | ❌ 只有整理回合會看摘要 |
| `memory/learnings.md` | 蒸餾後的經驗 | ✅ **每輪 Generate 全文注入** |
| `memory/library.json` | 因子庫：自有 `F-xxx` + 參考 `R-xxx` | ⚠️ 自有的注入 id/中文名/面向/公式/解釋；參考的只給名稱與描述、**不給公式**。兩者**絕不含績效數字** |

`library.json` 裡的 `test_metrics_sealed` 是密封欄位，只供人類查閱。任何把它
放進 prompt 的改動都等於毀掉整個實驗的可信度。

### learnings.md 的小節規則

Distill 追加經驗時，`section` 欄位只接受：

1. 白名單三節：`全域規則`、`禁忌方向`、`待驗證假設佇列`
2. **learnings.md 裡已經存在的小節**（整理回合會自行重組出「紅海地圖」
   「聚合審計」這類有用的小節，不該被擋掉）

憑空造的新小節（例如某個 category 名）會被歸入「全域規則」並在 log 警告。
這是為了防止長出一堆只有一兩條的孤兒小節——早期版本因為 `prompts/distill.md`
裡寫著「或 category名」，累積出 9 個垃圾小節。

### 每輪追加上限

| 項目 | 上限 | 理由 |
|---|---|---|
| `learnings_additions` | 全輪 3 條 | 一輪能學到的通則本來就不多 |
| `queue_suggestions` | 全輪 3 條 | 佇列每輪只該長幾條；消化速度有限 |
| 佇列總量 | 50 條（`memory.QUEUE_MAX_ITEMS`） | 安全網；滿了拒收新項而非丟舊的（頂端是整理回合排過序的高優先項） |
| 一輪消化 | 5 條 | `prompts/generate.md` 的軟性限制，其餘配額留給新假設 |

> 計數同時認得 `- x` 和 `1. x` 兩種清單格式。整理回合的 LLM 常把 `-` 改寫成
> 編號清單，只認 `-` 的話計數會歸零、上限形同失效。

### 佇列消費機制

佇列的每一條都有 `[Q-xxx]` 編號。完整循環：

```
① append_learning("待驗證假設佇列", ...) → 自動補 [Q-xxx] 編號
② Generate 讀到帶編號的佇列，若某候選是為了消化其中一條 →
   回傳 from_queue: "Q-012"
③ mining_loop 驗證完（不論成敗）→ mem.consume_queue_item("Q-012", {...})
   → 從 learnings.md 移除，全文＋verdict＋attempt 落到
     memory/queue_consumed.jsonl（只進不出，資訊不遺失）
④ 整理回合全文重寫後，mem.normalize_queue_ids() 幫新條目補編號
```

**消費與成敗無關**——佇列是「待辦清單」不是「願望清單」，試過就該移出，
否則每輪 Generate 都要重讀已經試過的東西。結果保存在 `queue_consumed.jsonl`。

一輪最多消化 5 條（`prompts/generate.md` 的軟性限制），其餘配額留給新假設。

防呆：`from_queue` 格式不符 `Q-\d{3,}`、或編號不在目前佇列中，一律忽略，
不會誤刪。整理回合被明確要求保留既有 `[Q-xxx]` 編號。

---

## 4. 整理回合

### 做什麼

1. **升級**：≥3 個獨立 attempt 支持的 single_case 經驗 → 合併標 `[promoted]`
2. **推翻**：與新證據矛盾的舊規則 → 標 `[失效]`（不刪除）
3. **壓縮**：全文壓到 `learnings_token_cap`（4000 tokens ≈ 8000 字元）內
4. **議程**：重排待驗證佇列、刪掉已被試過或已被禁忌覆蓋的項目
5. **審計消化**：把聚合審計的發現寫成 `[audit]` 規則
6. **運算子提案**：DSL 受限訊號中同類卡點 ≥2 次時提出新運算子

### 觸發條件

```python
by_round = round_id % consolidation_every_n_rounds == 0     # 每 10 輪
by_size  = len(learnings.md) > learnings_hard_cap_chars     # 超長安全網（24,000 字元）
# 且：距上次整理嘗試 ≥ consolidation_min_gap_rounds（3 輪）才允許 by_size 觸發
```

⚠️ **`by_size` 是安全網，不該變成常規機制。**整理回合的產出目標就是 8,000
字元，每輪增長約 1,000 字元——若把門檻設在 8,000，整理完 3 輪就又觸發，
每次 $0.63 + 5 分鐘。24,000 的門檻確保每 10 輪的常規觸發一定先發生。

`consolidation_min_gap_rounds` 是防重試風暴：整理失敗時 `by_size` 條件不會
消失，沒有間隔限制的話會每輪重試（實測 R57/R58 連燒兩次）。

### 整理回合看得到多少證據

```python
ATTEMPTS_DETAIL = 45                              # 最近 45 筆逐筆全文
ATTEMPTS_WINDOW = 10 × 15 = 150                   # 涵蓋範圍
# 45~150 筆之間只給聚合統計
```

聚合區塊長這樣（約 1,500 字元，取代 16,000 字元的逐筆列表）：

```
## 較早的 105 筆（A-0669~A-0773，聚合統計，不逐筆列出）
- verdict 分布：rejected_stage1 60；rejected_stage2 29；rejected_stage4 16
- failure_type 分布：hypothesis_wrong 46；expression_bad 30；duplicate 29
- category 分布：electronics_dual_confirmation 3；...
- 重複出現的 lesson（出現次數 × 前 45 字，升級 [promoted] 的候選）：
    (3×) 券資比短期變化速度未展現出比水準值更強的資訊含量...
```

「重複出現的 lesson」正是判斷該不該升級 `[promoted]` 需要的訊號，用 1/20 的
篇幅保住了證據基礎。若把 150 筆全逐筆列出，prompt 會從 34,030 漲到 53,714
字元（+58%），足以把呼叫推過逾時。

### 安全機制

- 重寫**前**自動備份到 `memory/learnings_history/learnings_<時間戳>.md`
- 新文本缺任何必要小節（`## 全域規則`/`## 禁忌方向`/`## 待驗證假設佇列`）
  → 拒絕採用，保留原文
- 原始 LLM 輸出**一律先落盤**到 `memory/consolidate_raw/<時間戳>.txt`
- JSON 解析失敗時先做**本地修復**（不花 token）再放棄

### JSON 本地修復是什麼

整理回合要求 LLM 把一份 ~8,000 字元的 markdown 塞進 `learnings_md` 這個 JSON
字串欄位，等於要它把 200+ 個換行全部寫成 `\n`。漏掉任何一個，`json.loads`
就噴 `Invalid control character`。

但**那份輸出其實是好的**，只是跳脫沒做乾淨——而且 token 在 `call_llm` 回傳
的當下就已經花掉了。所以流程是：

```
call_llm() 回來
  ① 原始輸出無條件落盤 → memory/consolidate_raw/
  ② json.loads() 直接試
  ③ 失敗 → escape_raw_controls() 就地補跳脫，再試一次（0 token）
  ④ 還是失敗 → 拋錯，訊息告訴你原始檔在哪，可手動挖出 learnings_md 貼回去
```

②③④ 完全不打 LLM。刻意**不做重試**——重試要再花一次完整 prompt 的錢。

---

## 5. 預算計量與控制

### 真實 token，不是估算

`config.llm.output_format: json` 會讓 `call_llm` 自動附上 `--output-format json`，
從回傳的信封拆出真實用量：

```json
{"result": "...", "total_cost_usd": 0.0731,
 "usage": {"input_tokens": 12000, "output_tokens": 3400,
           "cache_creation_input_tokens": 800, "cache_read_input_tokens": 25000}}
```

四項分開記錄。舊版用 `(prompt字數 + 輸出字數) / 3` 估算，實測同一通呼叫
**估算記 7 tokens、真實 41,200 tokens**——因為估算法看不到 Claude Code 自己
塞進去的系統提示、工具定義與 cache 讀取量。

CLI 版本太舊或格式改變時會自動退回估算（係數改為 `/2`），並在 `budget.json`
的 `estimated_calls` 記下有幾通是估的，不會讓整輪掛掉。

### budget.json 欄位

```json
{
 "week_of": "2026-08-17",          // 週一，跨週自動重置
 "rounds_used": 6,                  // 本週成功輪次（失敗不佔額度）
 "total_rounds": 59,                // 全期累計，不重置
 "last_consolidate_round": 59,      // 全期狀態，擋整理重試風暴
 "tokens_used": 1524980,
 "cost_usd": 8.412145,
 "tokens_breakdown": {"input": 42, "output": 366927,
                      "cache_creation": 415388, "cache_read": 742623},
 "llm_calls": 13,
 "estimated_calls": 0,              // 沒拿到真實 usage 的通數
 "failed_calls": 0                  // 失敗但仍計費的通數
}
```

### 失敗一律計帳

**token 在 `call_llm` 回傳的當下就已經花掉了**，成敗不影響這件事。所以記帳
與成敗解耦：

```python
meter = Meter()
try:
    run_round(ctx, mem, round_id, meter, mock_fn)
except Exception as e:
    ok = False
# ← 記帳在 try/except 外面，成敗都執行
b = load_budget(); commit_usage(b, meter); save_budget(b)
```

`Meter` 由呼叫端建立並傳入，所以中途拋例外時已花掉的量仍留在呼叫端手上。
涵蓋：JSON 解析重試的那通、最後放棄的那通、逾時的那通、整理回合失敗的那通。

失敗**不佔** `rounds_used`（維持原設計），但另記在 `failed_calls`。

### 三種週上限

```yaml
weekly_round_budget: 15       # 輪次
weekly_token_budget: 0        # 0 = 不限制
weekly_cost_budget_usd: 0     # 0 = 不限制
```

檢查點在迴圈開頭，**已經開始的那一輪一定會跑完並存檔**，不會半途中斷讓資料
處於不一致狀態。

> 建議先跑一週、用 `--budget` 看真實數字再設上限。不要沿用舊的
> `est_tokens_used`，那是估算值、低估 2~3 倍。

### 查看用量

```bash
python src/mining_loop.py --budget
```

```
本週（2026-08-17 起）用量：
  輪次    6/15
  tokens  1,524,980（未設上限）
  成本    $8.4121（未設上限）
  明細    in 42 / out 366,927 / cache 建立 415,388、讀取 742,623
  呼叫    13 通
  全期累計輪次 59
  ✅ 額度內
```

### 成本結構（實測）

一輪約 **$0.9~1.5**，其中每通呼叫平均輸出 **~30,000 tokens**。但 15 個候選的
JSON 本身只需要 4,000~6,000 tokens——**約 83% 的輸出是模型的思考推理**。

想降成本的三個槓桿：

| 做法 | 怎麼改 | 影響 |
|---|---|---|
| 降低推理深度 | `llm.command: "claude -p --effort low"` | 最大槓桿，但可能影響假設品質——**先跑 `claude --help` 確認你的 CLI 版本有這個 flag** |
| 減少候選數 | `candidates_per_round: 15 → 10` | 輸出量約略線性下降，Generate 也會變快 |
| 設成本上限 | `weekly_cost_budget_usd: 15` | 純保險，不影響行為 |

> `total_cost_usd` 是 CLI 的 client-side 估算，官方文件註明可能與實際帳單有
> 出入。當作相對用量的精確計量很好用，對帳到分請看 claude.ai 的 usage 頁面。

---

## 6. 逾時與失敗處理

### 逾時 = token 花掉但拿不到結果

`claude -p --output-format json` 會把整個回應緩衝到最後才一次吐出信封。被
timeout kill 掉時 pipe 裡是空的：

```
情境 A：CLI 逾時前已寫出部分內容 → TimeoutExpired.stdout 拿得到
情境 B：CLI 結尾才一次輸出（真實行為） → TimeoutExpired.stdout = None
```

所以逾時是**最貴的失敗方式**，timeout 要按「最壞情況的兩倍」抓，不能按平均
值抓。目前設定對歷史最大值有 2.1~3.2 倍餘裕。

逾時時：用估算值記帳、`Meter.timeouts` +1、若有片段則存到
`memory/llm_raw/<時間戳>_timeout.txt`、錯誤訊息直接告訴你該調哪個參數。

### 各階段失敗的影響範圍

| 失敗點 | 後果 |
|---|---|
| **Generate 失敗** | 整輪歸零（沒候選就沒東西可保），損失一通的 token |
| **Distill 失敗** | **降級繼續**：15 筆 attempts 與入庫照常，只缺歸因/lesson/佇列 |
| **Consolidate 失敗** | 挖礦資料完全不受影響，`learnings.md` 原封不動 |

Distill 降級是重要的省錢機制：走到 Distill 時 Generate 的 token 已經花掉、
本地漏斗也跑完了，而 attempts 落盤與入庫**都不需要 Distill 的輸出**（步驟 4
本來就用 `dist_map.get(id, {})` 容錯，入庫只看 `cands + diags`）。舊版會把
通過漏斗的因子一起丟掉。

降級的 attempt 會標 `"distill_failed": true`，整理回合與事後查帳看得出來。

實際發生過一次（Round 58 撞到月消費上限）：

```
⚠️ Distill 失敗（You've hit your monthly spend limit）
   → 降級繼續：15 筆 attempts 與入庫照常，但本輪沒有歸因、lesson 與佇列建議
完成（Distill 降級）。本輪 78,964 tokens，$0.6554
```

---

## 7. 運算子提案：從提案到上線

### 提案怎麼產生

整理回合看 `memory/dsl_limitations.jsonl`（Distill 每輪寫入的「假設成立但
DSL 表達不出來」訊號），同類卡點出現 ≥2 次時提出新運算子。

### 三道機械驗證

`consolidate.validate_proposal()` 檢查：

1. 名稱格式 `[a-z_][a-z0-9_]{2,20}`
2. 簽名在純函數白名單 `(x)` / `(x, n)` / `(x, y, n)`——**不得引入新常數參數**
3. 證據引用 ≥2 個且都是真實存在的 attempt id

⚠️ **機械驗證不檢查語意可用性，也不檢查範例公式是否合法。**`qtr_only` 通過
了全部三道驗證，但實際上在月頻面板下會產生 100% NaN（季底月只佔 1/3 樣本，
而 `MIN_PERIODS_FRAC = 2/3`），而且它自己給的範例 `ts_mean(..., 4)` 的窗口 4
根本不在白名單裡。**這就是人工閘門存在的理由。**

### 審核清單

```bash
python -c "import json;d=json.load(open('memory/operator_proposals.json',encoding='utf-8'));print([p['name'] for p in d['pending']])"
```

審核時該問的問題：

- **覆蓋率**：這個運算子會不會讓大量樣本變 NaN？過得了 `min_coverage: 0.60` 嗎？
- **窗口相容**：搭配 `MIN_PERIODS_FRAC = 2/3`，在白名單窗口 `[3,6,12,24]` 下
  還有有效值嗎？
- **前瞻風險**：語意是否只依賴 t 及以前的資料？
- **是否與既有運算子重疊**：例如 `cs_rank` 已經是產業內排名了
- **效能**：需要 `_roll_apply`（逐窗 Python 迴圈）的話會拉長 Evaluate 時間

### 批准後怎麼實作（五步驟）

**沒有自動機制**——`status: "pending_review"` 只是文字欄位，程式碼裡沒有任何
地方會讀它。批准 = 你自己寫。以 `rank_nz` 為例：

**① `src/dsl.py` 的 `_SIGNATURES` 加簽名**

```python
    # 截面
    "cs_rank": ("n", "n"), "cs_rank_all": ("n", "n"), "cs_z": ("n", "n"),
    "rank_nz": ("n", "n"),          # ← 新增（n=數值, w=窗口, b=布林）
```

**② `src/dsl.py` 的 `Engine.eval` 加實作**

```python
        if name == "rank_nz":
            v = self.eval(a[0])
            return self._per_group(v.where(v.notna() & (v != 0)),
                                   lambda s: s.rank(axis=1, pct=True))
```

> 簽名含 `w` 的運算子會被 `_TS_OPS` 自動收錄，實作要寫在 `if name in _TS_OPS:`
> 區塊內（窗口參數是 `a[-1][1]`）。

**③ `tests/test_dsl_no_lookahead.py` 的 `FORMULAS` 加一條用到它的公式**

這步**不能跳過**。那裡有守門測試：

```python
def test_all_operators_covered():
    missing = set(dsl._SIGNATURES) - covered
    assert not missing, f"未被前瞻測試覆蓋的運算子: {missing}"
```

加了運算子卻沒加對應公式，`pytest` 就會紅。

**④ 補正確性測試 + 更新提案狀態**

在 `tests/test_dsl_correctness.py` 加逐格對照測試，並把
`operator_proposals.json` 裡那筆從 `pending` 移到 `implemented`。

**⑤ 跑 `python src/unblock.py --apply` 解鎖被卡住的假設**

這步最容易被忘記，但省下的錢最多。原理：

```
提案的 evidence 欄位  →  就是「被這個 DSL 缺口卡住的 attempt 編號」
        ↓
       attempts/A-xxxx.json  →  裡面有完整的 hypothesis
        ↓
   （只取 failure_type = expression_bad 的：假設沒被證偽、只是表達不出來）
        ↓
   放回 learnings.md 的待驗證假設佇列，標【解鎖】
```

沒有這步的話，那些已經付過 Generate + Evaluate + Distill 成本、而且診斷結論是
「假設可能成立、只是寫不出來」的候選，會永遠躺在 attempts 裡沒人再看。
已解鎖的記在 `memory/unblocked.json`，不會重複放。

```bash
python src/unblock.py                        # 預覽
python src/unblock.py --apply                # 寫入佇列
python src/unblock.py --op streak_true --apply   # 只處理某個運算子
python src/unblock.py --limit 5 --apply          # 一次最多放回幾條
```

**然後下一輪就自動生效**——`operators_card()` 是從 `_SIGNATURES` 動態產生的，
`build_generate_prompt()` 每輪呼叫它，新運算子會自己出現在 DSL 規格卡裡。
**不需要改 `config.yaml`，也不需要改 prompt。**

### 已實作的三個運算子

| 運算子 | 語意 | 解決什麼 |
|---|---|---|
| `rank_nz(x)` | 產業內百分位排名，但把 0 與 NaN 排除在排名之外 | `if_else(cond,X,0)` 在 `cs_rank` 下大量同分並列稀釋 IC（`[promoted]` 全域規則第 3 條） |
| `industry_demean(x)` | 產業內去均值，**保留量級** | `cs_rank` 會丟掉量級資訊，無法表達「去除產業共同 driver 後的殘差」 |
| `streak(x, n)` | 從當期往回數、單期變化同號的連續期數（1..n） | 無法表達「改善是否有持續性」「連續兩季同向才算確認」 |
| `streak_true(cond, n)` | 布林條件連續為真的期數（0..n） | 模型三次用 `streak(布林, n)` 撞 type_error——它要的是「連續 12 期 ROE>0」這種語意 |
| `clip_std(x, n)` | 截尾至**前 n 期**（不含當期）的均值 ±3σ | `sdiv`/yoy 在低基期分母下比值爆量，排序被離群樣本主導 |

`streak` 有暖機期：sign 序列首格是 NaN，`_mp(6)=4`，所以前 4 格是 NaN。
當期無變化（0）或缺值時回 0——方向未定義不該算進趨勢。

⚠️ `clip_std` 的實作**刻意偏離提案偽代碼**：邊界用 `shift(1)` 取前 n 期，不含
當期。提案原本寫 `ts_mean(x,n)`（含當期），那樣離群值會撐高自己的上界而截不到
——實測 `[10]*15+[1000]` 的上界是 949.9，等於沒截。另加了歷史零變異保護
（`sd < eps` 就不截），否則會把序列硬壓成常數。

### 提案去重

`pending` / `implemented` / `rejected_log` 三個清單的名稱都會查。只查
`pending` 的話，被你否決的提案會在下次整理回合原封不動地再被提一次。

---

## 8. 因子組合實驗室（回測與合成）

漏斗是**逐因子**的檢驗：每個因子單獨看夠不夠好。但實盤買的是**組合**，
兩者不是同一件事——一個 IC 漂亮的因子，如果跟庫裡其他因子高度相關又多帶了
噪音，加進組合反而會拖低 Sharpe。這一層就是用來看組合層面的。

由兩個檔案組成：

| 檔案 | 角色 |
|---|---|
| `src/backtest.py` | 回測核心。吃「已經算好的 score」，產出月報酬 / 換手 / 績效 |
| `src/factor_lab.py` | 把因子合成成 score，再送進回測；三種分析模式 |

分工是刻意的：`backtest.py` **完全不做跨期運算**，score 的無前瞻性由上游的
`factor_lab` 保證。這樣前瞻風險只集中在一個地方，好審。

### 資料從哪來（不跨專案）

```
memory/factor_values.parquet    因子值：F-xxx（自有）+ R-xxx（前置專案匯入）同住
data/monthly_base.parquet       fwd_ret_1m 標籤 + group 產業別
```

兩個檔案都在本專案內。`R-xxx` 是 `seed_reference.py` 一次性灌進來的快照，
之後 `factor_lab` 不需要前置專案存在。

### 三種分析

**1. `--compare`：三組 × 多模型比較**

```bash
python src/factor_lab.py --compare
python src/factor_lab.py --compare --model equal,ridge,lgbm    # 需 pip install lightgbm
```

跑 `own`（只有 F-xxx）/ `reference`（只有 R-xxx）/ `all`（全部）三組，
各套上你指定的模型，輸出一張績效表：

```
=== 三組 × 3 模型（test 期）===
因子組       模型         因子數     CAGR  Sharpe      超額     IR    MaxDD     換手    月數
------------------------------------------------------------------------------
own       equal       26   32.66%   1.776   6.07%   0.89  -13.32%  55.4%    77
own       ridge       26   38.19%   1.772  10.72%   1.58  -15.99%  58.2%    77
own       lgbm        26   39.32%   1.761  11.70%   1.83  -15.90%  67.5%    77
reference equal       25   30.12%   1.658   4.09%   0.56  -13.66%  35.9%    77
reference ridge       25   35.43%   1.582   8.90%   1.28  -17.02%  39.1%    77
reference lgbm        25   41.65%   1.722  13.74%   2.07  -16.15%  64.3%    77
all       equal       51   32.10%   1.823   5.51%   0.73  -12.84%  47.4%    77
all       ridge       51   39.32%   1.785  11.63%   1.74  -15.59%  56.1%    77
all       lgbm        51   45.54%   1.917  16.36%   2.57  -16.66%  67.2%    77

同期等權全樣本基準 CAGR = 24.26%。
```

這張表回答的是「agent 挖的因子，比人工合成的 DFS 因子好嗎？」
兩組的 test 期起點相同（2020-01），比較是公平的。

**實測讀出來的三件事：**

1. **26 個自有因子勝過 25 個參考因子**（equal 6.07% vs 4.09%、
   ridge 10.72% vs 8.90%）——agent 挖的東西不是在重複已知的因子。
   唯一例外是 LightGBM，參考因子那組反而贏一點，代表 DFS 因子之間
   有較多可被樹模型利用的非線性交互。
2. **合併最好**（all + lgbm，IR 2.57）——兩組因子是互補的，不是取代關係。
3. **模型階梯是完整的**：equal → ridge 提升很大（IR 0.73 → 1.74），
   ridge → lgbm 再提升（→ 2.57）。三層都留著有意義。
   但 LightGBM 的**換手也最高**（67% vs 等權 47%），必須配成本敏感度看。

### ⚠️ 別只看 CAGR：超額與 IR 才是因子的功勞

上面那張表最容易被誤讀的一格是 CAGR。**同期等權全樣本基準的 CAGR 就有 24.26%**
——台股 test 期（2020-01 起）本身是大多頭。多頭組合報 32%，其中 24% 是 beta，
只有 6% 是因子選出來的。

所以表裡有三個欄位是專門用來剝掉 beta 的：

| 欄位 | 定義 | 怎麼讀 |
|---|---|---|
| `超額` | 年化 (多頭月報酬 − 當月等權基準) | 因子真正創造的報酬 |
| `IR` | 超額 / 超額的年化標準差 | 超額的穩定度；> 0.5 算可用，> 1.0 算好 |
| `BenchCAGR` | 基準本身的 CAGR（表尾印出） | 拿來對照 CAGR，別被絕對數字騙 |

`--loo` 與 `--greedy` 因此**預設用 IR 排序**（`--rank IR`）。
想看舊行為就 `--rank Sharpe`，也可以 `--rank Excess`。

> Sharpe 也會被 beta 灌水：多頭組合的 Sharpe 1.78 看起來很高，但基準本身在
> 這段期間的 Sharpe 就不低。IR 沒有這個問題，因為它每個月都減掉了基準。

**2. `--loo`：留一法，找出拖累者**

```bash
python src/factor_lab.py --loo --set own
python src/factor_lab.py --loo --set all --model ridge
```

逐一把一個因子拿掉重跑，看 IR 怎麼變：

```
全集（26 個因子，ridge，test 期）：IR 1.583  超額 10.72%  CAGR 38.19%  …

因子      名稱                       去掉後IR      Δ貢獻      去掉後超額    去掉後CAGR
----------------------------------------------------------------------
F-001   ROE趨勢改善                  1.727   -0.143     11.63%     39.42%  ← 拖累
F-022   生技穩健成長                   1.654   -0.071     11.31%     39.04%  ← 拖累
F-015   便宜成長孤兒股                  1.652   -0.069     11.07%     38.73%  ← 拖累
F-002   金融品質價值                   1.583   +0.000     10.83%     38.38%
…
F-026   金控效率重估                   1.166   +0.417      7.62%     34.33%
```

`Δ貢獻 = 全集 IR − 去掉它之後的 IR`。**負值 = 拿掉它反而更好**。
表尾（Δ 最大）的因子則是組合的主要支柱——拿掉 F-026，IR 從 1.58 掉到 1.17。

> ⚠️ **被標成拖累的比例本身就是訊號。**
> 上面 26 個自有因子只有 5 個被標拖累，Δ 都很小，這是健康的。
> 但同樣的指令跑 57 個因子的 `all` 組，**33 個被標成拖累**——那不代表
> 這 33 個因子都是垃圾，而代表這組因子**過度擁擠**（互相高度相關，
> 邊際貢獻都趨近於零，正負號落在噪音裡）。
> 這種情況下該讀的是**排序**而不是正負號，並且該先去重（見
> `seed_reference.py --max-ref-corr`）或改用 `--greedy` 從空集重挑。

⚠️ 留一法會跑 N+1 次完整回測（N = 因子數）。26 個因子 + Ridge 約 4 分鐘，
57 個約 15 分鐘，不是即時的。先用 `--model equal` 掃一遍會快很多。

**3. `--greedy`：貪婪前向選擇**

```bash
python src/factor_lab.py --greedy --set own --select-span validation --span test
```

從空集開始，每輪加入邊際 IR 提升最大的那一個，印出成長曲線與最佳 k。

#### ⚠️ 這裡有一個會自欺的陷阱：選擇偏誤

walk-forward 保證的是**模型參數**沒看到未來。但「要選哪幾個因子」**本身也是
一次擬合**。如果用 test 期的 IR 來挑因子、再用 test 期的 IR 當成績單，
等於拿答案卷去挑答案——k 越大數字越漂亮，但那是選擇偏誤，不是真實 edge。

`--select-span` 就是用來把兩件事分開的：在哪一期**挑**，在哪一期**報**。

```
（在 validation 期挑選，在 test 期回報——回報期的數字才是誠實的）

  k=1  加入 F-003  風格孤兒股      validation IR 2.459 ｜test 期 IR 0.238 超額 1.30%
  k=2  加入 F-009  營收加速趨勢    validation IR 3.010 ｜test 期 IR 1.219 超額 6.85%
  k=3  加入 F-007  營收穩健溢價    validation IR 4.406 ｜test 期 IR 1.425 超額 8.47%
  k=4  加入 F-010  金融資本穩健度  validation IR 4.393 ｜test 期 IR 1.269 超額 6.86%
  …
最佳組合（依 validation 期挑）：k=3，validation IR 4.406
  → 同一組在 test 期：IR 1.425，超額 8.47%，CAGR 34.42%
```

兩條曲線的**落差**就是選擇偏誤的大小：validation IR 4.4 → test IR 1.4。
那個 4.4 是 24 個月樣本上的噪音，不是可以帶到未來的東西。

#### 實測結論：這組因子「不要挑」比較好

26 個自有因子、Ridge、test 期：

| 做法 | 挑選期 | test 期 IR | test 期超額 |
|---|---|---|---|
| **全部 26 個，完全不挑** | — | **1.583** | **10.72%** |
| 貪婪挑 k=3 | validation | 1.425 | 8.47% |
| 貪婪挑 k=10 | ~~test~~ | ~~2.264~~ | ~~12.04%~~ ← 假數字 |

**全用比任何挑出來的子集都好。** Ridge 本來就會給沒用的因子小權重，
再手動砍一刀只是引入選擇噪音。這也是為什麼第三列那個 2.264 特別危險——
它看起來是全表最好的，實際上完全來自在測試集上挑答案。

不加 `--select-span` 時程式會在結尾印警告，提醒你這組數字不能當結論。

`--loo` 同理：它是**診斷工具**（看誰跟誰重複、誰是支柱），
不是拿來生成「精選因子清單」的。

**4. `--cost-scan`：成本敏感度**

```bash
python src/factor_lab.py --cost-scan --set all --model equal,ridge,lgbm
```

同一組分數，換不同的單次換手成本重跑回測：

```
=== 成本敏感度（all，51 個因子，test 期）===
模型           換手     0.2%     0.4%     0.8%     1.2%     1.6%   ← 超額（年化）
--------------------------------------------------------------------------
equal     47.4%    6.65%    5.51%    3.24%    0.96%   -1.32%   衰退 7.97%
ridge     56.1%   12.98%   11.63%    8.94%    6.25%    3.56%   衰退 9.42%
lgbm      67.2%   17.97%   16.36%   13.13%    9.90%    6.68%   衰退 11.29%
```

**為什麼一定要跑：** 模型可以靠更高的換手換到更好的帳面績效，
而回測裡的換手只按固定比例扣一次——真實世界的滑點與衝擊成本在小型股上
遠不是線性的。你設的 `cost: 0.004` 是一個假設，不是事實。

**這張表的判讀：**

- **看斜率，不看絕對值。** 衰退幅度應該 ≈ `換手 × Δ成本 × 12`。
  上表 lgbm：`0.672 × 0.014 × 12 = 11.29%`，與實測完全吻合——
  這代表持股不隨成本改變（正確，因為成本沒有進入最佳化），
  所以這是一次乾淨的敏感度測試。
- **LightGBM 在每一個成本水準都維持領先**：成本拉到 1.6% 時，
  等權已經翻負（−1.32%），Ridge 剩 3.56%，LightGBM 還有 6.68%。
  結論是它的優勢**不是**單純靠高換手買來的。
- 若某個模型的衰退明顯**超過**那個等式，代表成本回頭影響了選股，
  那就不是純粹的敏感度測試——要回頭檢查程式。

**你的成本假設合理嗎？** 台股單邊手續費 0.1425%（常有折讓）、
賣出證交稅 0.3%，來回約 0.45% 再加滑點。`0.004` 偏樂觀，
小型股實際更高。上表告訴你的是：就算成本是設定值的四倍，
LightGBM 仍然有超額——這比單一數字有說服力得多。

### 指定任意子集

```bash
python src/factor_lab.py --factors F-001,F-005,R-003 --model ridge
```

`--factors` 覆寫 `--set`，可以混用自有與參考因子。

### 為什麼順序是 equal → ridge → lgbm

等權（`equal`）**沒有任何參數可以過擬合**，是誠實的基準線。
Ridge 只有一個 `alpha`，仍然很難靠參數騙自己。LightGBM 有幾百顆樹，
在 78 個月的 test 期上很容易做出好看的數字。

判讀規則：**Ridge 贏不過等權，就別急著上 LightGBM**。那通常代表因子之間
沒有值得學的線性權重差異，更複雜的模型只會學到噪音。

LightGBM 是選配（`--model lgbm` 才會 import），沒裝也不影響其他功能。

### `--span`：只有 test 期是真的

```bash
python src/factor_lab.py --compare              # 預設 test，唯一真正的樣本外
python src/factor_lab.py --compare --span validation   # 會跳警告
```

agent 的因子是用 `sub_train`+`validation` 篩出來的，在那兩期跑回測是
**in-sample**，數字必然虛高。前置專案的 DFS 因子也是用 ≤2019-12 篩的。
兩邊的 test 期起點相同（2020-01），這是唯一公平的比較基準。

選 `sub_train` 或 `validation` 不會被擋，但會印警告——那兩期只適合用來
debug 管線，不適合拿來做決策。

### 前瞻紀律（三道）

| # | 面向 | 做法 | 測試 |
|---|---|---|---|
| 1 | 切分 | headline 只看 test 期；挑因子用 `--select-span` 另外一期 | `test_greedy_selects_on_select_span_not_report_span` |
| 2 | 訓練窗 | walk-forward + embargo | `test_segments_*`、`test_walk_forward_no_lookahead` |
| 3 | 標準化 | 產業內 z-score 只用當期橫斷面 | `test_prep_is_cross_sectional_only` |

**walk-forward + embargo 的切窗**（`factor_lab.segments()`）：

```
第 i 段：訓練 ym <= months[i-1-EMBARGO]，預測 months[i : i+RETRAIN_EVERY]
```

embargo 存在的理由：月頻標籤 `fwd_ret_1m` 看的是**下一個月**的報酬。
訓練期最後一個月的 y，其實已經用到了測試期第一個月的價格。
不留空檔，訓練與測試就會在標籤上重疊。

**標準化的紀律**：`prep()` 用 `groupby(["ym", "group"])` 做產業內 z-score，
這是**當期橫斷面**運算。

> ⛔ 絕不可為了圖方便改成全期 `mean()/std()`。那會把 2026 年的分布洩漏進
> 2012 年的數字裡，而且不會報錯，只會讓績效變好看。
> `test_prep_does_not_use_full_period_stats` 就是專門守這一條的。

**測試怎麼證明的**：造合成面板，複製一份把 cut 之後的因子值全部汙染成 `1e7`，
兩份各跑一次 `prep` → `walk_forward`，斷言 cut 之前的分數**位元級相同**
（`check_exact=True`，不給容差）。真的沒讀到未來，那些數字是同一串運算。

驗證過測試確實有效：把 `tr = proc[proc["ym"] <= cut]` 改成 `tr = proc`
（全期訓練），測試立刻紅；把 embargo 拿掉，`test_segments_embargo_gap` 立刻紅。

### 回測口徑

與 agent 的評估**同口徑**，這樣漏斗的 IC 與回測的績效才是在講同一件事：

- 逐月**在各產業內**取分數前 `top_q`（不是全池排序）
- 產業內不足 `min_stocks_per_group` 檔就跳過該產業該月
- 換手 = `|本期持股 △ 上期持股| / |聯集|`，成本按換手比例扣在報酬上
- 月數 < 6 的期間回空 dict（年化數字沒有意義）

### 覆蓋率過濾

`ml.min_feature_coverage`（預設 0.30）：覆蓋率低於此的因子不納入合成。
一個只有 20% 月份有值的因子，在組合裡 80% 的時間都是 `fillna(0)` 的假訊號。
被排除的因子會在執行時印出來。

---

## 9. 過擬合診斷：這個 IR 是真的嗎

`factor_lab --compare` 說 LightGBM 的 IR 有 2.57。這一章的工具
（`src/ml_diagnose.py`）存在的理由只有一個：**一個漂亮的 IR 有很多種假法**，
而每一種假法要用不同的方法拆穿。一個沒被拆穿的假 IR，比沒有數字更危險，
因為你會照著它下注。

| 假法 | 拆穿的方法 | 指令 |
|---|---|---|
| 只是搭上多頭順風車 | 分多空 régime 看 | `--regime` |
| 只是少數幾個月撐起來的 | 逐月攤開 + 拿掉最好 N 個月 | `--breadth` |
| 只是小型股／低流動股溢酬 | 流動性歸因與過濾 | `--liquidity` |
| 只是規模因子換個名字 | 規模中性版重跑 | `--importance` |
| 只是試了很多策略挑到的幸運兒 | Deflated Sharpe / PBO | `--dsr` |
| 報酬被換手成本吃光 | 成本敏感度 | `factor_lab --cost-scan` |
| （加擇時值不值得？） | 上限與損益兩平準確率 | `--timing` |

```bash
python src/ml_diagnose.py --all          # 五項全跑（約 18 分鐘）
python src/ml_diagnose.py --regime
python src/ml_diagnose.py --breadth
python src/ml_diagnose.py --regime-factors
python src/ml_diagnose.py --timing       # 擇時研究（不含在 --all）
python src/ml_diagnose.py --liquidity
python src/ml_diagnose.py --importance
python src/ml_diagnose.py --dsr --save memory/diag_dsr.json
```

移植自前置專案 `src/ml/` 的四支腳本（`ml_model` 的重要性與規模中性、
`ml_regime`、`ml_liquidity`、`ml_validation`），改成**完全在本專案內**執行。
需要 `scipy`（`--dsr`）、`lightgbm`、`shap`（選配，沒裝會跳過 SHAP 那段）。

### 移植時刻意偏離原版的三處

| # | 原版 | 本專案 | 為什麼 |
|---|---|---|---|
| 1 | 全部樣本外月份混在一起 | 一律只看 `test` 期 | 本專案有三段切分，agent 的因子是用前兩段選的 |
| 2 | `SIZE_FEATURES = ["liq_amt","neg_size"]` 寫死名單 | 公式偵測 + 相關度偵測兩道 | 寫死的名單只認得 DFS 因子的名字，抓不到 agent 自己挖的規模變體 |
| 3 | `bt.COST = cst` 改模組全域再改回 | 用參數傳 `cost=` | 中途丟例外會留下污染的全域狀態 |

---

### `--regime`：是選股技巧，還是多頭順風車？

把樣本外月份依「大盤（全池等權基準）漲跌」分組，各自算超額。
**如果大盤下跌月的超額仍為正且 t 值不小，那就是真的選股技巧。**

```
===== 全體股票池（all/lgbm，test 期，51 因子）=====
          狀態    月數       年化超額      IR       t      勝率
        全部月份    77     16.30%    2.55    6.46   84.4%
       大盤上漲月    50     18.04%    2.53    5.16   84.0%
       大盤下跌月    27     13.08%    2.78    4.17   85.2%
    最差 25% 月    20     14.30%    2.71    3.50   85.0%
  逐年超額：2020:+17.2%　2021:+28.4%　2022:+15.5%　2023:+20.6%
           2024:+15.2%　2025:+9.2%　2026:+5.9%

===== 可交易版（最流動 30%）=====
        全部月份    77     15.10%    1.16    2.94   58.4%
       大盤下跌月    30      8.39%    1.34    2.12   60.0%
    最差 25% 月    20      6.99%    0.98    1.27   55.0%
  逐年超額：2020:+26.3%　2021:+36.1%　2022:-1.5%　2023:-1.5%
           2024:+25.9%　2025:+0.8%　2026:+17.6%
```

**全體股票池的結論很乾淨**：下跌月的 IR（2.78）甚至比上漲月（2.53）高，
逐年超額七年全正。這不是多頭順風車。

**但「可交易版」是另一個故事**，而且這是這整套診斷最重要的一格：
只看最流動 30% 的股票時，年化超額仍有 15.10%（水準守得住），
但 IR 從 2.55 掉到 1.16、月勝率從 84% 掉到 58%，2022 與 2023 年小幅為負。
**超額的水準守得住，穩定度守不住**——因為持股從 84 檔縮到 24 檔，
集中度大幅上升。這不代表 edge 是假的，但代表實盤的體感會顛簸得多。

---

### `--breadth`：那個平均是怎麼來的？

`--regime` 只給每個桶的平均，看不出平均的組成。「下跌月平均超額 +13%」
有兩種完全不同的可能：**27 個月都 +1%**（穩定技巧），或是
**24 個月接近 0、3 個月 +10%**（少數事件，下次未必再有）。

```
===== 全體股票池 =====
桶              月數     超額為正      中位超額      年化超額     IR   去掉最好3月的IR
全部月份           77      84%     1.08%    16.30%   2.55         2.52
大盤上漲月          50      84%     1.32%    18.04%   2.53         2.41
大盤下跌月          27      85%     1.08%    13.08%   2.78         2.42
  下跌月超額的集中度：最大 3 個月佔總超額 33%（3/27 = 11% 的月份）

===== 可交易版（最流動 30%）=====
全部月份           77      58%     0.60%    15.10%   1.16         0.92
大盤上漲月          47      57%     1.25%    19.39%   1.22         0.85
大盤下跌月          30      60%     0.51%     8.39%   1.34         0.78
  下跌月超額的集中度：最大 3 個月佔總超額 58%（3/30 = 10% 的月份）
```

**兩個版本的答案完全相反，這一格是整套診斷最該記住的。**

全體股票池是**廣泛的**：27 個下跌月有 23 個超額為正（85%），
中位數 +1.08% 與平均 +1.09% 幾乎相同（分布不偏斜），
拿掉最好的 3 個月 IR 只從 2.78 掉到 2.42。

可交易版是**少數事件撐起來的**：只有 60% 的下跌月為正，
最好的 3 個月佔掉 58% 的總超額（月份佔比只有 10%），
拿掉之後 IR 從 1.34 崩到 0.78。逐月看，可交易版最好的三個月是
2020-12（+5.49%）、2020-02（+3.53%）、2022-05（+3.21%），
其中兩個在 2020 年——疫情初期的極端行情。

**判讀規則：比較「最大 N 月的超額佔比」與「N/總月數」。**
33% vs 11% 是三倍（可接受的偏斜）；58% vs 10% 是接近六倍（少數事件主導）。

這一項也順便印出持股的產業組成，用途見下一節。

---

### 持股是什麼？——產業組成與結構性限制

```
產業            持股佔比      全池佔比      偏離      貢獻佔比       平均月報酬
電子           60.6%     59.9%   +0.7%     63.8%       3.81%
生技           19.9%     20.0%   -0.1%     12.6%       2.28%
半導體          14.7%     15.1%   -0.3%     21.2%       5.21%
金融            4.8%      5.1%   -0.3%      2.3%       1.76%
```

**產業佔比與全池幾乎相同是設計使然，不是巧合。** 回測是逐月
「在各產業內」取分數前 `top_q`，產業權重因此**結構性地鎖在全池比例**上，
偏離都在 ±0.7% 以內。多空月份的配置也完全一樣（差異 < 0.1%）。

這句話的含意值得說清楚：

> **本策略完全沒有產業輪動，也沒有市場擇時，而且永遠 100% 投資。**
> 全部的超額都來自「產業內選股」這一個來源。

半導體是效率最高的產業（14.7% 的持股貢獻 21.2% 的報酬），
生技最差（19.9% 持股只貢獻 12.6%）。若哪天想加產業輪動，
這張表就是起點——但那要改 `backtest.portfolio_returns` 的選股邏輯，
不是換個因子就能做到的。

持股本身偏小型：流動性百分位平均 0.42，最常持有的名字如
華冠（流動性 0.06）、誠研（0.13）、及成（0.07）都是極小型股。

---

### `--timing`：加一層市場方向判斷值不值得？

這一項**不含在 `--all`**（要跑幾千次回測模擬，很慢），是給
「我想加擇時模組（HMM 之類）」這個念頭做決策支援用的。

先看目前回測的三條腿：

```
long        CAGR 45.54%  Sharpe 1.92  MaxDD -16.66%  勝率 72.7%  ← 所有 headline 用這條
long_short  CAGR 36.69%  Sharpe 2.63  MaxDD  -9.82%  勝率 80.5%  ← 算了但沒人讀
benchmark   CAGR 24.26%  Sharpe 1.18  MaxDD -17.73%  勝率 64.9%

空頭腿單獨：年化 -7.87%，月勝率 44.2%
```

**目前的回測是只做多的。** `long_short` 有算但沒有任何地方讀它。
值得注意的是它的風險調整績效**明顯較好**（Sharpe 2.63 vs 1.92、
MaxDD −9.82% vs −16.66%），因為對沖掉了 beta；代價是 CAGR 少了 9 個百分點。

#### 空方的訊號其實很強，只是不能單獨拿來賺錢

「空頭腿年化 −7.87%」很容易被讀成「空方沒有訊號」。拆開看則不然：

| | 年化報酬 | 相對基準 | 跑輸基準的月份 | 絕對報酬為負的月份 |
|---|---|---|---|---|
| 前 10%（做多） | +45.54% | +21.3% | — | — |
| 全池等權基準 | +24.26% | — | — | — |
| **後 10%** | **+5.22%** | **−16.05%** | **77.9%** | 44.2% |

後段班**相對基準每年輸 16%，而且 78% 的月份都輸**——這是很強的訊號。
問題出在它**絕對報酬仍是正的**（+5.22%），只有 44.2% 的月份真的下跌。

所以結論分成兩半：
- **單獨放空賺絕對報酬**：不行。台股 test 期是大多頭，放空後段班長期是逆風，
  再加上借券費與平盤下不得放空的限制，實務上更差。
- **放空當對沖**：很好。那 −16.05% 的相對弱勢正是 `long_short`
  Sharpe 2.63 的來源。

⚠️ 台股放空還有借券費、平盤下不得放空、個股可借券數量等限制，
`long_short` 這條腿目前**沒有納入這些成本**，要當真用還需要補。

再看擇時的天花板與門檻：

```
===== 擇時的上限（完美預測，只當天花板）=====
  下跌月空手              CAGR  64.37%  Sharpe 3.30  MaxDD  -0.69%
  下跌月反手做空後段班       CAGR 107.76%  Sharpe 5.17  MaxDD  -0.69%
  （對照）一直滿倉          CAGR  45.54%  Sharpe 1.92  MaxDD -16.66%

===== 損益兩平的準確率（下跌月空手，1000 次模擬取中位）=====
  基準線：test 期有 65% 的月份上漲，「永遠猜漲」就有 65% 準確率且績效等同滿倉。

     準確率     中位CAGR    中位Sharpe    勝過滿倉的機率
     50%     20.98%        1.28         0.0%
     60%     28.56%        1.64         0.1%
     65%     32.15%        1.82         1.5%
     70%     36.20%        1.99         5.2%
     75%     41.23%        2.24        22.9%
     80%     45.43%        2.42        49.3%
     90%     54.91%        2.85        98.3%
    100%     64.37%        3.30       100.0%
```

**這張表是動手寫擇時模組之前最該看的東西。**

1. **損益兩平在 80% 左右**。低於 80%，擇時的 CAGR 反而**輸給**一直滿倉。
   70% 準確率看起來不錯，實際上 CAGR 從 45.5% 掉到 36.2%，只有 5% 的機率贏。
2. **真正的門檻不是 50%，是 65%**。test 期有 65% 的月份上漲，
   所以「永遠猜漲」這個零成本策略就有 65% 準確率，而且它的績效**就是**
   目前滿倉的績效。擇時模組要證明的是「明顯贏過永遠猜漲」，不是「贏過丟銅板」。
   月頻市場方向預測要穩定做到 80%，在文獻上是極高的標準。
3. **Sharpe 的門檻低一些**：70% 準確率的中位 Sharpe（1.99）就已略勝滿倉（1.92），
   80% 是 2.42。所以擇時的價值主要在**降波動與縮回檔**，不在提高報酬。
   ——但如果目標是風險調整績效，`long_short`（Sharpe 2.63）不需要任何預測
   就達到了，那是更便宜的路。

⚠️ 這張表對擇時模組**有利**，實際門檻只會更高，因為模擬假設：
(a) 準確率與月份漲跌幅無關——實際模型常在小波動上對、大波動上錯；
(b) 進出場沒有成本——空手再進場是一次 100% 換手；
(c) 空手期間報酬為 0——沒計入資金閒置的機會成本。

若還是要做，建議的驗證順序：**先在 test 期外建模與調參**（HMM 的狀態數、
特徵、轉移矩陣都是可調參數，很容易在 test 期上過擬合），
**再用同一套 walk-forward + embargo 產生樣本外的狀態機率**，
最後才用 `--timing` 這張表對照真實準確率落在哪一格。

---

### `--regime-factors`：狀態訊號更該接在哪裡

「用狀態決定進出場」門檻是 80%。但**同樣一個狀態訊號，接在「因子權重」上
門檻低得多**——這是規劃 HMM 之前最該知道的一件事。

```
----- test 期：2020-01 ~ 2026-06 -----
因子      名稱                 ICIR_全   ICIR_漲   ICIR_跌   差異(跌-漲)
R-006   低波動（月）               0.39    -0.12     1.89       2.01
R-005   低波動（季）               0.37    -0.15     1.80       1.95
F-008   低調接近新高               0.27    -0.31     1.60       1.91
R-004   低波動（半年）              0.36    -0.15     1.71       1.85
F-005   低波動異常                0.36    -0.15     1.70       1.85
F-020   冷門獲利股                0.50     0.11     1.65       1.54
  |差異| > 0.5：18/51　|差異| > 1.0：8/51　漲跌變號：12/51
```

低波動族群在**上漲月的 ICIR 是負的**（−0.12 ~ −0.31），
**下跌月卻高達 +1.6 ~ +1.9**。把它們混在一起用，兩種狀態互相抵銷——
`ICIR_全` 只剩 0.36~0.39，遠低於任一單邊。**這就是狀態訊號的價值所在。**

上限測試（分類只用 ≤2019-12 的資料、狀態用完美預知）：

| 做法 | CAGR | 超額 | IR | MaxDD |
|---|---|---|---|---|
| 靜態：全部因子（基準） | 39.32% | 11.63% | 1.74 | −15.59% |
| **狀態切換：攻擊/防禦組** | 42.17% | 13.89% | **2.31** | −15.27% |
| 只在下跌月切到防禦組 | 40.40% | 12.39% | 1.83 | −15.27% |

**關鍵差別在失敗的代價：**

| 接法 | 猜錯的後果 | 損益兩平門檻 |
|---|---|---|
| 決定進出場 | 整個月不在場，錯過 +7% 的上漲月 | ~80% |
| 決定因子權重 | 用了比較不合適的那組因子，IC 仍為正 | 低一個量級 |

所以建議：**狀態訊號先接因子權重，不要先接進出場。**

#### 「變號」是雜訊，真正穩定的是「不對稱」

`|差異|` 的排行榜容易讓人誤讀成「因子在兩種狀態下方向相反」。
補上 t 值（= ICIR × √月數）之後，圖像完全不同：

| 因子 | 分類期 ICIR_漲 (t) | 分類期 ICIR_跌 (t) | test ICIR_漲 (t) | test ICIR_跌 (t) |
|---|---|---|---|---|
| R-006 低波動（月） | +0.34 (2.64) | +1.61 (**9.51**) | −0.12 (−0.87) | +1.89 (**9.82**) |
| R-005 低波動（季） | +0.31 (2.37) | +1.72 (**10.02**) | −0.15 (−1.04) | +1.80 (**9.34**) |
| F-008 低調接近新高 | +0.02 (0.17) | +1.57 (**8.31**) | −0.31 (−2.19) | +1.60 (**8.30**) |
| R-004 低波動（半年） | +0.29 (2.22) | +2.14 (**12.11**) | −0.15 (−1.03) | +1.71 (**8.88**) |
| F-005 低波動異常 | +0.28 (2.20) | +1.96 (**11.26**) | −0.15 (−1.05) | +1.70 (**8.84**) |

**下跌月的強度在兩個期間都是壓倒性的（t = 8~12），完全穩定。**
上漲月的數字則兩個期間都貼近零（|t| 大多 < 1.1，只有 F-008 勉強達 −2.19）。

所以「12/51 在 test 期變號」不是結構改變，而是**上漲月本來就接近零、
正負號在雜訊裡翻面**。真正穩定且可依賴的事實是**不對稱本身**：

> 這些因子在下跌月極度有效，在上漲月幾乎無效。兩種期間都成立。

這對狀態模型是**好消息**——要利用的是那個穩定的不對稱，不是善變的正負號。
但也因此**不要把「上漲月放空這些因子」當成策略**，那是在賭雜訊。

---

#### 切換因子**確實會降 beta**（一個容易搞錯的地方）

直覺上「換用哪些因子選股」只影響選股品質、不影響市場曝險——組合畢竟永遠
100% 做多。**但這個直覺是錯的**，因為不同因子挑出來的股票，本身的 beta 不同：

| 組合 | beta | alpha 年化 | CAGR | IR | MaxDD |
|---|---|---|---|---|---|
| 一直用全部因子 | 0.932 | +13.27% | 39.32% | 1.74 | −15.59% |
| 一直用低波動防禦組 | **0.722** | +8.34% | 27.37% | 0.23 | −14.41% |
| 上漲月全部／下跌月防禦（完美預知） | 0.880 | **+16.95%** | 42.94% | **1.98** | −14.41% |

低波動因子挑出來的就是低 beta 的股票，所以切到防禦組時整體 beta 從
0.93 降到 **0.72**——不需要放空、不需要空手，光靠換因子就降了兩成曝險。

代價是 alpha 從 13.27% 掉到 8.34%，而且**一直用防禦組是很糟的策略**
（IR 只有 0.23）。所以這正是狀態切換的用武之地：只在需要的時候切過去。

完美預知的切換版同時拿到三件事：beta 略降（0.932 → 0.880）、
alpha 上升（13.27% → 16.95%）、回檔縮小（−15.59% → −14.41%）。

---

### 用景氣／貨幣數據做狀態訊號的發布落後陷阱

如果 HMM 的特徵是**景氣對策信號、M1B 這類官方統計**，有一個會靜默地
灌水回測的坑：**這些數據的發布時間遠晚於它所代表的月份。**

- **景氣對策信號**：國發會每月 **27~30 日**發布，內容是**上個月**的數據。
  也就是 3 月 27 日才知道 2 月的燈號。
- **M1B / 貨幣總計數**：中央銀行約每月 25 日發布上月數據
  （實際日期見主計總處的預告發布時間表）。

所以在月底 `t` 換股時，最多只能用到 `t−1` 月的數據，而且要確認發布日
確實早於換股日。直接把「3 月的燈號」對上「3 月底的持股決策」，
就是前瞻偏差——**而且它不會報錯，只會讓回測變好看**。

**做法**：在建 `regime_table` 時就把數據往後移一期（或依實際發布日對齊），
並且比照 `tests/test_pipeline_no_lookahead.py` 的手法寫一支測試：
把 cut 之後的景氣數據汙染成極端值，斷言 cut 之前的狀態序列位元級不變。

> 順帶一提：坊間對景氣燈號的回測普遍發現，扣掉發布落後之後，
> 它的擇時績效沒有明顯勝過買進持有。這不代表不能用，但代表它更適合當
> 「因子權重的狀態變數」而不是「進出場的訊號」——與上面那張表的結論一致。

---

### `--liquidity`：edge 有多少來自買不到的股票？

**(A) 歸因**——持股的流動性分布與各層貢獻：

```
持股的流動性百分位平均：0.42（0.5 = 市場中位）

    層級      持股次數      貢獻佔比       平均月報酬
   低流動      2760     41.4%       3.50%
   中流動      2349     32.6%       3.24%
   高流動      1367     26.0%       4.49%
```

判讀重點是**貢獻佔比 vs 持股佔比**，不是貢獻佔比的絕對值。
低流動層佔 41.4% 的貢獻，但也佔了 42.6% 的持股次數——**沒有超額佔比**。
而且高流動層的平均月報酬（4.49%）反而最高。這是好消息。

**(B) 流動性過濾 × 成本壓力測試**：

```
    剔除底X%    持股~  CAGR@0.4%  Sharpe   年化超額   超額t    換手  CAGR@0.8%  CAGR@1.2%
       0%     84     45.46%    1.91   16.28%   6.45   68%    40.93%    36.54%
      30%     58     43.58%    1.65   15.62%   4.79   72%    38.84%    34.23%
      50%     41     42.13%    1.46   14.96%   3.58   75%    37.23%    32.48%
      70%     24     45.86%    1.40   15.09%   2.94   77%    40.73%    35.75%
```

剔除底 70% 之後超額仍有 15.09%——**edge 在買得到的股票上也成立**。
掉下來的是 Sharpe（1.91 → 1.40）與 t 值（6.45 → 2.94），那是持股數
從 84 檔縮到 24 檔的集中度效應，與上面 régime 表的可交易版互相印證。

---

### `--importance`：模型倚重什麼？是不是規模的變體？

先偵測規模代理，**兩道判準**：

```
=== 規模代理偵測 ===
兩道判準：(1) 公式直接用到 amt_21／turn_21　(2) 與 log(成交金額) 的相關 |ρ| > 0.6
  F-008   低調接近新高    -0.745  ← 規模代理（公式＋相關度）
  F-013   冷門成長股      -0.582  ← 規模代理（公式）
  F-020   冷門獲利股      -0.463  ← 規模代理（公式）

⚠️ 這些因子的公式裡有 amt_21／turn_21，但量測相關度未達門檻：['F-013', 'F-020']
     F-013 冷門成長股：sub(cs_rank(rev_yoy), cs_rank(amt_21))
     F-020 冷門獲利股：mul(cs_rank(op_margin), cs_rank(neg(amt_21)))
   公式是定義證據，比相關度更硬——一併列為規模代理。
```

**為什麼需要兩道。** 只用相關度（前置專案的做法用的是寫死名單，更粗）
會漏掉 F-013 與 F-020——它們的公式裡明明白白寫著 `amt_21`，
量測到的 ρ 卻只有 −0.58 / −0.46，低於門檻。
**相關度是行為證據，公式是定義證據**，兩道都要。

然後跑規模中性版，**並附對照組**：

```
=== 規模中性版：排除 3 個規模代理，剩 48 個因子 ===
版本             因子數     CAGR       超額     IR     換手
全部因子            51   45.54%   16.36%   2.57  67.2%
規模中性            48   41.55%   13.52%   2.14  66.1%

對照組（逐一拿掉重要性前段、但非規模代理的因子，各 1 個）：
   拿掉 R-022 淨值股價比      IR 掉幅  +24.1%
   拿掉 R-020 距年高點        IR 掉幅  +19.8%
   拿掉 F-025 半導體毛利拐點股  IR 掉幅  +17.8%
   拿掉 F-011 生技轉骨訊號     IR 掉幅  +16.0%
   拿掉 R-004 低波動（半年）    IR 掉幅   +4.6%
   拿掉 R-019 保留盈餘比       IR 掉幅   -3.3%
   中位掉幅 +16.9%

判讀：規模中性版掉了 +16.9%，對照組單一因子中位掉幅 +16.9%。
   → 掉幅與對照組同量級，規模傾斜不是主要來源。
```

**對照組是本專案加的，原版沒有，而它是這一段唯一能讓數字被解讀的東西。**
拿掉任何一個重要因子 IR 都會掉，所以「中性版掉了 16.9%」本身不說明任何事。
真正的問題是「掉得比一般情況多嗎」。答案是：拿掉**三個**規模代理的代價，
等於拿掉**一個**普通重要因子的代價——規模傾斜不是 edge 的來源。

---

### `--dsr`：扣掉「試了很多次」之後還顯著嗎？

這是最嚴格的一關。前面每一項都是在問「這個策略在不同切面下是否穩健」，
DSR 問的是不同的問題：**你試了 N 個策略才挑出這個，那本來就該有一個看起來很好。**

```
===== Deflated Sharpe（lgbm 的『主動報酬』，已剝離市場 beta）=====
樣本外月數 T：77　試驗數 N：54
主動報酬每月 Sharpe（≈月 IR）：0.742（年化 2.57）
去膨脹門檻 SR0（N 次試驗的期望最大值）：0.398（年化 1.38）
**Deflated Sharpe：0.997**
```

讀法：試 54 個策略，光靠運氣，期望中最好的那個年化 Sharpe 就會有 1.38。
我們的 2.57 遠高於這個門檻，「真實超額 Sharpe 超過門檻」的機率是 **99.7%**。

**一定要用「主動報酬」（超額）而不是原始報酬。** 不減基準的話，
所有策略都吃同一份多頭 beta，試驗集的 Sharpe 變異被壓縮，
去膨脹門檻會被低估，DSR 就會虛高。

```
===== PBO（回測過擬合機率，CSCV）=====
策略母體 N=54　切塊 S=12（C(12,6) = 924 組）
**PBO：0.001**

⚠️ PBO 極低（0.001）要打個折扣看。試驗母體裡 51 個是單因子策略、
   只有 3 個是合成模型，而合成模型在任何切塊都穩定勝過單因子——
   樣本內最佳幾乎總是模型，樣本外也是，PBO 於是趨近 0。
```

**這個警告是程式自動印的，不是我事後補的註腳。** PBO 最有鑑別力的用法是
**互相可比的**策略母體（例如同一個模型的多組超參數）。這裡的 0.001
說明的是「合成勝過單因子」很穩健，**不是**「這組超參數沒過擬合」。

一個誠實的診斷工具必須包含「這個數字在什麼情況下不能用」，
否則它只是在生產讓人安心的裝飾品。

---

### 綜合結論怎麼下

51 因子 + LightGBM 的六項檢定：

| 檢定 | 結果 | 判讀 |
|---|---|---|
| Deflated Sharpe | **0.997** | ✅ 扣掉 54 次試驗仍顯著 |
| PBO | 0.001 | ⚠️ 有但書，見上 |
| 大盤下跌月超額 | +13.08%（IR 2.78, t 4.17） | ✅ 逆風時反而更好 |
| 剔除底 70% 低流動股 | 超額 15.09%（原 16.28%） | ✅ 買得到的股票也成立 |
| 規模中性 | IR 2.57 → 2.14（−16.9%） | ✅ 與對照組同量級 |
| 成本拉到 1.6% | 超額仍有 6.68% | ✅ 不是靠高換手 |

**沒有一項出現紅燈**，但有一項黃燈：可交易版（最流動 30%）的
IR 從 2.55 掉到 1.16、月勝率 84% → 58%。水準守得住、穩定度守不住。
實盤要有心理準備：這不會是一條平滑的曲線。

⚠️ 最後一句提醒：這六項檢定都是在 **同一個 test 期**上做的。
它們互相獨立地檢查不同的失效模式，但共用同一段歷史。
真正的樣本外只有一種——**往前走**。

---

## 10. 市場狀態模型（HMM）

`src/hmm_regime.py`。用途**不是決定進出場**（第 9 章的 `--timing` 顯示那要
80% 準確率才損益兩平），而是**切換因子組**——第 9 章的 `--regime-factors`
顯示低波動族群在下跌月的 ICIR t 值高達 8~12、上漲月卻貼近零，
這個不對稱在訓練期與測試期都成立。

```bash
python src/hmm_regime.py --fit       # 訓練期擬合一次，看狀態性質
python src/hmm_regime.py --predict   # walk-forward 樣本外方向準確率
python src/hmm_regime.py --apply     # 用狀態切因子組，比較靜態
python src/hmm_regime.py --predict --features ret,vol,breadth --states 3
```

### `hmm` 參數

| 參數 | 預設 | 說明 |
|---|---|---|
| `n_states` | 2 | 狀態數。2 = 多頭/空頭；3 以上在 ~170 個月的樣本上容易過擬合 |
| `features` | `[ret, vol]` | `ret` = 上月已實現報酬、`vol` = 過去 6 個月波動、`breadth` = 上漲家數比 |
| `vol_window` | 6 | 波動的回看窗 |
| `min_train_months` | 60 | 第一次擬合前至少要幾個月 |
| `refit_every` | 12 | 每幾個月重擬一次參數 |
| `seed` | 0 | Baum-Welch 的初始化種子。**換 seed 結果會變**，這本身就是模型不穩定的訊號 |

### 一個非直覺的發現：狀態標籤不能望文生義

訓練期（2012-04 ~ 2019-12）擬合出來的兩個狀態：

| 狀態 | 觀測報酬均值 | 觀測波動 | → 下月市場報酬 | → 下月上漲比例 |
|---|---|---|---|---|
| 低報酬態 | +0.16% | 6.54% | **+2.56%** | **72.0%** |
| 高報酬態 | +1.24% | 3.35% | +0.40% | 61.8% |

**高波動狀態之後反而漲得多**——波動的風險溢酬／反彈效應。

第一版程式照直覺寫成「觀測報酬均值最高的那個狀態 = 多頭態」，
結果 test 期方向準確率只有 **35.1%**（基準 64.9%）、t = **−2.33**、p = 0.023。
顯著地反向。那不是模型無效，是**標籤貼反了**。

修正方式必須用資料決定，而且只能用訓練資料：`bull_state_from_train()`
在訓練窗內比較各狀態的後續市場報酬，取最高者。

> ⛔ 這裡有一個 off-by-one 陷阱：觀測 `X[j]` 對應月份 `idx[j]`，而
> `mkt[idx[j]]` 是「`idx[j]` → 下個月」的報酬，要下個月底才知道。
> 擬合點在 `idx[n-1]` 時，可用的配對只到 `j = n-2`。
> 多用一格就是偷看當期答案——`test_bull_state_uses_only_realised_pairs`
> 專門守這一格。

修正後 test 期：預測看多的月份平均 **+4.07%**、看空的 **+1.11%**，
t = 2.16、p = 0.036。

### 只餵價格資料的實測結果

**方向預測：有訊號，但不足以擇時。**

| 門檻 | 預測漲的月數 | 準確率 | vs 基準 | 預測漲時平均報酬 | 預測跌時平均報酬 |
|---|---|---|---|---|---|
| 0.3 | 26 / 77 | 53.2% | −11.7% | +4.05% | +0.94% |
| 0.5 | 23 / 77 | 49.4% | −15.6% | **+4.07%** | **+1.11%** |
| 0.7 | 21 / 77 | 46.8% | −18.2% | +4.03% | +1.23% |

兩組報酬確實顯著分開（p = 0.036），但方向準確率 49.4% **輸給**
「永遠猜漲」的 64.9%——模型太保守，77 個月只喊 23 個月看多。

這正好印證第 9 章的結論：**準確率會被多數類別灌水，t 檢定才是真的在問
「預測為漲的月份報酬是否真的比較高」。** 兩個數字要一起看。

**切換因子組（test 期）：降風險，但不加報酬。**

| 做法 | beta | alpha | CAGR | IR | MaxDD |
|---|---|---|---|---|---|
| 靜態：全部因子 | 0.932 | +13.27% | 39.32% | **1.74** | −15.59% |
| 靜態：只用防禦組 | 0.711 | +9.75% | 28.82% | 0.35 | −13.16% |
| **HMM 切換（樣本外）** | **0.791** | +13.52% | 35.80% | 1.08 | **−13.16%** |
| 完美預知（上限） | 0.874 | +16.97% | 42.82% | 1.98 | −13.16% |

**誠實的結論：這一版沒有達標。** 驗收標準是「HMM 切換那一列要落在
『靜態全部因子』與『完美預知』之間」，而 IR 1.08 落在靜態的 1.74 之下。

它確實把 beta 降了 15%（0.932 → 0.791）、回檔縮小 2.4 個百分點、
alpha 還微幅上升，但報酬穩定度掉了。原因同上——模型太保守，
70% 的時間待在防禦態，而防禦組單獨的 IR 只有 0.35。

**下一步**：餵入景氣／貨幣數據（注意第 9 章講的發布落後），
目標是讓模型在真正的空頭月才切防禦，而不是一有波動就切。
`--apply` 那張表就是驗收標準，不要換別的指標來讓數字好看。

### 防禦組怎麼選（分類只用訓練期）

`--apply` 依「下跌月 ICIR 的 **t 值**」排序取前 1/6：

```
分類期 2012-01 ~ 2019-12（下跌 35 個月），選出防禦組 8 個：
   R-004  低波動（半年）    t_跌 12.67   ICIR_跌 2.14   ICIR_漲 0.29
   F-005  低波動異常       t_跌 11.60   ICIR_跌 1.96   ICIR_漲 0.28
   F-018  低波動隱藏成長股   t_跌 11.45   ICIR_跌 1.94   ICIR_漲 0.58
   R-005  低波動（季）      t_跌 10.17   ICIR_跌 1.72   ICIR_漲 0.31
   F-004  毛利穩定溢價      t_跌  9.98   ICIR_跌 1.69   ICIR_漲 0.28
   R-006  低波動（月）      t_跌  9.51   ICIR_跌 1.61   ICIR_漲 0.34
   F-008  低調接近新高      t_跌  9.29   ICIR_跌 1.57   ICIR_漲 0.02
   R-020  距年高點        t_跌  9.28   ICIR_跌 1.57   ICIR_漲 0.04
```

用 **t 值**而不是 `|ICIR差異|` 是刻意的：差異排行榜會把「上漲月的雜訊翻面」
排上來（見第 9 章），t 值只獎勵「下跌月真的強」這個穩定的部分。

### 三道前瞻紀律

| # | 面向 | 做法 | 測試 |
|---|---|---|---|
| 1 | 時點對齊 | `obs_ret[t] = mkt[t-1]` | `test_market_series_lags_observations_by_one_month` |
| 2 | 推論 | 只用前向濾波，不用 Viterbi／平滑 | `test_forward_filter_is_causal` |
| 3 | 擬合 | 擴張窗，參數與標籤只用訓練窗 | `test_walk_forward_states_no_lookahead` |

**第 2 道最容易中招。** `hmmlearn` 的 `predict()` 是 Viterbi、
`predict_proba()` 是**平滑**機率 `P(state_t | o_1..o_T)`——兩者都用到 `t`
之後的觀測。本模組只用 hmmlearn 擬合參數（Baum-Welch），
狀態推論自己寫前向遞迴，每一步只吃到當期為止的觀測。

測試裡有一條「反向確認」：`test_forward_filter_differs_from_smoothing`
斷言平滑機率**確實會**被未來觀測改變。少了它，上面那條 causal 測試
可能只是在測一個恆等式。

驗證過測試有效：把 `forward_filter` 換成 `predict_proba`、
或把 `market_series` 的 `shift(1)` 拿掉，對應的測試立刻轉紅。

---

## 11. config.yaml 完整參數說明

### `dsl`

| 參數 | 預設 | 說明 |
|---|---|---|
| `window_whitelist` | `[3, 6, 12, 24]` | 窗口參數只能是這幾個（單位：月）。改動會使既有 `fhash` 的語意基準改變 |
| `max_depth` | 3 | 運算子嵌套深度上限。審計顯示深度 3 的 train→test 衰減 20% vs 深度 2 的 -3% |
| `max_fields` | 3 | 每式使用欄位數上限 |

### `split`

| 參數 | 說明 |
|---|---|
| `sub_train` | 2012-01 ~ 2017-12，用於 Stage 1/2/3/4 |
| `validation` | 2018-01 ~ 2019-12，用於衰減檢查 |
| `test` | 2020-01 ~ 2026-06 — ⛔ **逐因子指標永不回饋 LLM** |

### `funnel`

見第 1 節的漏斗表。`stage4b_industry.enabled: false` 可關閉產業限定通道。

### `budget`

| 參數 | 預設 | 說明 |
|---|---|---|
| `weekly_round_budget` | 15 | 每週成功輪次上限 |
| `candidates_per_round` | 15 | 每輪候選數。**降低這個是縮短耗時與成本最直接的槓桿** |
| `consolidation_every_n_rounds` | 10 | 常規整理間隔 |
| `learnings_token_cap` | 4000 | 告訴整理回合壓縮到多少 tokens（≈8000 字元） |
| `learnings_hard_cap_chars` | 24000 | 超長觸發門檻（安全網）。**不要設成 8000**，會變成每 3 輪整理一次 |
| `consolidation_min_gap_rounds` | 3 | 兩次整理嘗試的最小間隔（擋重試風暴） |
| `weekly_token_budget` | 0 | 0 = 不限制 |
| `weekly_cost_budget_usd` | 0 | 0 = 不限制 |

### `llm`

| 參數 | 預設 | 說明 |
|---|---|---|
| `command` | `"claude -p"` | 可自由加旗標，例如 `"claude -p --effort low"`。`--output-format json` 會自動附加 |
| `output_format` | `json` | `json` = 取真實 token/成本；`text` = 退回字數估算 |
| `retry_limit` | 1 | JSON 陣列解析失敗的重試次數（Generate/Distill 用；整理回合不重試） |
| `timeout_sec` | 1200 | Generate / Distill。歷史最大 561s → 2.14x 餘裕 |
| `consolidate_timeout_sec` | 1800 | 整理回合。歷史最大 534s → 3.24x 餘裕 |

### `backtest`（`src/backtest.py`）

| 參數 | 預設 | 說明 |
|---|---|---|
| `top_q` | 0.10 | 各產業內做多前 10%。調小 → 更集中、換手更高 |
| `cost` | 0.004 | 單次換手成本（手續費+證交稅+滑點的近似），按換手比例扣 |
| `weighting` | `equal` | `equal` = 等權；`score` = 依分數排名配重 |
| `ann` | 12 | 年化因子（月頻 = 12） |

`min_stocks_per_group` 若沒在此區塊指定，會沿用 `funnel.min_stocks_per_group`（8）。

> `cost` 是最值得做敏感度掃描的參數。把它從 0.004 拉到 0.012，如果績效崩掉，
> 代表報酬多半來自高換手，實盤存疑。

### `ml`（`src/factor_lab.py`）

| 參數 | 預設 | 說明 |
|---|---|---|
| `min_train_months` | 48 | 第一次訓練前至少要幾個月。設太小 → 早期模型是在噪音上擬合 |
| `retrain_every` | 12 | 每幾個月重訓一次。設太小 → 跑很久且模型不穩 |
| `embargo` | 1 | 訓練尾與測試頭之間空幾個月。**月頻標籤看下一月，故必須 ≥ 1** |
| `ridge_alpha` | 10.0 | Ridge 的正則化強度 |
| `min_feature_coverage` | 0.30 | 覆蓋率低於此的因子不納入合成 |

⚠️ **`embargo` 不可設成 0**。`fwd_ret_1m` 看的是下一個月，設 0 會讓訓練期最後
一個月的標籤與測試期第一個月重疊，這是最容易被忽略的前瞻洩漏。
`tests/test_factor_lab.py::test_segments_embargo_gap` 會擋住這種改動。

---

## 12. 日常操作指令速查

```bash
# 挖礦
python src/mining_loop.py --rounds 5     # 連跑 5 輪
python src/mining_loop.py --mock         # 不耗額度跑通管線
python src/mining_loop.py --dry-run      # 只印 Generate prompt
python src/mining_loop.py --budget       # 只印用量，不呼叫 LLM

# 查看
python src/query_attempts.py --limit 40
python src/query_attempts.py --verdict passed
python src/report.py                     # 因子庫總覽（終端機）
python src/report.py --html              # 產生 memory/report.html 並自動開啟
python src/report.py --save              # 另存 memory/report.md
python src/audit.py                      # 聚合審計（可餵 agent 的版本）
python src/audit.py --human              # 含個體明細，勿餵 agent

# 維護
python src/seed_reference.py --list      # 參考因子：預覽篩選結果
python src/seed_reference.py --apply     # 參考因子：匯入因子庫
python src/seed_reference.py --clear     # 參考因子：移除
python src/unblock.py                    # 預覽可解鎖的假設
python src/unblock.py --apply            # 放回待驗證假設佇列
python src/consolidate.py                # 手動整理（會計入預算）
python src/consolidate.py --no-budget    # 手動整理但不計帳
python src/consolidate.py --mock         # 測試整理管線

# 因子組合實驗室
python src/factor_lab.py --compare                       # own/reference/all × equal,ridge
python src/factor_lab.py --compare --model equal,ridge,lgbm   # 加 LightGBM
python src/factor_lab.py --loo --set own                 # 留一法：找拖累者（IR 排序）
python src/factor_lab.py --loo --set own --rank Sharpe   # 改用 Sharpe 排序
python src/factor_lab.py --greedy --set own --select-span validation --span test
python src/factor_lab.py --cost-scan --set all --model equal,ridge,lgbm  # 成本敏感度
python src/factor_lab.py --factors F-001,R-003 --model ridge  # 指定子集

# 過擬合診斷
python src/ml_diagnose.py --all                # 五項全跑（約 18 分鐘；不含 --timing）
python src/ml_diagnose.py --regime             # 多空 régime + 逐年超額
python src/ml_diagnose.py --breadth            # 逐月分布、集中度、產業組成
python src/ml_diagnose.py --timing             # 擇時研究：上限與損益兩平準確率
python src/ml_diagnose.py --liquidity          # 流動性歸因 + 過濾×成本
python src/ml_diagnose.py --importance         # 特徵重要性 + SHAP + 規模中性
python src/ml_diagnose.py --dsr                # Deflated Sharpe + PBO

# 市場狀態模型
python src/hmm_regime.py --fit                # 訓練期擬合，看狀態性質
python src/hmm_regime.py --predict            # walk-forward 樣本外方向準確率
python src/hmm_regime.py --apply              # 用狀態切因子組，比較靜態
python src/factor_lab.py --compare --save memory/lab_compare.json

# 測試
python -m pytest tests/ -q               # 217 passed（約 2 分鐘；缺 data/ 時會跳過 5 支）
python -m pytest tests/test_pipeline_no_lookahead.py -q  # 只跑全管線前瞻檢測
python -m pytest tests/test_factor_lab.py -q         # 只跑因子合成/回測前瞻檢測
python -m pytest tests/test_dsl_no_lookahead.py -q   # 只跑前瞻偏差測試
```

---

## 13. 疑難排解

### `Failed to authenticate: OAuth session expired and could not be refreshed`

CLI 登入憑證過期，不是程式問題。依序試：

1. 開一般 cmd 執行 `claude`，用 `/status` 看 `Login` 那列是否顯示
   `Expired — log in again`；是的話 `/login`
2. 互動模式正常但 `claude -p` 壞掉 → `claude logout` → `claude login`，
   並確認 `claude --version` 與互動介面顯示的版本一致
3. **長期解**：改用一年期 token
   ```cmd
   claude setup-token
   setx CLAUDE_CODE_OAUTH_TOKEN "<token>"
   ```

### `You've hit your monthly spend limit`

Anthropic 帳號層級的月消費上限。設 `weekly_cost_budget_usd` 讓系統在撞到
Anthropic 的上限之前先自己停，或降 `candidates_per_round` / 加 `--effort low`。

### `LLM 呼叫逾時`

調高 `timeout_sec` / `consolidate_timeout_sec`。若逾時前有收到片段，路徑會寫在
錯誤訊息裡（`memory/llm_raw/`）。逾時的 token 已用估算值計入預算。

### 整理回合 `JSON 物件未閉合` / `Invalid control character`

先看 `memory/consolidate_raw/<最新時間戳>.txt`——本地修復已經試過一次仍失敗，
但**原始輸出還在**。可以手動從中取出 `learnings_md` 的內容貼進
`memory/learnings.md`（`learnings.md` 本身未被更動）。

### `RuntimeWarning: invalid value encountered in divide`

已修。成因是 `xsec_corr()` 在某月因子為常數時，`rank` 全同分 → 標準差 0 →
`np.corrcoef` 除以零。結果本來就是 NaN 會被濾掉，只是會洗版。若再出現，
檢查是否有其他 `.corr()` 呼叫缺少 `nunique() < 2` 保護。

### 終端機好像卡住了

Generate 約 300~560s、Distill 約 200~390s、整理約 300~530s，這期間沒有輸出是
正常的。現在每通呼叫前後都有進度行，看 `mining.log` 最後一行是「呼叫中...」
就表示還在跑。真的卡住的話會在 timeout 後拋錯，不會無限等待。

### 佇列項目一直重複出現

檢查 `learnings.md` 的佇列條目有沒有 `[Q-xxx]` 編號。整理回合全文重寫時若把
編號改掉或刪掉，消費機制就認不得。跑 `python -c "import sys;sys.path.insert(0,'src');
from memory import Memory;print(Memory().normalize_queue_ids())"` 補回去。

### 實作了新運算子，但沒看到相關的新候選

跑 `python src/unblock.py` 看有沒有可解鎖的假設。若顯示 0 條，可能是那個運算子的
`evidence` 對應的 attempt 失因不是 `expression_bad`（`hypothesis_wrong` 代表機制
已被證偽，刻意不解鎖），或是已經解鎖過了（見 `memory/unblocked.json`）。

### 前瞻偏差檢測（三層）

```bash
python -m pytest tests/test_dsl_no_lookahead.py -q      # DSL 層：33 個運算子
python -m pytest tests/test_pipeline_no_lookahead.py -q # 全管線（資料 → 漏斗 → 診斷）
python -m pytest tests/test_factor_lab.py -q            # 下游（因子合成 → 回測）
```

**DSL 層**：對覆蓋全部運算子的公式集，把 `t_cut` 之後的資料換成 1e6 重算，
斷言 `t_cut` 以前逐格完全一致。`test_all_operators_covered` 確保新增運算子
時不會忘了加測試公式。

**全管線層**：造一份合成 `monthly_base`，複製一份把 sub_train 結束月之後的
**所有欄位**汙染，兩邊各跑一次完整的 `evaluate_batch`，斷言 sub-train 期的
診斷數字逐項相同。同時檢查：

- 切分不重疊（`max(sub_train) < min(validation) < max(validation) < min(test)`）
- `fwd_ret_1m`（label，本來就看未來）不在 `ctx.fields` 也不在 `ctx.data`
- 參考因子進入 Stage 2 後同樣不引入前瞻（造一個未來全汙染的 R-001 測）

> ⚠️ 汙染起點**從 `config.yaml` 的 `split.sub_train[1]` 讀**，不寫死。
> 這裡曾經寫死 index 導致汙染點落在 sub_train 內部，測試自己製造了假陽性。

**下游層**（因子合成 → 回測）：漏斗把因子放行之後，還有一段路才變成組合績效，
那段路有自己的三個洩漏面（詳見[第 8 章](#8-因子組合實驗室回測與合成)）：

- `prep` 的產業內 z-score 只用當期橫斷面（改成全期 mean/std 會洩漏且不報錯）
- `walk_forward` 的訓練窗不含測試期，且留 `embargo` 個月空檔
- `backtest` 只吃算好的 score，不做任何跨期運算

同樣用「汙染未來 → 斷言過去位元級不變」的手法（`check_exact=True`）。
這套測試驗證過確實有效：把訓練窗改成全期、或把 embargo 拿掉，測試立刻轉紅。

上游資料對齊也查過：`build_base.py` 與前置專案 `mine_dfs.py` 的月頻快照都是
`groupby(["stock_id","ym"]).tail(1)`（月底），`fwd_ret_1m` 都是下月報酬，
兩邊定義一致，所以參考因子的 `ym=M` 與 agent 的 `ym=M` 指的是同一個時點。

### 測試 `test_all_operators_covered` 失敗

你加了 DSL 運算子但沒在 `tests/test_dsl_no_lookahead.py` 的 `FORMULAS` 加一條
用到它的公式。這是刻意的守門測試，不要繞過。

### learnings.md 被整理回合改壞了

每次重寫前都會備份到 `memory/learnings_history/learnings_<時間戳>.md`，直接
複製回去即可。`learnings.md` 也可以隨時手動編輯——它是給人看也給人改的。


### 2026-09-20 呼叫失敗與預算說明

失敗CLI／錯誤信封／逾時會記錄可得用量；未回報金額顯示未知，不能理解為免費。token或美元上限設0表示未限制；若啟用美元上限且出現未知金額，核對帳單前不啟動下一通。額度在每通呼叫前檢查，已在進行的一通不保證不超支。預算檔損毀時先修復紀錄，不應刪掉它讓預算歸零。

本週已發生但CLI未回報的費用，請以供應商帳單核對；修復程式不會自動追溯補齊先前漏記。
