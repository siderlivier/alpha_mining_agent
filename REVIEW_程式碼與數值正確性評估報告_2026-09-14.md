# 程式碼與數值正確性評估報告

> **2026-09-18 更新：** 原審查保留為歷史快照；第一批程式修正見第 9 節，後續已完成的快取重建見第 10 節。下文「尚未修正／重建」須按紀錄時間閱讀；最新狀態以第 13 節及 TODO 現況摘要為準。

> 文件建立：2026-09-14；詳細審查與實驗紀錄：2026-09-10。
> 報告建立時 Git HEAD：`1d034adbeb3bb3524123e7fd0326d7d02e1aa0e6`。
> 狀態：**僅評估，尚未修正**。此次只新增報告及更新檔案對照表，未修改程式碼、因子庫、資料或既有成果。
> **人類審閱文件：含 test 期個別因子及績效資訊，不應放入 Generate／Distill／Consolidate prompt 或 learnings.md。**

## 1. 評估結論與閱讀方式

本專案已有受限 DSL、分期評估、walk-forward、embargo、測試與記憶隔離等保護措施；但保護範圍沒有涵蓋整條資料與研究流程。本次確認的問題包括：參考因子選取使用 test、跨缺月計算下一月報酬、股數單位及面額假設、產業因子使用範圍失效，以及績效／成本／衰減計算錯誤。**目前成果數字不能直接視為已排除前瞻與選擇偏誤的樣本外績效。**

報告列出 27 項議題，其中部分是直接計算錯誤，部分是可重現的邊界故障，部分是研究結論的適用範圍問題。它們不等於 27 個已在正式執行中發生的事故，也不能把各項影響量簡單相加。完整修正後的 CAGR、IR、因子數及排名仍須重建資料並重跑才能確定。

### 嚴重程度

| 等級 | 定義 |
|---|---|
| P1 高 | 可能破壞樣本外有效性、主要資料／選股正確性，或正式研究資產完整性，應優先處理 |
| P2 中 | 特定條件下數值、篩選、成本或執行結果錯誤；影響範圍相對局部 |
| P3 低 | 統計顯示、報表或健全性問題，通常不直接改變底層投資報酬 |

### 證據分類

- **現況確認**：由既有資料、因子庫或輸出確認問題存在。
- **案例重現**：以記憶體資料或 mock 重現；不代表正式環境已發生。
- **靜態確認**：程式路徑可確認，但未對正式資產注入故障。
- **方法論風險**：計算未必違反其局部定義，但不足以支持目前較廣的結論。

下文路徑連結對應受檢檔案；行號為審查定位，後續修改可能使其位移。修正方向與驗收案例均為建議，**尚未執行**。

## 2. 專案理解與檢查範圍

### 2.1 核心流程

1. 上游日頻 PIT 面板經 build_base 轉成月頻資料，另匯入 DFS 參考因子及總經資料。
2. LLM Generate 提出假設與 DSL 公式，經語法限制、IC、去相關及品質門檻評估；另有產業專屬通道。
3. Distill 歸納結果，attempts 保存嘗試歷史，通過者寫入 library 與 factor_values。
4. Consolidate 使用聚合審計與歷史經驗整理記憶。
5. factor_lab 以 equal、ridge、LightGBM 合成因子，進行 walk-forward 與回測；ml_diagnose 評估過擬合、流動性等；HMM 支線依市場狀態切換因子組。

設定切分為 sub-train 2012-01～2017-12、validation 2018-01～2019-12、test 2020-01～2026-06。審查時月頻面板有 140,564 列、36 欄、958 檔股票、4 個產業群組；自有 26 個因子、參考 25 個因子，共 51 個；1,043 次 attempts、68 輪，其中 8 個自有因子為產業專屬。最後一個月沒有下一月報酬，實際 test 績效為 77 個月。

### 2.2 覆蓋檔案

從 README 開始，閱讀設定、欄位、prompt、規格與資料／結果文件，並檢視以下 21 個 src Python 檔案：

`admit.py`、`audit.py`、`backtest.py`、`build_base.py`、`build_regime.py`、`consolidate.py`、`dsl.py`、`eval_candidates.py`、`factor_lab.py`、`fetch_macro.py`、`hmm_regime.py`、`memory.py`、`mining_loop.py`、`mining_stats.py`、`ml_diagnose.py`、`query_attempts.py`、`report.py`、`seed_memory.py`、`seed_reference.py`、`threshold_sweep.py`、`unblock.py`。

亦檢視 tests 下 14 個 Python 檔案：conftest、DSL correctness／no-lookahead／validation、factor_lab、fetch_macro、funnel、hmm_regime、industry_track、m4、memory、ml_diagnose、pipeline_no_lookahead、seed_reference。為追查資料語意，另閱讀上游 tw_alpha_strategy 的 mine_dfs 相關程式；並非宣稱完整審查上游整個專案或第三方套件。

### 2.3 驗證及限制

既有測試結果為 **241 passed、6 warnings，116.64 秒**，Python 3.10.9。警告包括 1 個 joblib 實體核心偵測問題及 5 個 pandas concat FutureWarning。當時使用以下方式關閉 bytecode／pytest cache，將 mining log 導向暫存目錄：

```powershell
python -B -c "import sys,tempfile; from pathlib import Path; sys.path.insert(0,str(Path('src').resolve())); import mining_loop,pytest; mining_loop.LOG_PATH=Path(tempfile.mkdtemp(prefix='alpha-review-'))/'mining.log'; sys.exit(pytest.main(['tests','-q','-p','no:cacheprovider']))"
```

額外實驗使用既有本機資料、記憶體運算或 mock；沒有呼叫真實 LLM、重新抓取網路資料、完整重建管線或修正後重新挖礦。此次文件建立沿用上述審查紀錄，沒有把它描述為 2026-09-14 的完整重跑。測試通過代表已寫出的測試成立，不代表以下盲點已被排除。

## 3. 問題總表

| ID | 等級 | 問題 | 證據 | 對數值的影響 |
|---|---|---|---|---|
| R01 | P1 | 參考因子選取／去重使用 test | 現況確認 | 汙染參考池及下游選擇，影響樣本外解讀 |
| R02 | P1 | 下一列被當成下一月 | 現況確認 | 102 筆跨缺月標籤，27 筆位於 test |
| R03 | P1 | 股數單位與面額假設不一致 | 現況確認 | 換手、籌碼、估值及相關因子可能失真 |
| R04 | P1 | 產業專屬因子在全市場使用 | 現況＋實驗 | 現有 8 因子受影響；組合績效確實改變 |
| R05 | P1 | 用未來報酬是否存在篩選股票 | 現況確認 | 改變選股集合、標準化及 benchmark |
| R06 | P1 | 衰減比較混用全市場／產業範圍 | 現況確認 | 8 因子審計衰減與學習回饋錯誤 |
| R07 | P2 | MaxDD 漏掉初始淨值 | 案例＋現況 | 自有 ridge 回撤由 -15.99% 應為 -17.21% |
| R08 | P1 | 重疊聚合仍能推回個別 test 資訊 | 現況＋代數 | test 隔離保證失效，影響後續研究選擇 |
| R09 | P2 | 全期覆蓋率決定特徵集合 | 靜態確認 | 未來缺值可改變早期模型 |
| R10 | P2 | DSR／PBO 因子方向使用全期 IC | 靜態＋現況 | 潛在前瞻；現有 51 因子未觀察符號差異 |
| R11 | P1 | DSR 試驗池不足以代表整個搜尋 | 方法論風險 | 不宜將 DSR 解讀為完整挖礦流程已去偏 |
| R12 | P2 | 換手定義及空頭交易成本問題 | 案例重現 | 成本、淨報酬及成本敏感度受影響 |
| R13 | P2 | 負向候選方向未一致處理 | 靜態確認 | 改變候選通過率及因子庫 |
| R14 | P2 | 零換手被當成缺值 | 案例重現 | 合格低換手候選可能誤拒 |
| R15 | P2 | 四捨五入後才套門檻 | 靜態確認 | 邊界候選誤收／誤拒及排序變動 |
| R16 | P2 | 負 EPS 成長率經濟方向相反 | 靜態確認 | EPS 成長特徵及因子排序可能反向 |
| R17 | P2 | 負向參考因子未統一方向 | 現況確認 | equal 基準含反向訊號；ridge 不一定同樣受害 |
| R18 | P1 | 入庫跨檔案寫入非原子操作 | 靜態確認 | 失敗時中繼資料與因子值可能不一致 |
| R19 | P2 | JSON 解析器誤算字串中的括號 | 案例重現 | 合法 LLM 輸出可能解析失敗 |
| R20 | P2 | 候選 ID 重複導致診斷錯配 | 案例重現 | 公式與判斷結果可能對錯人 |
| R21 | P2 | 失敗 LLM 呼叫漏記 usage | 案例重現 | token／成本／預算數字低估 |
| R22 | P1 | mock 可寫入正式記憶 | 靜態確認 | 汙染因子庫、attempts、經驗及統計 |
| R23 | P2 | 部分總經更新覆蓋完整快取 | 靜態確認 | 其他來源欄位消失，下游樣本可能改變 |
| R24 | P2 | 殖利率日期排序及單位猜測 | 案例重現 | 月末值或數量級錯誤 |
| R25 | P2 | BetaExcess 混用 CAGR 與算術年化 | 案例重現 | 績效歸因不守恆 |
| R26 | P3 | 多頭月數與績效期間不一致 | 靜態確認 | 樣本數／敘述失真，非報酬本身 |
| R27 | P3 | 報表漏算產業通過、token 舊欄位及空庫除零 | 現況＋案例 | 通過率、token 顯示錯誤或報表失敗 |

## 4. 詳細問題、修正方向與驗收條件

### R01｜P1：參考因子的選取與去重使用了 test

**位置：** [seed_reference.py](src/seed_reference.py) 約 165–174、257–258 行；上游 `../tw_alpha_strategy/src/mine_dfs.py` 約 25、380–394 行。

匯入條件使用 ICIR_test 的絕對值、train/test 同號及衰減限制；上游 TRAIN_END 為 2019-12，test 為其後期間，與本專案 test 重疊。既有 dfs_candidates 與參考因子中繼資料的 icir_test 可對上。去重又使用全期因子值相關性，沒有 pre-test 截止點。

**影響：** reference／all 比較本身有選擇偏誤；自有候選 Stage 2 也會對這個經 test 挑選的參考池去相關，因此可能間接受影響。不能把結果解讀為「參考池完全在 2019 年以前固定」。尚未量化重建後因子數及績效，不能指定膨脹幅度。

**修正方向：** 選取及去重只用 train／validation，保存選取截止日與來源；已曝光 test 保留為探索性結果，另設真正未使用的 holdout。

**驗收：** 任意改寫 2020 年後的報酬及因子值，都不能改變 pre-test 參考名單、方向或去重結果。

### R02｜P1：跨缺月報酬被標成下一月報酬

**位置：** [build_base.py](src/build_base.py) 約 150 行。

`close.shift(-1) / close - 1` 找到的是同股票下一筆資料，沒有驗證月份連續。既有資料有 102 個跨月缺口，其中 27 個位於 test；舊價格快取經同樣 winsorize 後可重現 monthly_base 的標籤。

| 股票 | 起始月 | 下一筆月 | 間隔月數 | 原始跨期報酬 | 現有標籤 |
|---|---|---|---:|---:|---:|
| 1701 | 2024-08 | 2026-06 | 22 | -2.6005% | -2.6005% |
| 1785 | 2016-05 | 2017-01 | 8 | 96.5714% | 48.5446% |
| 2311 | 2018-04 | 2026-06 | 98 | -14.6067% | -14.6067% |
| 2448 | 2020-12 | 2026-06 | 66 | -11.9565% | -11.9565% |
| 2475 | 2019-05 | 2026-06 | 85 | 970.5882% | 26.6205% |
| 2499 | 2020-04 | 2026-06 | 74 | 157.2816% | 87.8442% |

**影響：** 不只是一個月報酬不準，train 的某筆標籤還可能包含數年後的資訊；一個月 embargo 無法消除。IC、訓練目標、選股績效均可能受影響。相同的「12 筆等於 12 個月」假設也須追查年增率。

**修正方向：** 使用完整月曆索引或顯式驗證下一月份；追查異常尾端資料與下市／停牌處理，不能只把缺值股票事先排除來掩蓋問題。

**驗收：** 1 月後直接接 4 月時，1 月不得產生一個月標籤；跨 train/test 的缺口不得把後期价格寫入早期目標。另驗證下市持倉的報酬政策。

### R03｜P1：股數換算及面額假設使特徵比例失真

**位置：** [build_base.py](src/build_base.py) 約 72、88、130 行；上游 mine_dfs 約 189–210 行。

換手與籌碼分母使用 `b_CapitalStock * 100`，市值使用 `price * (capital / 10)`。上游已有 shares_issued 與面額調整資訊，兩種本地假設並不等價於實際股數。

實際資料例：2330 的 capital 約 2.593237e11、issued shares 約 2.593237e10，現有換手分母約為實際股數的 1,000 倍；2327 的 capital 約 5.182415e9、issued shares 約 2.071465e9，比值約 250.18 倍。

**影響：** 固定共同倍數可能在單純橫斷面 rank 中抵消，但股票之間倍數不同就不能抵消。換手、外資流量、bp／sp 等可能受影響，並傳入 F002、F003、F008、F014、F016、F017、F019、F021、F022、F024、F026 等相關公式。這不是宣稱每個因子的每一筆排名都必然改變。

**修正方向：** 優先使用當時可得的實際發行股數；缺值 fallback 必須明確記錄 capital 單位、面額及生效日期。

**驗收：** 同時以一般與非 10 元面額股票驗算股數、市值、換手及籌碼比例；檢查公司行動前後連續性，再重算相關因子。

### R04｜P1：產業專屬因子被存成全市場值並用於 ML

**位置：** [admit.py](src/admit.py) 約 42–48 行、[memory.py](src/memory.py) 約 225 行、[factor_lab.py](src/factor_lab.py) 約 104 行。

入庫只在計算 sealed test 時切產業，傳給 memory 的仍是完整 fac；後續合併特徵未套用 scope。現有 8 個 scoped 因子都存在範圍外非空值。

| 因子 | 宣告範圍 | 非空總筆數 | 範圍外筆數 |
|---|---|---:|---:|
| F009 | 電子 | 122,563 | 46,411 |
| F013 | 電子 | 126,156 | 47,862 |
| F014 | 電子 | 118,422 | 42,212 |
| F016 | 電子 | 120,325 | 44,064 |
| F017 | 電子 | 119,978 | 44,161 |
| F018 | 電子 | 126,156 | 47,862 |
| F019 | 電子 | 120,642 | 43,194 |
| F025 | 生技 | 120,123 | 97,351 |

**影響與局部實驗：** 固定同一組 26 個 own 特徵，只在記憶體把 scope 外值設為 NaN，再沿原 prep 填 0 路徑執行 ridge。沒有重新套覆蓋率篩選，也未修正其他問題。

| 指標 | 原始結果 | 僅遮罩 scope 外值 |
|---|---:|---:|
| CAGR | 38.1888% | 37.6055% |
| Sharpe | 1.7721 | 1.7680 |
| 年化超額報酬 | 10.7203% | 10.2555% |
| IR | 1.5834 | 1.5304 |
| 平均換手 | 58.2302% | 58.4208% |
| 月數 | 77 | 77 |

這只證明作用範圍會改變现有結果，不是「全部修正後的績效」，也不能與其他修正影響相加。

**修正方向：** 明確定義 scope 在儲存、載入、標準化、缺值填補及預測中的一致契約。

**驗收：** 更改 scope 外的因子原始值不得改變組合分數；另確認填 0 是否真為預期中性值，以及覆蓋率分母是否符合 scope。

### R05｜P1：用未來標籤的存在與否決定當期股票池

**位置：** [factor_lab.py](src/factor_lab.py) 約 116 行、[backtest.py](src/backtest.py) 約 57 行。

訓練／評估資料先 dropna target；回測又在排名選股前刪除 fwd_ret_1m 為空的股票。審查資料在終端月份以前有 40 筆缺報酬資料。

**影響：** 即使 score 本身因果正確，未來是否有價格仍改變當期候選集合，進而影響標準化、持倉與 benchmark。40 筆不能直接全判定為下市損失，偏差方向須追查原因。

**修正方向：** 以當時已知資料決定可投資股票池，先產生持倉，之後按明確政策處理缺價格、停牌與下市。預測模式不應要求未來 target。

**驗收：** 對同一月、相同當期資訊，清空某股票未來報酬不得使選股程序事先換成另一檔；缺價應呈現為估值問題或明確錯誤。

### R06｜P1：產業因子的衰減分子分母範圍不一致

**位置：** [audit.py](src/audit.py) 約 56–65 行；[report.py](src/report.py) 的 train／validation／test 展示。

audit 用全市場 sub_train.icir 配上產業限定的 sealed test.icir，而非 industry_metrics.train_icir。現有 8 個產業因子皆受影響。

| 因子 | 全市場 train ICIR | 產業 train ICIR | 混用範圍的衰減約值 | 同產業口徑衰減約值 |
|---|---:|---:|---:|---:|
| F009 | 0.38 | 0.68 | -29% | 28% |
| F013 | 0.43 | 0.71 | 33% | 59% |
| F014 | 0.52 | 0.81 | -48% | 6% |
| F016 | 0.36 | 0.60 | 6% | 43% |
| F017 | 0.32 | 0.64 | -22% | 40% |
| F018 | 0.88 | 1.26 | 25% | 48% |
| F019 | 0.45 | 0.74 | -42% | 13% |
| F025 | 0.85 | 0.61 | 49% | 29% |

表內中繼數值已有取整，衰減僅供近似比較。

**影響：** 因子的退化程度甚至可能由正轉負；錯誤聚合又回饋 Consolidate，影響後續搜尋。報表並排數值也容易被誤讀。

**修正方向／驗收：** 每個 train、validation、test 指標攜帶相同 scope；產業案例的衰減須可從同一產業的未取整數值重算，禁止跨範圍比較。

### R07｜P2：最大回撤忽略期初資產 1

**位置：** [backtest.py](src/backtest.py) 約 106 行。

curve.cummax() 從第一期報酬後開始，首月虧損時遺漏期初高點。輸入六個月報酬 [-0.5, 0, 0, 0, 0, 0]，現行 MaxDD 為 0%，應為 -50%。

現有 own ridge test 的 long MaxDD 由 -15.9937% 應改為 **-17.2137%**；benchmark 由 -17.7350% 應為 **-18.4579%**；同次 long_short 為 -8.9294%，未改變。這是固定原報酬序列、僅補期初高點的結果。

**影響：** 低估風險，不改變該序列 CAGR 或 Sharpe。

**修正方向／驗收：** 納入期初淨值；測試首期下跌後不反彈、先漲後跌及全正報酬，並驗證其他績效欄位不受影響。

### R08｜P1：重疊聚合不能保證個別 test 資訊無法反推

**位置：** [audit.py](src/audit.py) 約 80、91 行及多維度分組輸出。

每組至少 2 個因子，只能防止直接輸出單因子組。若同時公布 A+B、A+C、B+C 的平均及樣本數，仍可聯立解出個體。現有分組的成員矩陣為 18×26、rank 15；F013、F014 的個體座標可被識別。實際輸出衰減取整為整數百分比，因此反推的是帶取整誤差的近似資訊，並非精確原值。

**影響：** 「看不到 fid 就學不到單因子 test」的保證不成立；即使無人主動反推，持續把 test 聚合餵入搜尋，也使 test 參與研究決策。未主張已發生蓄意利用。

**修正方向：** 回饋改用 validation；真正 sealed test 留在研究循環之外。若仍提供重疊 test 聚合，須撤回完全隔離的說法。

**驗收：** 不只搜尋輸出有無 fid，還要檢驗分組是否能代數識別個體，並測試 test 的任何變更不影響搜尋 prompt。

### R09｜P2：全期覆蓋率用於選擇模型特徵

**位置：** [factor_lab.py](src/factor_lab.py) 約 121 行。

以全期 notna.mean() ≥ 0.30 篩選因子，包含 test。未来缺值分布可能決定早期模型使用哪些特徵。現有資料尚未證明哪一因子因跨過門檻而實際換組。

**影響：** 特徵可用性的前瞻；不需直接讀取未来報酬值也可能發生。

**修正方向／驗收：** 特徵集合在 pre-test 固定，或每個 fold 僅以已知歷史决定；修改遠期缺值不得改變早期模型及預測。

### R10｜P2：過擬合診斷用全期 IC 決定因子方向

**位置：** [ml_diagnose.py](src/ml_diagnose.py) 約 756 行，_active_returns。

先對完整 sub 算 mean IC 並翻轉方向，再切 test。當 train 與全期方向不同時，未來資料會影響 test 策略定義。現有 51 個因子的 train 截止 2019-12 方向與全期方向一致，所以本次未觀察到現有符號因此改變。

**影響：** 潛在的診斷前瞻，不能以本次恰好同號證明實作正確。

**修正方向／驗收：** 方向只從歷史估計後鎖定；構造 train 負、test 強正的案例，確認 test 不能促使策略事後翻向。

### R11｜P1：DSR 的試驗池不足以支持整個挖礦流程已去偏的結論

**位置：** [ml_diagnose.py](src/ml_diagnose.py) 約 803 行及 DSR／PBO 結果解讀。

目前診斷池主要是存活因子與組合模型，約 54 條序列；完整挖礦已有 1,043 次 attempts，還有上游參考選取與後續模型選擇。對存活池計算 DSR，並不等於涵蓋整個搜尋歷史。

**影響：** 這是統計結論範圍問題，不是直接斷言 DSR 公式寫錯。不能把顯著性當成完整研究程序的去偏證明；也不能草率把 trial count 改成 1,043 並假設全部獨立。

**修正方向：** 記錄實際試驗序列、相關性及選擇程序，明確界定 DSR／PBO 評估的是哪個候選宇宙；使用獨立 holdout 或適當巢狀評估。

**驗收：** 結果附帶候選來源、納入／排除規則及未涵蓋的搜尋步驟；重複／高度相關試驗的處理可追溯。

### R12｜P2：換手不是權重交易量，多空報酬又漏空頭成本

**位置：** [backtest.py](src/backtest.py) 約 79、86 行。

換手採集合差異除聯集。固定 n 檔、替換比例 q 時，此值為 2q/(1+q)，不是常見的一側權重換手 q：換一半得 66.67%，而非 50%。若它只是明定代理指標，不是代數算錯，但目前直接拿它扣交易成本，且未反映權重漂移或 score 權重改變。

long_short 只扣 long 的 turnover。已用 mock 確認：多頭名單不變、空頭全部替換，第二期記錄的 turnover 仍為 0，空頭換倉未扣費。

**影響：** 成本敏感度與淨報酬失真；前者方向依成本定義而定，漏空頭費用則會高估多空淨報酬。這與文件已揭露的借券費缺省是不同問題。

**修正方向／驗收：** 明確定義雙邊／單邊成本及槓桿，依交易前後權重計算各腿交易額；測試半數替換、持股不變但權重變、僅空頭替換等案例。

### R13｜P2：負向候選沒有在漏斗中一致套用方向

**位置：** [eval_candidates.py](src/eval_candidates.py) 約 367、435 行。

Stage 1 可接受負向訊號，後續 Stage 4 卻要求正 ICIR，未先依宣告方向統一因子。具有穩定負向預測力的候選可能因此被拒；個別產業方向例外不代表整條漏斗已一致。

**影響：** 改變候選通過率與因子庫，無法只從現有 survivors 算出漏掉多少策略。

**修正方向／驗收：** raw 方向檢查與交易方向分開，保存 orientation，後續全部指標依一致方向計算；正負等價策略在定向後應有一致判斷。

### R14｜P2：合法的 0 換手被當成 1

**位置：** [eval_candidates.py](src/eval_candidates.py) 約 447 行。

`diag["turnover_m"] or 1` 把 0 視為 false。mock 診斷的 IC、ICIR、validation、decay、long 與 coverage 均合格，turnover=0 且無產業補救通道時，卻收到「月換手 0.0 > 0.4」的拒絕原因。

**影響：** 低換手候選誤拒，且訊息自相矛盾。

**修正方向／驗收：** 用 is None 等方式區別缺值與零；0、None、門檻上下各一個值分別驗證。

### R15｜P2：展示用四捨五入介入門檻決策

**位置：** [eval_candidates.py](src/eval_candidates.py) 約 320–325 行及後续 gate。

IC、ICIR、coverage、turnover、decay 先取整，再供篩選及排序使用。例如 ICIR 0.446 取為 0.45 後可通過 0.45 門檻；換手 0.404 取為 0.40 後可通過 0.40 上限。極低換手取為零還會觸發 R14。

**影響：** 邊界結果与原始門檻不一致，可能誤收或誤拒。

**修正方向／驗收：** 保留原精度決策、輸出時才格式化；針對每個門檻測上下相鄰值及取整臨界值。

### R16｜P2：負 EPS 的 pct_change 與獲利改善方向相反

**位置：** [build_base.py](src/build_base.py) 約 146–147 行及 [fields.yaml](fields.yaml) 的成長欄位說明。

以 pct_change(12) 表示 EPS 成長：-2 變 -1 得 -50%，虧損收斂卻被標為負成長；-1 變 -2 得 +100%，虧損擴大卻是正成長。這符合一般百分比公式的算術定義，但不符合「愈高愈改善」的經濟解讀。

**影響：** F022、F024 等依赖 EPS 成長的公式可能排序相反；另須處理 R02 的非連續月份。

**修正方向／驗收：** 明確選定差額除以前期絕對值、盈虧轉換分類或其他合理定義，不能宣稱只有一種通用正解。測試負轉負、負轉正、正轉負及零分母，並同步欄位描述。

### R17｜P2：負向參考因子直接進 equal 平均

**位置：** [seed_reference.py](src/seed_reference.py) 約 398 行及 [factor_lab.py](src/factor_lab.py) equal 合成。

参考選取接受負向因子，但保存原值未統一定向。R023 neg_debt_ratio 的 train ICIR 約 -0.33924；equal 將標準化原值直接正向平均，與「把每個有效訊號依獲利方向合成」的解讀不同。

**影響：** equal 基準可能混入反向訊號；ridge 可學負係數，不能推論它同樣受影響。若故意定義 raw equal，應明示此限制，避免不公平比較。

**修正方向／驗收：** 決定並記錄共同基準的方向政策，方向只能來自 train；因子與其相反数在對應定向後應產生相同基準訊號。

### R18｜P1：入庫的 metadata 與 parquet 寫入不具原子性

**位置：** [memory.py](src/memory.py) 約 222–225 行。

先保存 library.json，之後才 stack／寫 factor_values.parquet。若後段失敗，可能有 metadata 無因子值，重試又被公式 hash 重複檢查擋下。现有兩邊 ID 集合一致，未觀察到已發生的不一致；本次沒有對正式檔案注入故障。

**影響：** 研究資產可能部分提交，影響可用特徵數、重跑及恢復。

**修正方向／驗收：** 以暫存檔、提交 manifest 或可恢復交易協定處理跨檔一致性。暫存測試目錄模擬每個寫入點失敗，確認可回復或完成提交，且重試不會永久卡住。

### R19｜P2：JSON 擷取忽略字串中的括號

**位置：** [mining_loop.py](src/mining_loop.py) 約 339 行。

手寫括號計數沒有處理 JSON 字串狀態與跳脫。合法資料 `[{"lesson":"x ] y"}]` 會得到 JSONDecodeError；`[{"lesson":"x [ y"}]` 會被判未閉合。

**影響：** 合法 LLM 回覆無法使用，浪費呼叫並使回合失敗。

**修正方向／驗收：** 使用標準 JSON decoder 擷取完整值，或完整處理字串／跳脫狀態；測試括號、引號、反斜線及前後說明文字。

### R20｜P2：重複 candidate ID 使不同公式共用診斷

**位置：** [mining_loop.py](src/mining_loop.py) 約 469 行及 [eval_candidates.py](src/eval_candidates.py) 以 cid 對應結果的路徑。

沒有先驗證唯一 ID。案例將 `cs_rank(x)` 與 `bad(x)` 都命名為 a，後續對應會讓兩筆取得同一份最終診斷，無效公式可能被配到有效公式的結果。

**影響：** 歸因、attempt、通過／入庫流程可能錯配；並非表示目前因子庫已證實含此種錯誤。

**修正方向／驗收：** 批次開始前驗證 schema、ID 型別與唯一性，診斷與來源須一對一；重複 ID 應明確拒絕整批或安全重新編號。

### R21｜P2：非零結束碼的 LLM 使用量未計入預算

**位置：** [mining_loop.py](src/mining_loop.py) 約 286 行。

在解析 usage 前，先因 returncode != 0 丟錯。mock 回傳 input=100、output=50、cost=0.1 的錯誤結果，meter 仍為 0。

**影響：** 重試及失敗呼叫有費用時，token／成本統計低估，預算限制也會失準。

**修正方向／驗收：** 可解析的失敗 envelope 先計 usage 再處理錯誤，避免重複入帳；區分未知用量與真正零用量。

### R22｜P1：mock 模式仍使用正式 Memory 路徑

**位置：** [mining_loop.py](src/mining_loop.py) main／run_round 約 704–721 行；[consolidate.py](src/consolidate.py) mock 路徑。

mock 主要替換 LLM 提供者，並未自動隔離 Memory；run_round 仍可寫 attempts、因子庫、learnings 與預算。Consolidate 的 mock 也可能把簡化內容寫入正式 learnings。本次只做靜態追查，沒有執行正式目錄的 mock 命令。

**影響：** 使用者以為試跑卻汙染正式研究資產，進而改變因子數、統計與之後搜尋。

**修正方向／驗收：** mock 預設使用獨立暫存根目錄；測試執行前後正式 memory 與預算檔 hash 必須完全不變。

### R23｜P2：部分總經更新覆蓋其他來源欄位

**位置：** [fetch_macro.py](src/fetch_macro.py) 約 795 行。

目前以此次取得的 parts 重建並覆蓋 OUT。只執行某來源或一個來源失敗時，先前成功保存的其他來源可能消失。

**影響：** 總經特徵、可用歷史與依賴它們的模型改變；無法只以本次輸出筆數判定完整性。

**修正方向／驗收：** 依來源分區更新或合併既有資料，保留失敗來源並標示新鮮度；測試單來源更新及部分失敗時其他欄位／歷史不變。

### R24｜P2：殖利率月末選值依輸入順序，單位依數值猜測

**位置：** [fetch_macro.py](src/fetch_macro.py) 約 661–665 行。

未先按日期排序就 group.last；mock 以 1/31=4、1/2=3 的降冪資料輸入，月值得到 0.03 而非 0.04。另以 median > 0.5 判斷是否除 100，低利率 0.25% 可被誤當成 25%。全期中位數變化還可能改寫較早資料的單位解讀。

**影響：** 月末值或數量級錯誤。審查時現有 macro 的 8 欄不包含該殖利率欄位；不能把這項潛在影響算進當前純價格 HMM 的績效。

**修正方向／驗收：** 固定來源單位，顯式排序日期；輸入順序反轉不應改變結果，0.25% 應轉為 0.0025，追加未來資料不得改變歷史單位。

### R25｜P2：BetaExcess 的年化口徑造成歸因不守恆

**位置：** [hmm_regime.py](src/hmm_regime.py) 約 652 行。

使用 `(beta - 1) * benchmark_CAGR`，但 alpha／超額以月平均乘 12 算術年化。要符合線性迴歸分解，beta 貢獻應搭配相同算術年化 benchmark。

案例 benchmark=[-0.2,0.3] 重複六次、long=0.8*benchmark+0.01：真實算術年化超額為 0，但目前分解總和約為 0.06694；正確 beta 貢獻為 -0.12，可與 alpha 抵銷。

**影響：** 歸因錯誤，不直接改變既有月報酬。小樣本低波動測試中的小誤差不代表誤差永遠受限。

**修正方向／驗收：** 統一算術或明確定義的幾何歸因；合成 beta／alpha 模型應在數值精度內守恆。

### R26｜P3：多頭月數與績效評估區間不同

**位置：** [hmm_regime.py](src/hmm_regime.py) 約 498、791 行；[threshold_sweep.py](src/threshold_sweep.py) 約 49 行。

月數可能統計整個處理區間的 bull，而旁邊績效只取 test，讓讀者誤以為是同一樣本。

**影響：** 影響樣本數與解讀，不直接改變已切片的 CAGR。

**修正方向／驗收：** 所有樣本數與績效採用相同 evaluated index；加入額外 train 月份不應改變 test 月數。

### R27｜P3：人類報表的通過率、token 及空庫處理不完整

**位置：** [report.py](src/report.py) 約 37、96–118、194–205 行。

分類／總數只計 passed，漏掉 passed_industry，現況 26 個通過者只顯示 18 個，入庫比例約由 2.49% 顯示成 1.73%。token 仍讀舊欄位 est_tokens_used，顯示 0，而現有 tokens 為 504,717。空庫 HTML 路徑除以 n，已重現 ZeroDivisionError。

**影響：** 研究操作統計錯誤或報表無法產生，不直接改變底層回測報酬；另有 R06 的跨範圍展示問題。

**修正方向／驗收：** 統一成功狀態與 usage schema，處理零分母；以全市場通過、產業通過、失敗混合樣本及全空樣本驗證各種輸出格式。

## 5. 已知假設與問題邊界

以下事項需要揭露，但不應全部重新命名為本次新發現的程式 bug：

1. **報酬 winsorize 是既有設計。** build_base 每月把報酬裁到 1%／99%，可重現 2,952 筆被修改的值。這會改變訓練與回測，但規格已有記載。若要宣稱可交易真實報酬，須區分錯價與合法極端波動，不能單純裁尾後視為原始投資報酬，也不能假設取消裁尾就修好資料。
2. **借券費等限制已有揭露。** 與 R12 另外漏掉空頭換倉成本是不同層次。
3. **test 已反覆被研究。** 更改程式不能使已使用過的樣本重新成為未觸碰 holdout；既有結果需保存研究脈絡。
4. **快取與更新後上游規模不同。** 上游已有約 1,616 檔／6 群組，而受檢月頻快取仍是 958 檔／4 群組，這在專案脈絡中已有記錄。重建時應一起處理資料版本與快取失效，不能新舊結果混用。
5. **欄位文件有漂移。** fields.yaml 實際 32 個欄位，部分文件仍寫 27；本次未確認實際公式有白名單失配，不能因 README 示例不同就判定現有公式無效。
6. **核心 DSL 未新增確認缺陷。** AST 白名單與無 eval 設計，以及 HMM 的 causal forward filtering 與 expanding-window 測試，都是有效保護；它們不會自動保證上游標籤、股票池或研究選擇沒有前瞻。

## 6. 現有測試為何沒有攔住問題

| 測試覆蓋邊界 | 未涵蓋風險 | 建議補強 |
|---|---|---|
| pipeline 測試從合成 monthly_base 開始 | 原始股數單位、月曆缺口已在輸入之前發生 | 加 build_base 的小型端到端資料案例 |
| walk-forward 使用固定特徵集合 | 全期 coverage 決定是否入模 | 未來缺值擾動不影響歷史模型 |
| 產業測試偏重 metadata／通過狀態 | 儲存與 ML 沒有真正限制 scope | scope 外擾動不影響分數 |
| audit 測試檢查輸出不含 fid | 重疊分組可聯立反推 | 成員矩陣可識別性與 prompt 隔離 |
| 績效測試未涵蓋期初大虧 | MaxDD 忽略期初高點 | 首月下跌、後續不反彈 |
| 參考去重測試檢查相關性 | 選取／去重時間截止點 | 修改 test 不影響參考池 |
| 正常流程／成功呼叫 | 部分寫入、失敗 usage、重複 ID、字串括號 | 暫存目錄故障注入與合法邊界輸入 |

新增測試應針對行為不變量與可觀察錯誤，不只複製實作公式。

## 7. 建議修正順序與交付驗收

### 第一階段：固定研究邊界與保護資產

- 處理 R01、R08、R09、R10、R11，界定選取期間、方向、試驗池與真正 holdout。
- 處理 R18、R22，確保入庫可恢復、mock 隔離。
- 保存既有資料版本、因子公式、attempts 與原始結果，勿覆寫歷史來製造「從未出錯」的紀錄。

**驗收：** 修改 test 不影響此前選取與 prompt；測試失敗不汙染正式 memory。

### 第二階段：修正資料、作用範圍與報酬契約

- 優先 R02、R03、R04、R05，再處理 R16、R23、R24。
- 定義股數來源、完整月曆、下市估值、產業 scope、成長率及資料單位。
- 重建 monthly_base 與相關因子值，保存新舊差異與來源版本。

**驗收：** 月份與單位案例通過；scope 外不影響結果；可投資集合不依赖未來報酬存在與否。

### 第三階段：修正評估與執行邏輯

- 處理 R06、R07、R12～R15、R17、R19～R21、R25～R27。
- 使用未取整原值判斷門檻，統一方向、成本及歸因口徑。

**驗收：** 每項最小重現案例由失敗轉為通過；完整既有測試仍通過；新增驗證均不需要寫入正式研究資產。

### 第四階段：重新產生結果並更新結論

- 先固定修正後資料與設定，再重跑因子評估、組合、回測與診斷。
- 對照因子數、名單、IC／ICIR、CAGR、IR、MaxDD、成本及樣本數。
- 更新 RESULTS／README 時清楚列出版本、修正影響與剩餘限制；已曝光 test 仍標為探索性，不能當成新 holdout。

**重要限制：** 本報告未執行以上修正或重建，不能作為「問題已排除」的證明。R04 與 R07 的局部重算只用於量化特定問題，不代表其他問題沒有影響。

## 8. 後續追蹤欄位

27 項目前皆為 **待處理／未修正**。後續建議逐項保留：負責人、修正 commit、驗證命令、測試結果、資料版本、新舊數值差異及接受的剩餘限制。方法論議題可採重新界定結論範圍處理，但須記錄理由，不能只以「測試通過」關閉。

本文件建立時工作樹為乾淨狀態；文件交付的允許變更為本報告與 FILES_檔案對照表.md。程式碼、設定、記憶庫及資料不在此次修改範圍。

## 9. 2026-09-18 第一批挖礦修正

使用者本次授權修改程式，優先處理挖礦與入庫判斷，HMM／測試期评估修正延後；另明確授權純空頭因子入庫並標記用途。這與原只讀審查是不同階段。

| 原問題 | 本批實作 | 邊界 |
|---|---|---|
| R13 負向方向 | neg 以 -1 定向；raw_mean_ic 保存原方向；Stage 1 仍檢查原假設一致性。後續評估、入庫、密封指標及 crosssec 重算統一定向 | 不依 test 決定方向；舊 metadata 缺 value_orientation 時沿用 +1 |
| R14 零換手 | 明確區分 None 與 0，合法 0 可通過 | 未改回測成本公式，R12 仍待處理 |
| R15 先取整後判斷 | IC、ICIR、衰減、coverage、兩腿、換手與資格排序保留原精度 | 年度／條件 IC 等純展示欄位仍可取整 |
| R16 負獲利成長 | EPS／營業利益改為 (本期−去年同月)／abs(去年同月)×100；精確月曆配對，零分母／缺月為缺值 | **僅完成程式修正，現存 monthly_base／factor_values 尚未重建，歷史數字未更新** |
| R20 ID 錯配 | evaluate／admit 拒絕重複 ID；入庫核對公式及定向一致性 | 其餘 LLM 解析／schema 問題仍待處理 |

### 交易用途與定向契約

- allow_short_only=true：其他門檻合格時允許純空頭腿候選入庫。trading_use 分成 long_only、short_only、long_short；兩腿皆非正為 unusable。
- 有效腿沿用「相對當月股票池均值的年化超額 > 0」定義，不是含借券費後的絕對獲利保證。
- value_orientation 記錄儲存值相對原公式的乘數；formula 與 direction 保存原假設。讀取已儲存值不再翻向，重算原公式才套用乘數。
- short_only 換手使用定向後底部股票的換手；產業通道以該產業的兩腿判斷用途。
- factor_lab 多頭組合自動排除 short_only，明確指定則報錯。報表及 library_summary 顯示用途與定向。
- 未新增純空頭交易回測／借券執行系統，入庫不等於可直接實盤。

### 名稱及後續需求

F-025 更名為「生技毛利回升與營收背離」，name_history 保留舊名「半導體毛利拐點股」與日期。公式、category、scope、來源 attempt 與歷史績效均保留。

產業工具規則已確認：只允許新增產業通過 train／validation 重測後擴大 scope，不重加舊排除產業；新產業不合格則不加入。現有 industry_ic_series、industry_qualify、crosssec_oos 可重用；groups_seen、套用／備份流程及多產業 scope 下游相容性**尚未實作**。新增產業不自動代表統計獨立，重測後挑選也會消耗樣本外性。

「參考因子保留舊版紀錄並改用新因子」尚未執行。現有 --clear／--apply 不足以排除 R01 時間洩漏或保證完整版本追溯，不能宣稱已完成。

### 驗證及資料遷移邊界

完整測試：255 passed、6 warnings（84.15 秒）；警告為既有 joblib 核心偵測與 pandas concat FutureWarning。之後另補純空頭入庫與原精度批內排序兩項案例，單獨驗證。測試入庫使用暫存目錄，mining log 亦導向暫存，未啟動真實 LLM。

回歸案例包括門檻取整邊界、零換手、负向評估至入庫、純空頭用途／換手、重複 ID、精確月曆獲利成長與多頭組合排除純空頭。

本批未覆寫 attempts、learnings、monthly_base、factor_values 或歷史績效。**繼續正式挖礦前，仍需備份並重建成長欄位及受影響因子值，分開保存歷史入庫判斷與新版重測結果。** R16 尚不能在資料層結案；本批不代表其餘挖礦問題或整體樣本外有效性已修復。

## 10. 2026-09-18 快取及自有因子值重建完成

本次使用者明確要求完成資料快取及受影響因子值重建。工作 ID：`20260918_115917_growth`，manifest 狀態為 `committed`。

### 10.1 範圍與結果

檢查發現 monthly_base 已是 1,616 檔／6 產業，但 26 個自有因子值均仍來自舊股票池，且每個因子的共同鍵都有數值差異。因此此次不只更新直接使用成長欄位的 F-014、F-016、F-022、F-024，而是同步重算全部 26 個自有因子。

| 項目 | 重建結果 |
|---|---|
| 月頻面板 | 239,919 列 × 36 欄，1,616 檔／6 產業 |
| 其他欄位 | 股票、月份、產業及其他 34 欄與重建前完全一致 |
| EPS 成長 | 51,148 列實質變更 |
| 營業利益成長 | 58,766 列實質變更 |
| 自有因子 | 26 個；3,125,909 筆舊值 → 4,470,861 筆新值 |
| 參考因子 | 25 個、3,201,065 筆，逐筆完全保留 |
| 合併因子快取 | 7,671,926 筆 |
| 無面板對應鍵 | 排除 1,266 筆由滾動計算產生、但該股票該月未在面板中的值 |

成長欄位「實質變更」以 rtol=1e-12、atol=1e-12 排除浮點計算順序微差；若要求位元／精確數值相同，兩欄各有 129,259 列不同。NaN 與 NaN 視為一致，新增／消失的缺值計入變更。

8 個產業限定因子仍使用原 scope；本次重建時已移除其 scope 外的可用值。全市場因子依現有完整面板計算。**值已計算不代表新增產業已通過資格重測**；未更改 library 中的 scope，也未宣稱完成自動產業擴張。舊的 scope=None 行為仍待附錄 F 的明確政策與版本遷移。

這次移除了目前快取的 scope 外值，但 R04 的整條未來入庫流程仍須統一遮罩契約，不能只靠一次重建即宣告永遠不再發生。

### 10.2 驗證與歷史保存

- 重建先寫 staging，驗證後才替換正式 monthly_base 與 factor_values。
- 成長欄位用獨立的「去年同月 merge」方法逐列交叉核對。
- F-014、F-016、F-024 另以 pandas 產業內 rank 手算核對；F-022 另以 12 月窗口（最少 8 筆）、安全除法及產業內 rank 核對。全部 26 個公式使用現有 DSL 及保存的 value_orientation。
- 檢查全部自有因子 ID、數值有限性、唯一 factor_id／ym／stock_id、面板鍵存在性及原產業範圍。
- 參考因子與備份逐筆比較完全相同；library.json 位元雜湊不變，原始入庫指標、密封 test、公式與名稱歷史均未覆寫。
- 替換前確認上游資料、相關程式、設定、欄位及原正式檔案 hash 未變；替換後確認正式檔案等於已驗證 staging 的 hash。
- 未重新判定舊因子是否通過、未重新挖礦、未重算宣傳績效；attempts 與 learnings 保留。
- 正式快取更新後執行 test_funnel 與 test_pipeline_no_lookahead：**22 passed、5 個既有 pandas FutureWarning，66.22 秒**。

本次是有備份、先驗證後替換、例外時可回復的兩檔遷移。它尚不是附錄 F 所提議的單一 generation 指標切換；未來多工作同時運作仍需要工作鎖與一致版本讀取。

### 10.3 備份、差異與回復

本機備份目錄：[data/rebuild_history/20260918_115917_growth](data/rebuild_history/20260918_115917_growth)。

- [manifest.json](data/rebuild_history/20260918_115917_growth/manifest.json)：輸入／輸出 hash、上游版本、程式版本、狀態與逐因子差異。
- [factor_changes.json](data/rebuild_history/20260918_115917_growth/factor_changes.json)：每個因子的新增、移除、共同鍵變更筆數。
- monthly_base.parquet、factor_values.parquet：更新前原始檔案。
- library.json、config.yaml、fields.yaml：本次來源中繼資料快照。
- *.staged.parquet：驗證後重建值，便於重現比較。

回復時先停止寫入工作，核對 manifest 中 inputs 的 hash，將兩份舊 parquet 一起還原，再確認正式檔案 hash 對得上。不可只回復其中一份。若後續已有新的因子入庫，不得直接覆蓋成此舊版，須先處理後续差異。

備份目錄不進 Git，避免大型重複檔案膨脹；本報告保留可追溯摘要。本機備份不等同異地備份。

### 10.4 剩餘限制與設計交付

R16 本次已完成程式與現有資料／自有因子值遷移；原歷史績效仍屬舊版本，不能直接沿用成新版績效。R02 跨缺月標籤、R03 股數、R05 股票池等其他未修正問題仍在，不因重建成長欄位而自動消失。

25 個參考因子仍是舊版本；本次沒有執行 --clear 或換版，亦未解決 R01 的選取洩漏。產業擴張、參考換版及 Web UI 的具體程式設計已加入 [SPEC 附錄 F](SPEC_架構設計規格書.md#附錄-f資料擴張自動產業範圍與參考因子換版設計2026-09-18-提案)，均清楚標記為尚未實作的提案。
# 第 11 節：資料擴張功能驗證（2026-09-18）

新增共用產業檢查／提交核心與本機 Web UI，使用既有產業 IC 資格函式，僅以 train／validation 決策。正式資料只讀檢查涵蓋 1,616 檔股票、6 個產業、26 個自有因子；6 個因子符合新增產業條件，分類來源一致性衝突為 0。本次未套用至正式因子庫。

完整測試 273 項通過，包含合成資料下的舊排除保護、樣本不足待測、固定空頭用途、test 污染隔離、雜湊過期、鎖、提交失敗回復及 HTTP 保護。6 個警告涉及 joblib 核心數偵測及 pandas 未來行為；此次測試未失敗。瀏覽器工具回報分頁工作階段錯誤，未完成視覺／點擊驗收。

數值影響：核准後只有 approved_groups 內的因子值供正式使用；原始入庫指標保留為歷史。擴張資格不等於 test 期盈利保證，也不解決本報告先前所有資料與回測問題。詳細操作見 `SCOPE_資料擴張操作說明.md`。


## 12. 2026-09-19 三組高優先修復與正式遷移

### 程式與資料處置

| 議題 | 本次結果 | 邊界 |
|---|---|---|
| R02 | 本專案使用股票＋精確月份尋找下一月價格；營收年增／月增也改精確月曆配對 | 缺月／停牌／下市且無可用次月價格時標籤缺值；不杜撰終止持倉報酬。R05 的下游股票池選擇另待修 |
| R03 | 日頻 PIT shares_issued 同時用於換手、法人流量及月末市值；不再以股本×100或固定10元面額推算；市值只使用原始價 | 股數有效涵蓋率約95.1%；缺值不猜測。上游資料正確性與公司行動來源仍是外部資料契約，本批未改上游程式 |
| R01 | 新版固定候選池是 fields.yaml 全部32欄；不用上游 test 篩選後的 DFS 名單／CSV；訓練定向、訓練與驗證篩選及去重 | 既有研究已看過 test，不能把歷史績效重新稱為全新 holdout |
| R17 | 入選負向欄位取負儲存，metadata 記錄 orientation，名稱／描述標記反向 | 現有18個參考因子的值已逐筆核對方向；equal 不再把已知負向訊號原值直接加總 |
| R18 | Memory.commit_snapshot 共用互斥鎖、雙檔備份與提交日誌；異常回復兩檔；讀取前恢復未完成交易；失效程序鎖由 --recover 處理 | 保護使用 Memory API 的讀寫；直接從外部讀寫 parquet／JSON 者不受鎖契約保護。不是任意歷史版本回溯 UI |
| R22 | mining_loop --mock 使用獨立 Memory／Context／budget／log；consolidate --mock 使用暫存記憶及輸出；API 拒絕 mock 寫正式 Memory | 實跑一輪 mock，4筆attempt、2個入庫均在隔離目錄；正式版本未被 mock 修改 |

### 正式數值變更

- monthly_base 維持239,919列、1,616股票、6產業。
- 103筆跨缺月標籤改為缺值；重新逐月 winsorize 後總計1,905筆 fwd_ret_1m 改變。
- turn_21 改變238,266筆、frgn_net_21 改變228,004筆、trust_net_21 改變95,567筆。
- bp 改變44,336筆、sp 改變62,218筆；bp 缺值由42,326增至51,273，主因不再用猜測股數補值。不是所有欄位缺值都下降。
- 26個自有因子值已重建；原始入庫指標保持歷史原值。原25個DFS參考由18個新參考取代（R-026～R-043），舊metadata／值留在備份。
- 先前6個新增產業重測後5個仍通過；F-026營建 train ICIR=0.5573116、valid=0.5326131，因train<0.60撤回新增範圍，原產業保留，未重加任何舊排除產業。

### 驗證與備份

完整測試 **281 passed**，另有6個既有警告（joblib核心偵測、pandas未來行為）。新增缺月／股數、提交失敗雙檔回復、模擬中斷讀取復原、參考選取與去重 test 隔離、mock正式路徑阻擋測試。實跑隔離挖礦與隔離整理。

遷移备份與欄位差異：`data/rebuild_history/20260919_234830_priority_repairs/manifest.json`；範圍重測明細：同目錄 `expanded_scope_recheck.json`。

一般資料提交交易：`352771c3e5b349f6a4a65545e6dafea8`；參考換版交易：`793c1a0a81044c00af68e7f9a04303e4`，均在 `memory/transactions/` 保留前版。最後產業提交為 `20260919_235425_6f5fb8c4`，其報告在 `memory/scope_runs/`。

本批修復不等於原27項議題全部結案；R05、回測成本、過擬合研究方法與HMM等仍按原先優先順序處理。舊績效文件已標示歷史版本，未宣稱新版策略報酬。


## 13. 2026-09-20 R19／R21 挖礦執行與成本記錄修復

R19：`parse_json_array` 使用 JSONDecoder.raw_decode，能正確處理字串內方括號、巢狀陣列與跳脫字元；不拆取損壞外層陣列中的內層片段。移除全域刪除markdown圍欄的做法，避免改寫合法字串內容。consolidate物件解析同樣保留字串內圍欄。

R21：成功、非零退出、is_error信封及逾時都先將可得usage／cost計入Meter；失敗後外層finally仍提交預算。可解析的逾時信封採真實回報，缺usage才估算token。沒有金額回報時維護unknown_cost_calls，顯示的金額只代表已知部分，不等於零成本。

新增保護：budget.json以暫存檔替換；JSON損毀停止執行，不歸零。每通真實CLI呼叫（包括解析重試）前檢查已記錄用量＋本輪尚未提交用量；若設美元上限且有未知金額則停止下一通。設定未變：token與美元上限0仍表示未限制。這不是供應商端硬額度，單通最終費用無法事先保證；程序硬中止及歷史漏記仍須以供應商帳單核對，未聲稱可自動還原。

### 研究目的校正

使用者主要要比較agent自有因子與前置專案DFS。先前改為18個基礎欄位，解決了新的篩選流程但改變比較對象；2026-09-20九組表應明確解讀為「agent vs 基礎欄位」，不能回答「agent vs 舊DFS」。後續應另列並重建同一資料版本的DFS比較組，保留固定名單的歷史比較與只用train/validation選取的評估流程，兩者不能混稱全新holdout。

可信度優先事項：兩组共用資料、可交易股票池、成本與期間；R05股票池不得由未來標籤決定；R09特徵覆蓋只能使用當時訓練資料；R08不得把可反推test的重疊聚合送回LLM；固定模型與規則後，以尚未參與選取的後續資料追蹤。已被看過的test不能藉重新命名或重分割恢復獨立性。

驗證：完整測試292 passed，6項既有警告；另實跑一輪隔離mock（4個候選、2個模擬入庫、2通模擬呼叫）。本次沒有真實付費LLM呼叫、沒有修改正式因子庫或重新選擇模型。

## 14. 2026-09-20 聚焦挖礦與回測正確性

使用者指定：R12 優先維持前置成本口徑；總經為 HMM 所下載，HMM 已放棄，相關事項先暫緩、最後再評估封存。R10／R11／R25 本次說明，尚未修改診斷方法。

| 編號 | 本批處置 | 邊界 |
|---|---|---|
| R04 | Memory.admit 以完整 group_map 驗證核准範圍；入庫、Stage 2 及模型載入遮罩，缺映射拒絕入庫 | approved_groups 代表目前用途，industry_scope／原指標保留入庫歷史 |
| R05 | load_panel 保留未知標籤列、只在訓練過濾 y；Context 股票欄位不由未來標籤決定。回測先依 score 選股，任何所需持股／基準報酬缺失即 MissingReturnError，附固定持股名單；perf 不跳過缺月 | **部分完成**。停牌、恢復交易、清算與估值仍待處理；沒有生成無缺口完整績效。compare_upstream 的前置相容診斷保留既有警示 |
| R06 | audit／text／HTML／Markdown 報表共用 admission_metrics；scoped train／valid 從 industry_metrics 取得，衰減按同一原精度分母重算，缺少同範圍資料不退回全市場 | 這是歷史入庫範圍，不是假稱新增核准產業後的重算結果 |
| R07 | 淨值高水位至少為初始 1，首月損失計入最大回撤 | 不改同一完整序列的報酬及 Sharpe |
| R08 | 預設 audit 僅用 validation，test 只供 human；Generate／Consolidate 排除舊 [audit] 經驗，新條目使用 [audit:validation] | 已確認正式歷史檔有 1 條舊規則，prompt 為 0 條；原檔不刪。無標記歷史衍生影響無法追溯消除，不是新的 holdout |
| R09 | 特徵覆蓋固定於首個 walk-forward 訓練截止以前，且不超過 sub_train；不使用後續缺值 | 改變特徵選取的資料範圍，舊績效不自動成為新結果 |
| R27 | passed＋passed_industry 同時計入、token 優先 tokens_used、空庫 HTML 零分母保護 | 正式 attempts 為 18＋8＝26 個通過，已實產人類報表驗證 |

驗證：完整 307 passed（6 個既有警告）；最後收尾另跑 36 項目標測試通過。新增案例涵蓋同產業衰減、sealed test 變更不改完整整理 prompt、舊 test 經驗隔離、scope 外值遮罩、未來覆蓋不改特徵、缺報酬不換股、基準缺價明確失敗、初始回撤及報表空庫／產業通過／token。

新的人類報表保存在 `logs/research_repairs_20260920/human_report.html` 及 `.md`，沒有覆寫既有歷史報表。本批沒有真實 LLM 呼叫、修改正式因子值或更新前置程式。

### R12：前置相容口徑的兩個不同問題

名單換手為 `|新舊名單對稱差| / |聯集|`；等檔數更換一半時為 66.67%，不是一側權重換手 50%。明確稱為名單換手近似且兩邊一致，可以保留作相對比較，但不能宣稱完整實際成交費用。使用者指定下，本批沒有改這個公式。

long_short 只扣 long 成本：多頭名單不動、空頭全部換掉時仍可能零成本。這是真實的空頭費用遺漏，會高估多空淨績效；目前主要多頭比較不受這個「空頭漏扣」項目影響。已修正文檔名稱，R12 不視為完整修復；若日後以多空淨績效為主，須另統一兩專案的空頭費用。

### 總經依賴與暫緩範圍

32 個 mining 欄位及 monthly_base 沒有接入 `macro_monthly.parquet`；程式讀取該檔的路徑在 HMM.attach_macro，沒有進入現行 DSL 因子設計或挖礦 prompt。故 R23 總經快取、R24 殖利率，以及 R25 HMM 歸因、R26 HMM／threshold_sweep 月數一起暫緩。

`market_monthly.parquet` 和 `regime_table.json` 是大盤報酬與年度多空標註，由 build_regime 產生，Context／候選評估仍使用；它們不是該總經下載檔，不能因 HMM 停用一併刪除。

### R10／R11／R25 的具體意義

R10：`ml_diagnose._active_returns(orient=True)` 在切回報期間以前，對全期算平均 IC 再翻向。若訓練時正向、test 強負，全期負向會讓診斷事後改成反向買，等於用 test 改答案。應只在歷史期定向並固定，或沿用已定向因子值。這是 DSR／PBO 診斷路徑問題，不是宣稱先前 LightGBM 的所有主回測分數都因此翻向。

R11：DSR／PBO 主要用最後存活的因子＋合成模型建試驗池。歷史 1,043 次 attempts、淘汰者、上游參考池篩選等未完整納入；因此只能描述目前提供的候選池，不能當作整段挖礦搜尋已去偏的證明。不能把 N 直接改為 1,043 假裝試驗都獨立；需要完整試驗血緣、報酬序列與相关性處理，並保留真正獨立的後續評估。

R25：線性迴歸 `策略月報酬 = alpha + beta × 基準月報酬 + residual` 的年化超額分解應為 `12×alpha + (beta−1)×12×平均基準月報酬`（含對應殘差）。現行 BetaExcess 換用基準 CAGR，混合幾何與算術口徑，貢獻無法相加守恆。它不改月報酬或原始 CAGR，錯在 HMM 歸因解釋；隨 HMM 暫緩即可。
