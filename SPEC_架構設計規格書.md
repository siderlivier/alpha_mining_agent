# 台股 Alpha 因子挖掘 Agent — 架構設計規格書

> **2026-09-18 挖礦判斷增補（本段優先於下文舊版定義）：**
> 門檻與排序使用未取整數值。direction 為原公式方向，neg 固定乘 -1 後評估與儲存，
> Stage 1 仍驗證原假設方向；不依 test 決定方向。允許純空頭腿因子入庫，
> 以 trading_use 區分 long_only／short_only／long_short；純空頭使用空頭持倉換手，
> 不加入現有多頭組合。腿的有效性仍是相對股票池均值的正超額，未包含借券可行性。
> EPS／營業利益成長改採精確去年同月差額除前值絕對值；前值零或缺月不計。
> 細節、測試與資料遷移狀態見 REVIEW_程式碼與數值正確性評估報告_2026-09-14.md 第 9 節。

**版本**：v1.0（2026-07-16）
**狀態**：設計定稿，待實作
**前置專案**：`tw_alpha_strategy`（FinMind 台股多因子策略，PIT 對齊 panel、IC/ICIR 評估、回測框架）
**主要參考**：FactorMiner 論文（Wang et al., 2026, arXiv:2602.14670）、FinLab〈AI 不睡覺的量化研究員〉

---

## 0. 文件目的

本文件是實作前的完整規格書。除了「做什麼」，每個設計都附上「**為什麼這樣設計**」（標註為 📌 理由），因為本系統的多數設計決策是為了防範特定的失敗模式（前瞻偏差、過擬合、垃圾經驗累積、token 超支），理解理由才能在日後修改時不破壞防線。

---

## 1. 背景與目標

### 1.1 要解決的問題

人工挖掘 alpha 因子的瓶頸：

1. **效率低**：人類研究員驗證一個因子假設需數天，一年產出有限。
2. **沒有系統性記憶**：失敗的嘗試散落在 notebook 裡，同一條死路會被重複探索（前置專案的 GP 實驗就是例子——大量算力花在生成與既有因子高度相關的變體上）。
3. **相關性紅海**：因子庫越大，新因子越可能只是舊因子的變體。FactorMiner 論文的數據：因子庫超過 80 個後，新因子通過率趨近於零；已知的禁忌方向（VWAP 偏差變體、標準化量價比、簡單 delta reversal）與現有因子相關性普遍 > 0.5。

FactorMiner 論文證明的核心結論：**有經驗記憶的 LLM Agent，有效因子產出效率是無記憶版本的 3 倍**（60% vs 20%），且拒絕率反而更高（55.2% vs 43.8%）——記憶讓它把算力留給值得嘗試的假設。

### 1.2 本系統的目標

建立一個可長期運行的因子挖掘 Agent，具備：

1. **自我進化迴圈**：Retrieve（讀記憶）→ Generate（生成因子假設）→ Evaluate（本地 Python 驗證）→ Distill（蒸餾經驗入庫）。
2. **假設先行的研究紀律**：每個因子必須先有經濟直覺假設與預測，再驗證，失敗必須歸因歸檔。
3. **嚴格的防過擬合與防前瞻偏差機制**（見第 9 章，這是本系統與「隨便讓 LLM 生因子」的根本差異）。
4. **金錢成本最小化**：以 Claude Pro 訂閱額度運行（Claude Code headless 模式），無 API 帳單；所有計算在本地 Python，LLM 每輪只消耗約 5K token。

### 1.3 非目標（明確排除）

- ❌ 不做即時交易、下單、實盤訊號。
- ❌ 不做日內/高頻因子（資料為日頻，評估為月頻截面）。
- ❌ 初版不做多 Agent 協作、不做向量資料庫（理由見 7.5）。
- ❌ 不追求單次挖掘的爆發力，追求「長期累積下因子庫的品質與多樣性」。

---

## 2. 設計哲學：四大原則

所有後續設計都從這四條推導而來。遇到設計衝突時，優先序 P3 > P2 > P4 > P1。

### P1：LLM 只做推理，不做計算

LLM 負責的只有兩件事：**提出因子假設**（Generate）與**歸納經驗**（Distill）。IC 計算、相關性檢查、回測、資料處理全部由本地 Python 完成，LLM 只讀寫緊湊的 JSON/Markdown 摘要。

📌 理由：token 是本系統唯一的邊際成本。一次完整回測的原始輸出可能是數萬 token，壓縮成 15 行診斷 JSON 後只剩約 300 token，資訊密度反而更高（見 6.4）。這也是使用者在前置專案已驗證的直覺：把分析驗證包成 Python 工具讓 agent 調用，能大幅減少 token 消耗。

### P2：假設先行（Pre-registration）

每個候選因子必須在**看到任何評估結果之前**，先寫下：假設（為什麼這個因子應該有效的經濟直覺）、預測（預期的量化表現）。評估後對照「預測 vs 實際」進行歸因。

📌 理由一：防止事後合理化。人類研究員最常見的自欺是「先看到結果好，再編一個故事」，LLM 同樣會犯——如果讓它先看結果再寫假設，它會為任何雜訊編出流暢的敘事，這種敘事進入記憶庫就是毒藥。
📌 理由二：「預測 vs 實際」的落差是校準訊號。模型只有知道「我以為會 ICIR 0.5、實際 0.2」，才能學到自己的直覺在哪類因子上系統性高估，這比單純記錄「這個因子失敗了」的資訊量大得多。

### P3：過擬合防線優先於績效

任何會讓測試集資訊洩漏進生成迴圈的設計，一律禁止，即使它能讓帳面績效更好看。具體機制見第 9 章。

📌 理由：無人值守的 agent 每週產生數百個候選因子，是極端的多重檢定環境。若測試集參與迭代，跑三個月後測試集績效將完全失去意義——這是整個系統最容易「靜默失效」的地方：系統看起來運作正常、績效數字漂亮，但全是過擬合幻覺。前置專案的 DFS/GP 實驗已親眼見過「訓練集極好、測試集直接爛掉」。

### P4：一切狀態存於檔案

記憶、因子庫、嘗試紀錄、預算計數全部是本地檔案（JSON / Markdown / Parquet）。系統沒有常駐進程、沒有資料庫服務、沒有外部依賴。

📌 理由一：可中斷續跑。Pro 額度用完、電腦關機、session 崩潰，下次啟動讀檔案就恢復狀態，「長時間挖掘」是靠檔案持久化而非長時間進程實現的。
📌 理由二：人類可審計。所有記憶都是可讀文字，你隨時可以打開 `learnings.md` 檢查 agent 學到的東西是否合理、手動修正錯誤經驗（等效於 Agno 的 human-in-the-loop，但零框架成本）。
📌 理由三：這正是 Agno 三層記憶在本場景的極簡替代——我們的記憶規模（數百筆嘗試、數十條經驗）用檔案綽綽有餘，引入框架只增加依賴與學習成本。

---

## 3. 系統架構總覽

```
┌────────────────────────────────────────────────────────────┐
│                    Orchestrator（本地 Python）                │
│  mining_loop.py：排程、預算控制、組 prompt、呼叫 claude -p、    │
│  解析回覆、觸發評估、寫入記憶                                   │
└──────┬─────────────────────────────────────────┬───────────┘
       │ 每輪 1–2 次 LLM 呼叫（~5K token）          │ 本地執行（免費）
┌──────▼──────────┐                    ┌─────────▼───────────┐
│  LLM（Claude，    │                    │  工具層（Python CLI） │
│  訂閱額度）        │                    │  eval_candidates.py  │
│  · Generate：讀   │                    │   四階段漏斗+診斷 JSON │
│    記憶→出15-20個  │                    │  audit.py 聚合審計    │
│    假設+公式       │                    │  consolidate 整理輔助 │
│  · Distill：讀診  │                    │  query_attempts.py    │
│    斷→歸因→寫經驗  │                    │   按標籤檢索個案       │
└──────┬──────────┘                    └─────────┬───────────┘
       │ 讀/寫（經 orchestrator）                  │ 讀/寫
┌──────▼─────────────────────────────────────────▼───────────┐
│                      記憶層（純檔案）                          │
│  attempts/*.json   逐筆嘗試紀錄（假設/預測/結果/歸因），只進不出  │
│  library.json      入庫因子中繼資料                            │
│  factor_values.parquet  入庫因子月頻值矩陣（Stage 2 去相關用）   │
│  learnings.md      蒸餾經驗（唯一注入 prompt 的記憶）           │
│  budget.json       週預算計數                                 │
├─────────────────────────────────────────────────────────────┤
│                      資料層（唯讀）                            │
│  panel.parquet     PIT 對齊日頻面板（沿用 tw_alpha_strategy）   │
│  monthly_base.parquet  月頻基礎欄位快照（一次性預計算）          │
│  regime_table.json 逐年市場狀態標註（一次性人工建立）            │
└─────────────────────────────────────────────────────────────┘
```

**一輪（round）的資料流**：

1. Orchestrator 讀 `learnings.md` + `library.json` 摘要 → 組 Generate prompt。
2. `claude -p` 回傳 15–20 個候選（每個含 hypothesis / prediction / formula / category）。
3. `eval_candidates.py` 跑四階段漏斗 → 每個候選產出診斷 JSON。
4. Orchestrator 把「候選 + 診斷」組成 Distill prompt → `claude -p` 回傳每個候選的 diagnosis / lesson。
5. Orchestrator 寫入 `attempts/`，存活者入 `library.json` + `factor_values.parquet`，新 lesson 追加至 `learnings.md`。
6. 更新 `budget.json`。每 N 輪觸發整理回合與聚合審計（見 7.4、9.3）。

📌 為什麼 Generate 與 Distill 拆成兩次 LLM 呼叫，而不是一個長對話：headless 的 `claude -p` 每次呼叫無狀態、可精準控制輸入輸出，避免長對話的上下文累積吃掉額度；且兩步之間夾著本地評估（可能跑數分鐘），無狀態呼叫天然適配。批次化（一次 15–20 個候選）則是為了攤薄每次呼叫的固定 prompt 成本（記憶摘要那 ~2K token 是固定開銷，多生成幾個候選就多攤薄一分）。

---

## 4. 資料層

### 4.1 資料來源

沿用 `tw_alpha_strategy` 已下載的 FinMind 資料（約 495MB，2012–2026，四大產業，含已下市公司）：

| 檔案 | 內容 | 用途 |
|---|---|---|
| `data/processed/panel.parquet` | PIT 對齊的日頻面板（還原價、報酬、月營收、三大報表科目，皆以發布日對齊） | 一切因子計算的唯一來源 |
| `data/raw/benchmarks.parquet` | 0050 / TAIEX / TPEx 報酬指數 | regime 標註、回測基準 |

📌 理由：(a) 資料已完成最難的工程——PIT 發布日對齊與還原價回推，重做毫無意義；(b) 完全離線，agent 運行時零 API 依賴、零網路故障點；(c) 含已下市公司，無倖存者偏差。未來擴充（更多產業、FinLab 補充籌碼面資料）只需重跑既有 fetch 腳本並擴充 `monthly_base` 欄位白名單，架構不變。

### 4.2 月頻基礎欄位快照（`monthly_base.parquet`）

一次性預計算：從 `panel.parquet` 取每月月底快照，含所有 DSL 可用的基礎欄位（見 5.4）與目標變數 `fwd_ret_1m`（未來一月報酬）。此檔是評估工具的直接輸入。

📌 理由：因子評估是月頻截面，每輪重複從 500MB 日頻 panel 聚合是浪費（每輪省數十秒 × 每週數百次候選評估）。預計算一次，評估工具啟動時間從分鐘級降到秒級。基礎欄位的滾動計算（vol_21、mom_60 等日頻窗口）在快照生成時完成，快照後 DSL 只在月頻上操作——這也簡化了 DSL 的前瞻偏差審查面。

### 4.3 三段式資料切分

| 區段 | 期間 | 誰能看到 | 用途 |
|---|---|---|---|
| **sub-train** | 2012-01 ~ 2017-12 | agent（逐因子、無限制） | 因子有效性的主要證據 |
| **validation** | 2018-01 ~ 2019-12 | agent（逐因子、無限制） | 樣本外衰減訊號，餵給歸因 |
| **test** | 2020-01 ~ 2026-06 | ⛔ agent 永不接觸逐因子數字 | 入庫時靜默記錄；僅經 `audit.py` 以聚合統計形式低頻回饋（見 9.2） |

📌 理由：前置專案的切分是 train(2012–2019)/test(2020–2026)，agent 若在整個 train 上迭代，就沒有任何樣本外訊號可以餵給歸因迴圈——但使用者的需求（讓模型學到「哪類設計會過擬合」）又必須有樣本外衰減資料。解法是把原 train 再切一刀：sub-train 與 validation 之間的衰減模式，與 train→test 的衰減模式本質相同（過擬合因子在任何樣本外都衰減），agent 可以從中自由學習，而真正的 test 保持凍結。代價是訓練證據少了兩年，可接受。
📌 validation 選 2018–2019 而非隨機抽樣：時間序列資料必須按時間切分（隨機抽樣會讓相鄰月份跨越 train/valid 邊界，資訊洩漏）。2018 含中美貿易戰下跌、2019 為反彈多頭，兩種市場狀態都有覆蓋。
📌 test 期含 2020 COVID 崩跌、2022 空頭、2024–25 多頭，是壓力測試完整性最好的區段，必須保護。

### 4.4 市場狀態標註表（`regime_table.json`）

一次性建立的靜態檔案，逐年記錄（2012–2026）：TAIEX 年報酬、年化波動、多空標籤（多頭/空頭/盤整/急跌反彈）、風格備註（如「2017 大型權值股獨強」「2018 Q4 貿易戰急跌」「2022 升息空頭、成長股重挫」）。

評估工具輸出逐年 IC 時自動 join 此表；此外對每個因子計算**上漲月 vs 下跌月的條件 IC**（以 TAIEX 月報酬正負分組）。

📌 理由：使用者明確要求——「強漲年因子表現好但下跌年差」這類資訊是歸因的關鍵素材。裸的逐年 IC 數列（如 `2015: 0.06, 2016: -0.01...`）LLM 無法解讀，join 上狀態標籤後才能產出「此因子在空頭年失效，屬順勢型」這種可入庫的 lesson。條件 IC 比年度粒度更細：一年內也有漲跌月，月粒度的條件分組統計功效更高。此表是靜態常識，人工寫一次即可，不花任何 token。

---

## 5. 因子表示法：受限 DSL

### 5.1 決策：受限 DSL，不用自由 Python 片段

LLM 生成的因子一律以受限表達式字串表示，例如：

```
ts_rank(rev_yoy_ma3, 12) * sign(mom_60)
cs_rank(gross_margin) - cs_rank(delay(gross_margin, 12))
if_else(roe > ts_med(roe, 24), cs_rank(rev_yoy_ma3), neg(cs_rank(vol_63)))
```

由本地解析器轉為 AST 後在白名單運算子上執行。**禁止 LLM 輸出任意 Python 程式碼。**

📌 理由一（最重要）：**前瞻偏差在語言層面被鎖死**。白名單裡所有時間序列運算子都只向後看（rolling/delay/shift 皆為正向落後），基礎欄位在 `monthly_base` 生成時已 PIT 對齊。自由 Python 片段則可能寫出 `shift(-1)`、用到未來聚合值、或在 groupby 中洩漏未來資訊——無人值守的 agent 不會像人一樣自我審查，一個帶前瞻的因子會以驚人的 ICIR 通過所有關卡並汙染整個因子庫。
📌 理由二：**可解析性**。AST 可以做結構去重（兩個公式代數等價的檢測）、複雜度計算（節點深度）、參數審查（魔術數字檢測），這些對自由程式碼幾乎不可行。
📌 理由三：**可重現**。公式字串即因子的完整定義，任何時候重算結果必然一致；Python 片段可能依賴環境狀態。
📌 代價與緩解：表達力受限。緩解方式是運算子白名單可控地擴充——當 agent 在 Distill 中反覆表示「想要某運算但 DSL 不支援」時，由人工審核後加入新運算子（人在迴路的擴充點，也是安全閥）。

### 5.2 運算子白名單

| 類別 | 運算子 | 語義 | 備註 |
|---|---|---|---|
| 時間序列 | `ts_mean(x,n)` `ts_std(x,n)` `ts_max(x,n)` `ts_min(x,n)` `ts_med(x,n)` | 過去 n 個月的統計量 | 含當月，僅向後看 |
| 時間序列 | `ts_rank(x,n)` | 當月值在過去 n 月中的分位 | FactorMiner 高頻使用 |
| 時間序列 | `delay(x,n)` `delta(x,n)` | n 月前的值 / 與 n 月前的差 | |
| 時間序列 | `ts_slope(x,n)` `ts_rsq(x,n)` `ts_resi(x,n)` | 過去 n 月線性回歸的斜率/R²/末端殘差 | 支援 FactorMiner 的「趨勢自適應」模式（R² 高用 slope 追勢、R² 低用 resi 反轉） |
| 時間序列 | `ts_corr(x,y,n)` | 兩序列過去 n 月相關 | |
| 截面 | `cs_rank(x)` | 當月**產業內**百分位排名 | 預設產業內，延續前置專案的產業中性框架 |
| 截面 | `cs_rank_all(x)` | 當月全池百分位排名 | 顯式跨產業比較時用 |
| 截面 | `cs_z(x)` | 當月產業內 z-score（winsor 後） | |
| 算術 | `+ - *` `sdiv(x,y)` | 四則；除法為安全除（分母近零回 NaN） | |
| 算術 | `log1p_abs(x)` `abs(x)` `sign(x)` `neg(x)` | 非線性變換 | log 取 `sign(x)*log1p(|x|)` 防負值 |
| 邏輯 | `if_else(cond,a,b)` `greater(x,y)` `less(x,y)` `and_(c1,c2)` `or_(c1,c2)` | 條件組合 | cond 只能由 greater/less/and_/or_ 構成 |

📌 選型理由：對照 FactorMiner 的 60+ 運算子集合，去掉日內/高頻專用（VWAP 類——論文自己也證明是紅海）與 GPU 特化運算，保留在月頻截面有意義的核心集。初版刻意偏小（~25 個）：運算子越多，搜尋空間越大、等價變體越多、Stage 2/3 的去重負擔越重。從小集合開始，按需擴充。

### 5.3 複雜度硬限制（機械防線）

解析器在執行前強制檢查，違規直接拒絕（不消耗評估算力，且在 attempt 紀錄中標記 `rejected_syntax`）：

| 規則 | 限制 | 📌 理由 |
|---|---|---|
| AST 運算子深度 | ≤ 3 | 過擬合與表達式複雜度高度相關。前置專案 GP 實驗失敗的主因即符號回歸生成超深表達式，train 極好 test 爛掉。深度 3 已能表達「排名(成長率的變化) × 條件」這種夠豐富的結構 |
| 使用欄位數 | ≤ 3 | 同上；且欄位越多，因子越難歸因（不知道哪個成分在起作用），lesson 品質下降 |
| 窗口參數白名單 | n ∈ {3, 6, 12, 24}（月） | 禁止 17、23 這類魔術數字——它們幾乎必然是對歷史雜訊的擬合。常規窗口對應真實的經濟週期（季、半年、年、兩年） |
| 條件嵌套 | if_else 不得嵌套 if_else | 嵌套條件是最典型的過擬合結構（等於手工決策樹） |
| 常數 | 比較閾值僅允許 0 與欄位的 ts_med | 「roe > 13.7」這種精調閾值是雜訊擬合；「roe > 自身兩年中位數」是結構性陳述 |

📌 總理由：這些防線**零 token 成本**。與其讓因子跑完評估、再由 LLM 歸因出「複雜度太高導致過擬合」（每次花數百 token 且不保證歸因正確），不如在語法層直接禁止。機械防線擋掉的是「已知必然壞」的形態，LLM 的歸因算力留給「真正有資訊量的失敗」。

### 5.4 基礎欄位白名單（`fields.yaml`）

初版從 `mine_dfs.py` 的欄位生成邏輯移植，分四組（實作時以該檔案的 coalesce 邏輯為準）：

- **價量技術**：`mom_20 / mom_60 / mom_120 / mom_240`（多期動能）、`vol_21 / vol_63 / vol_126`（波動率）、`amt_21`（月均成交額）、`turn_21`（週轉）、`px_hi252`（距 52 週高點）
- **獲利品質**：`roe`、`gross_margin`、`op_margin`、`net_margin`、`accruals`（應計項）、`ocf_ratio`（營業現金流/淨利）
- **成長**：`rev_yoy`、`rev_yoy_ma3`、`rev_mom`、`eps_yoy`、`op_income_yoy`
- **估值**：`ep`（盈餘殖利率 1/PE）、`bp`（淨值比倒數）、`sp`（營收市值比）

每個欄位在 yaml 中附一行語義描述（會注入 Generate prompt，讓 LLM 理解欄位含義）與資料起始年份。

📌 理由：欄位白名單與運算子白名單共同構成封閉的搜尋空間——任何公式的合法性可被機器完全驗證。附語義描述是因為 LLM 的假設品質取決於它對欄位經濟含義的理解；欄位名 `accruals` 配上「應計項高代表盈餘品質差，經典反向因子（Sloan 1996）」的描述，生成的假設會好一個檔次。

### 5.5 解析與執行

- 解析：以 Python `ast` 模組解析表達式 → 校驗節點全在白名單 → 轉為可執行計算圖。**絕不使用 `eval()`**。
- 執行：在 `monthly_base` 的 (stock_id × month) 面板上向量化計算，輸出因子值矩陣。
- 等價檢測：AST 正規化（交換律排序、雙重否定消除）後哈希，公式級去重在評估前完成。

📌 理由：`ast` 白名單校驗是防注入的標準做法（LLM 輸出不可信任，哪怕是自己家的 LLM）；等價檢測讓「`a*b` 與 `b*a`」這種平凡重複不佔用 Stage 1–3 的算力。

---

## 6. 評估管線：四階段漏斗（`eval_candidates.py`）

### 6.1 總體設計

輸入：一批候選（JSON，每個含 id / formula / hypothesis / prediction / category）。
輸出：每個候選一份診斷 JSON（見 6.4）。
流程：語法檢查 → Stage 1 → Stage 2 → Stage 3 → Stage 4，任一階段淘汰即停止（但仍輸出該階段的診斷），全過者標記 `passed`。

📌 漏斗式設計的理由（承 FactorMiner）：計算成本遞增（Stage 1 毫秒級、Stage 4 秒級），把便宜的檢查放前面，讓大多數壞候選在花大算力之前死掉。FactorMiner 的通過率參考：Stage 1 ~40%、Stage 2 ~25%、Stage 3 ~15%、Stage 4 ~8%——即百裡挑八，這是正常且健康的拒絕率。

### 6.2 各階段定義與門檻

所有門檻集中於 `config.yaml`，以下為初版預設值。**所有階段只使用 sub-train + validation 資料，絕不觸碰 test。**

| 階段 | 檢查 | 淘汰條件（預設） | 📌 理由 |
|---|---|---|---|
| Stage 0 | 語法/複雜度 | 違反 5.3 任一規則 | 零成本擋掉必然壞的形態 |
| Stage 1 | 快速 IC 篩選 | sub-train 平均月 rank-IC 絕對值 < 0.02，**或 IC 符號與 hypothesis 宣稱方向相反** | 0.02 是月頻截面因子的最低可用線（年化資訊比約 0.3 的量級）。方向檢查是 pre-registration 的執行機制：方向反了代表假設本身錯誤，即使 |IC| 大也不能當作「意外發現」直接收下——可以讓 LLM 在歸因時提出反向假設、下輪重新提交，強迫假設與證據一致 |
| Stage 2 | 因子庫去相關 | 與 `factor_values.parquet` 中任一入庫因子的月頻截面值 Spearman \|ρ\| > 0.5（取逐月截面相關的時間平均） | 這是對抗「相關性紅海」的核心關卡。FactorMiner 證實多數新候選是既有因子變體；ρ 0.5 是論文採用的門檻。**淘汰時必須點名相關性最高的兇手因子與 ρ 值**——這是歸因品質的關鍵素材（「你這個『新』因子其實就是 F-012」） |
| Stage 3 | 批次內去重 | 同批候選兩兩 \|ρ\| > 0.7 時，只保留 sub-train ICIR 較高者 | LLM 一次生成的 15–20 個候選常是同一想法的變體（採樣的本性）。批內門檻放寬到 0.7 是因為同批變體中最好的那個值得進 Stage 4，過嚴會誤殺 |
| Stage 4 | 完整驗證 | 任一不達標：sub-train ICIR < 0.45；validation ICIR < 0.30；validation 對 sub-train 的 ICIR 衰減 > 50%；多頭腿 IC ≤ 0（見 6.3）；截面覆蓋率 < 60%；月均單邊換手 > 40% | 門檻取自前置專案 DFS 海選的經驗分布（存活因子 train ICIR 多在 0.5 上下）。衰減上限 50% 是防過擬合的主判準。台股放空困難，只有空頭腿有效的因子實務不可交易，必須擋 |

入庫者額外靜默計算 test 期指標寫入 `library.json`（欄位 `test_metrics_sealed`），**不出現在回傳給 LLM 的任何輸出中**。

### 6.3 診斷指標定義

每個候選（無論死在哪一階段）都計算截至該階段可得的診斷：

1. **逐年 IC（join regime 標註）**：`{"2013": {"ic": 0.041, "regime": "多頭"}, ...}`——看因子是全期穩定還是狀態依賴。
2. **條件 IC**：TAIEX 上漲月 vs 下跌月分組的平均 IC——順勢型/避險型因子的判別依據。
3. **多空腿分解**：因子前 20% 組合超額報酬（多頭腿）與後 20%（空頭腿）分別統計。📌 台股融券成本高、多數帳戶純做多，空頭腿撐起來的 ICIR 是紙上富貴。
4. **產業 IC 分解**：半導體/電子/生技/金融各自的 ICIR——「只在生技有效」也是有價值的發現（可入庫為產業專屬因子），但必須被標示。
5. **覆蓋率**：因子值非缺失的股票月佔比——太稀疏的訊號組合起來波動大、易被個股事件綁架。
6. **換手率**：因子月度自相關推算的持倉翻動——高換手因子扣成本後常歸零（前置專案已有成本框架，Stage 4 直接引用）。
7. **Stage 2 兇手**：`{"factor": "F-012", "rho": 0.63, "factor_desc": "營收加速度"}`。
8. **預測對照**：prediction 中宣稱的數字 vs 實際。

📌 總理由：**歸因品質 = 診斷資料品質**。若評估只回「ICIR=0.21，淘汰」，LLM 的歸因只能是幻覺，寫進記憶的是垃圾——垃圾經驗比沒有經驗更糟，因為它會自信地誤導後續搜尋。上述八項每一項都對應一類具體的失敗模式，讓歸因是「有據推論」而非「編故事」。

### 6.4 診斷輸出格式（回傳 LLM 的部分）

每個候選壓縮為約 15 行 JSON（~300 token）：

```json
{
  "id": "A-0137",
  "verdict": "rejected_stage2",
  "sub_train": {"icir": 0.51, "mean_ic": 0.038},
  "validation": {"icir": 0.19, "decay_pct": 63},
  "ic_by_year": {"2013": [0.05, "多頭"], "2015": [-0.01, "盤整"], "2018": [0.07, "空頭急跌"]},
  "cond_ic": {"up_months": 0.012, "down_months": 0.055},
  "legs": {"long_excess_ann": 0.021, "short_excess_ann": 0.089},
  "industry_icir": {"半導體": 0.6, "電子": 0.4, "生技": 0.1, "金融": -0.1},
  "coverage": 0.83, "turnover_m": 0.31,
  "stage2_culprit": {"factor": "F-012", "rho": 0.63},
  "prediction_check": {"claimed": "ICIR>0.5, corr<0.4", "actual": "ICIR 0.51 ✓, corr 0.63 ✗"}
}
```

📌 理由：固定 schema 讓 Distill prompt 可以用表格化方式一次處理整批候選；欄位名短、數字取 2–3 位有效數字，都是為了 token 密度。`ic_by_year` 只保留有代表性的年份（極值年 + regime 轉折年）而非全列，由工具端裁剪。

---

## 7. 記憶層

### 7.1 兩層結構總覽

| 層 | 檔案 | 性質 | 誰讀 |
|---|---|---|---|
| 原始層 | `attempts/*.json` | 逐筆完整紀錄，**只進不出、永不刪改** | 工具檢索（`query_attempts.py`）、整理回合抽樣 |
| 蒸餾層 | `learnings.md` | 人可讀的通則，持續增修 | **唯一注入每輪 prompt 的記憶** |

📌 理由：原始層是 ground truth，保證任何 lesson 都可溯源驗證（每條 lesson 必須引用 attempt 編號）；蒸餾層是工作記憶，控制 prompt 體積。兩層分離後，「記憶膨脹」只影響原始層（磁碟便宜），prompt 成本恆定。這對應 FactorMiner 的 experience memory 與 Agno 的 Learned Memory，但以檔案實現。

### 7.2 嘗試紀錄 Schema（`attempts/A-XXXX.json`）

```json
{
  "id": "A-0137",
  "ts": "2026-07-20T02:31:00",
  "round": 42,
  "category": "revenue_momentum",
  "hypothesis": "營收動能強但股價尚未反應的股票存在延遲定價，因為散戶主導的中小型股資訊擴散慢",
  "prediction": "sub-train ICIR > 0.5；與庫內 rev 系因子 |ρ| < 0.4；下跌月 IC 應高於上漲月",
  "formula": "cs_rank(rev_yoy_ma3) - cs_rank(mom_60)",
  "complexity": {"depth": 2, "fields": 2},
  "result": { "...": "6.4 的完整診斷 JSON" },
  "diagnosis": "失敗主因非假設錯誤，而是與 F-012(營收加速度)本質重疊（ρ=0.63）：『營收強而價格弱』的差值在截面上退化為營收強度本身",
  "lesson": "rev 系欄位與價格動能做 rank 差值，會與純 rev 因子高相關；若要捕捉『延遲定價』需改用條件式（rev 強且 mom 位於中段）而非差值",
  "lesson_confidence": "single_case",
  "verdict": "rejected_stage2"
}
```

欄位設計要點與理由：

- `hypothesis` 必須包含**機制**（為什麼市場會犯這個錯）而非現象描述。📌 「營收成長的股票會漲」是套套邏輯；「散戶主導導致資訊擴散慢」是可被打臉的機制陳述，打臉時的 lesson 才有泛化力。
- `prediction` 必須含可對照的量化門檻。📌 見 P2——校準訊號的來源。
- `lesson_confidence`：`single_case`（單一個案）/ `corroborated`（多案支持）/ `promoted`（已升級進 learnings.md 通則）。📌 防止單一個案被當成定律——這是 LLM 歸納的常見毛病，一次失敗就宣布整個方向死刑會過度收縮搜尋空間。
- 只進不出。📌 修改歷史紀錄 = 銷毀證據；被推翻的 lesson 在 learnings.md 標記失效，但原始 attempt 不動。

### 7.3 蒸餾經驗檔（`learnings.md`）

結構（按 category 分節）：

```markdown
# 因子挖掘經驗庫（agent 維護，人類可隨時修訂）

## 全域規則
- [G-01|promoted] 動能確認條件普遍提升 rev 系因子表現，機制：過濾市場尚未認可的
  營收成長（可能藏有壞消息）。證據：A-0003, A-0019, A-0044。
- [G-02|promoted] 差值型組合（cs_rank(a)-cs_rank(b)）傾向退化為其中強勢成分，
  與單因子高相關。證據：A-0137, A-0152, A-0170。

## 禁忌方向（Stage 2 紅海區 / 反覆失敗區）
- [F-01] 投信買賣超類條件：滯後指標，加入後 CAGR 顯著劣化。證據：前置專案結論（seed）。
- [F-02] 低波動因子在台股月頻近乎無效。證據：前置專案 + FinLab 文章（seed）。

## revenue_momentum
- [R-01|corroborated] ...

## 待驗證假設佇列（agent 自己排的研究議程）
- 條件式延遲定價（rev 強 & mom 中段），源自 A-0137 歸因。優先級：高。
```

要點與理由：

- 每條有編號、置信等級、證據引用。📌 可溯源、可推翻、可被整理回合升降級。
- **種子經驗（v1.1 修訂）**：初始化為**空骨架，不預載任何理論**（含 FinLab 文章與前置專案的結論）。📌 使用者決策：預設理論塞進記憶會汙染 agent 自己的歸納——所有經驗必須來自它親手跑過的 attempt 與證據編號，寧可多花幾輪重新發現，也不要無法溯源的先驗。（且實測發現先驗未必可靠：如「低波動無效」在 IC 口徑下不成立，見 M1 驗收紀錄。）
- **待驗證佇列**：Distill 產生的新假設進佇列，Generate 優先消化。📌 讓跨 session 的研究有連續性——這正是「探險家每天醒來不忘記昨天走過的路」的具體實現。
- 全檔控制在 ~4K token 內（超出即觸發整理回合強制壓縮）。📌 它是每輪 prompt 的固定成本，必須有上限。

### 7.4 整理回合（Consolidation，每 30 輪或 learnings.md 超限時觸發）

專用的 LLM 回合（不生成新因子），輸入 learnings.md 全文 + 近 30 輪 attempts 的摘要表 + `query_attempts.py` 按需調的個案，任務：

1. **升級**：≥3 個獨立個案支持的 single_case lesson 合併為通則（promoted）。
2. **推翻**：與新證據矛盾的舊規則標記 `[失效|見 A-XXXX]`（不刪除，保留審計軌跡）。
3. **壓縮**：合併重複、刪除已無資訊量的條目，控制總量。
4. **議程更新**：重排待驗證佇列的優先級。

📌 理由：這是使用者提出的核心疑慮（「只把成功/失敗因子塞 prompt，模型會不會不知道自己錯在哪」）的制度性解答。單輪 Distill 只能做個案歸因，泛化必須靠定期回顧——與人類研究團隊的 review meeting 同構。頻率選 30 輪是平衡：太頻繁浪費 token（每次 ~8K，是普通輪的 1.6 倍），太稀疏則垃圾經驗存活太久。

### 7.5 為什麼初版不用向量資料庫

📌 (a) 規模不需要：learnings.md 全文注入僅 ~4K token，數百筆 attempts 用 category 標籤 + `query_attempts.py`（本地 grep/filter，零 token）即可精準檢索；(b) 向量檢索的失敗模式更隱蔽——相似度召回錯經驗比沒召回更糟且難察覺；(c) 省掉 embedding 模型與 Chroma 依賴，符合 P4。**觸發重估的條件**：attempts > 2,000 筆、或 category 數 > 30、或整理回合反覆超限，屆時再引入向量檢索作為 query_attempts 的補充。

---

## 8. 挖礦迴圈與編排（`mining_loop.py`）

### 8.1 執行核心決策：Claude Code headless（`claude -p`）

| 選項 | 邊際成本 | 排除理由 |
|---|---|---|
| **`claude -p` + Pro 訂閱** ✅ | 0（額度內） | — |
| Anthropic API 自建迴圈 | 按 token 計費 | 違反成本最小化；本系統每輪間有分鐘級本地計算，API 的 7×24 能力用不上 |
| Agno 框架 | API 計費 + 框架學習 | 三層記憶已由檔案替代（P4）；多工具編排已由 orchestrator 替代 |

📌 注意事項：Claude Code 與 Claude app **共用 Pro 訂閱額度**，預算控制（8.4）因此是硬需求而非優化項。

### 8.2 一輪的完整流程（偽代碼）

```python
def run_round(round_id):
    check_budget()                                    # 超限即優雅退出
    mem   = load_learnings() + library_summary()      # ~2.5K token
    gen   = claude_p(GENERATE_PROMPT.format(memory=mem, n=15))   # → 候選 JSON
    cands = parse_and_validate(gen)                   # 語法層過濾
    diags = run_eval(cands)                           # 本地，四階段漏斗
    dist  = claude_p(DISTILL_PROMPT.format(pairs=pack(cands, diags)))
    write_attempts(cands, diags, dist)                # 逐筆歸檔
    admit_survivors(diags)                            # 入庫 + 靜默算 test 指標
    append_lessons(dist)                              # 追加 learnings.md
    update_budget(tokens_used)
    if round_id % 30 == 0: run_consolidation(); run_audit()
```

### 8.3 Prompt 模板要點

**Generate prompt** 的組成（~3K token）：角色設定（台股量化研究員）＋ DSL 規格卡（運算子/欄位/複雜度規則的壓縮版）＋ learnings.md ＋ 因子庫一覽（僅 id/公式/category/一句話描述——足以避開紅海，不含詳細績效）＋ 指令（先寫 hypothesis 與 prediction、再寫 formula；優先消化待驗證佇列；顯式避開禁忌方向；15 個候選中至少 3 個嘗試全新 category）。

📌 「至少 3 個新 category」是探索-利用平衡的顯式機制：純利用會讓 agent 在已知有效區域打轉，加劇批內相關性；FactorMiner 的多樣性是靠去相關硬約束逼出來的，我們在生成端就注入探索配額，減少浪費。
📌 因子庫一覽不含詳細績效：LLM 知道「庫裡有什麼」即可避開重複；給它逐因子績效排名反而誘導它圍著最強因子做變體（趨附行為），正是 Stage 2 要擋的東西。

**Distill prompt** 的組成（~2K token）：候選+診斷對照表 ＋ 指令（對每個候選：對照 prediction 與實際、寫 diagnosis 與 lesson、標 lesson_confidence；區分「假設錯誤」「假設對但公式表達不當」「與庫內重複」三類失因——它們的 lesson 完全不同）。

📌 三類失因的區分是歸因品質的核心：假設錯誤 → 方向入禁忌清單；表達不當 → 假設進待驗證佇列換公式重試；庫內重複 → 記錄紅海邊界。混為一談會把還有救的假設錯殺。

### 8.4 預算控制（`budget.json`）

```json
{"week_of": "2026-07-13", "rounds_used": 12, "weekly_round_budget": 15,
 "est_tokens_used": 61000, "consolidation_due": 18}
```

- 每輪開始前檢查，超限優雅退出（狀態已全部落盤，下週續跑）。
- 預設 `weekly_round_budget: 15`（Pro 額度下約 75–90K token/週，含整理回合），為日常使用保留大部分額度。📌 使用者明確要求每週保留額度給自己日常使用；此值在 config 一行可調，未來升級 Max 或發現額度餘裕再放大。
- 排程：Windows 工作排程器在深夜時段觸發（如 02:00 跑 5 輪，每週 3 晚）。📌 深夜跑避開與日常使用的額度競爭時段。

### 8.5 錯誤處理

- LLM 輸出無法解析為 JSON → 重試一次（附錯誤訊息），再失敗則跳過本輪並記 log。📌 重試也花 token，上限一次。
- 單一候選公式執行異常 → 該候選標 `rejected_error`，不影響同批其他候選。
- 評估工具崩潰 → 本輪 attempts 不落盤（保持原子性），log 待人工檢查。

---

## 9. 防過擬合紀律（彙總）

本章集中陳述所有防線。**任何未來修改若觸及本章，必須人工重新審視整體洩漏面。**

### 9.1 資訊隔離牆

| 資訊 | 能否進入生成迴圈 | 形式 |
|---|---|---|
| sub-train 逐因子指標 | ✅ 無限制 | 診斷 JSON |
| validation 逐因子指標與衰減 | ✅ 無限制 | 診斷 JSON |
| test 逐因子指標 | ⛔ 永不 | 只寫入 `library.json.test_metrics_sealed`，供人類查看 |
| test 聚合統計 | ⚠️ 低頻、僅分類聚合 | `audit.py`，見 9.2 |

📌 為什麼 validation 可以無限制回饋：它會逐漸被「消耗」（agent 隱性地對 2018–2019 過擬合），這是已知並接受的代價——validation 的作用是提供樣本外衰減的**教學訊號**，最終審判由凍結的 test 承擔。若連 validation 都吝嗇回饋，agent 就學不到過擬合的模式識別，違背本系統目的。

### 9.2 聚合審計（`audit.py`，每 30 輪隨整理回合執行）

對全體入庫因子計算 train→test 衰減，但輸出**只按維度聚合**：category、AST 深度、運算子類型、窗口參數、欄位組。範例輸出（回饋給整理回合）：

```
深度=3 的因子平均 test ICIR 衰減 61%（n=14）；深度≤2 衰減 24%（n=31）
ts_corr 系因子衰減 78%（n=5）→ 建議標記高風險運算子
rev 系 category 衰減 18%，最穩健
```

📌 理由：使用者的需求（「模型要知道哪類設計方式導致過擬合」）與 test 保護的矛盾，用**資訊量限制**化解——聚合統計每次只洩漏幾十 bit 的類別級資訊，且頻率低（每 30 輪一次），agent 能學到設計原則（「深表達式壞」「ts_corr 危險」），但無法藉此挑選任何特定因子。這與差分隱私的思路同構：控制的不是「碰不碰資料」，是「洩漏多少 bit」。
📌 附帶紀律：audit 輸出進 learnings.md 的全域規則區，標註 `[audit]` 來源。

### 9.3 全部防線清單

1. DSL 白名單運算子僅向後看（前瞻偏差語言級封鎖）—— 5.1
2. 複雜度硬限制：深度/欄位數/窗口白名單/禁嵌套/禁魔術閾值 —— 5.3
3. 三段式切分，test 凍結 —— 4.3
4. Stage 1 假設方向一致性檢查（pre-registration 執行機制）—— 6.2
5. Stage 4 validation 衰減上限 50% —— 6.2
6. 逐因子 test 指標對 LLM 密封 —— 9.1
7. test 只以低頻聚合統計回饋 —— 9.2
8. 因子庫一覽不含績效排名（防趨附）—— 8.3
9. `ml_validation.py` 的 DSR/PBO 作為**人工觸發**的季度總體檢，不進 agent 迴圈 —— 📌 多重檢定校正需要全體嘗試次數作輸入（attempts 檔案數即是），Deflated Sharpe 會告訴你「在嘗試了 N 次之後，這個 Sharpe 還剩多少可信度」，這是人類決定是否實盤的最後一道關，本質上是治理問題而非工程問題，故留在人工側。

---

## 10. 目錄結構

```
alpha_mining_agent/
├── SPEC_架構設計規格書.md      # 本文件
├── config.yaml                 # 所有門檻/預算/路徑（單一事實來源）
├── fields.yaml                 # 基礎欄位白名單 + 語義描述
├── data/
│   ├── monthly_base.parquet    # 月頻基礎欄位快照（build_base.py 產出）
│   └── regime_table.json       # 市場狀態標註（人工一次性建立）
├── memory/
│   ├── attempts/               # A-XXXX.json 逐筆嘗試（只進不出）
│   ├── library.json            # 入庫因子中繼資料（含密封 test 指標）
│   ├── factor_values.parquet   # 入庫因子月頻值矩陣
│   ├── learnings.md            # 蒸餾經驗（prompt 注入源）
│   └── budget.json             # 週預算計數
├── prompts/
│   ├── generate.md             # Generate 模板
│   ├── distill.md              # Distill 模板
│   └── consolidate.md          # 整理回合模板
├── src/
│   ├── dsl.py                  # 解析器 + 白名單校驗 + 執行引擎 + 等價哈希
│   ├── build_base.py           # panel.parquet → monthly_base.parquet
│   ├── eval_candidates.py      # 四階段漏斗 + 診斷 JSON
│   ├── mining_loop.py          # orchestrator（claude -p 編排）
│   ├── audit.py                # 聚合審計
│   ├── query_attempts.py       # 按標籤檢索個案
│   └── seed_memory.py          # 初始化種子經驗
└── tests/
    ├── test_dsl_no_lookahead.py  # 前瞻偏差單元測試（最重要的測試）
    └── test_funnel.py
```

📌 `test_dsl_no_lookahead.py` 的設計：對每個時間序列運算子，構造「在 t 月之後注入異常值」的資料，斷言 t 月的因子值不變。這是整個系統可信度的基石測試，M0 必須先過。

---

## 11. 開發里程碑

| 階段 | 內容 | 驗收標準 |
|---|---|---|
| **M0** | DSL：解析器、白名單、執行引擎、複雜度檢查、等價哈希 | 前瞻偏差測試全過；手工 10 個公式計算結果與 pandas 手算一致 |
| **M1** | 資料與評估：`build_base.py`、`regime_table.json`、`eval_candidates.py` 四階段 + 診斷 JSON | 用前置專案 DFS 的已知因子跑通，IC 數字與 `factor_eval.py` 對得上 |
| **M2** | 記憶層：attempts/library/learnings 讀寫、`seed_memory.py`（灌入前置專案與 FinLab 既有結論）、`query_attempts.py` | 種子經驗檔人工審核通過 |
| **M3** | 迴圈：prompt 模板、`mining_loop.py`、**人工監督跑 5 輪** | 5 輪產出的 hypothesis/diagnosis/lesson 經人工審核合格；每輪 token 實測 ≤ 7K |
| **M4** | 自動化：預算控制、Windows 排程、`audit.py`、整理回合 | 無人值守連跑一週無事故；週 token 用量在預算內 |

📌 M3 的人工監督是關鍵閘門：在放手自動化之前，必須先確認 lesson 品質——如果歸因是垃圾，自動化只會加速累積垃圾。驗收標準是「你讀了 5 輪的 lesson，願意讓它繼續寫下去」。

---

## 12. 風險與未決事項

| 風險 | 影響 | 緩解 |
|---|---|---|
| `claude -p` 輸出格式不穩定 | 解析失敗浪費額度 | prompt 中給 JSON schema + few-shot；重試上限 1 次；M3 實測調整 |
| validation（2018–19）逐漸被消耗 | 衰減訊號鈍化 | 中期方案：滾動 validation（如改用 walk-forward 多窗口）；列入 M4 後評估 |
| 經驗庫被錯誤 lesson 汙染 | 搜尋空間錯誤收縮 | lesson_confidence 分級 + 整理回合推翻機制 + 人工隨時可改 learnings.md |
| Pro 額度變動 / weekly limit 撞牆 | 排程中斷 | 預算保守設定（15 輪/週）；一切落盤可續跑 |
| 因子庫成長後 Stage 2 通過率趨零（紅海） | 產出停滯 | 屆時擴充欄位白名單（新資料源：籌碼面、FinLab 補充）比擴運算子更有效——新資訊維度才是新 alpha 的來源 |
| DSL 表達力不足 | 錯過某類因子 | 運算子擴充走人工審核流程（5.1 的安全閥） |

**未決事項**（實作時決定）：
1. Stage 4 是否加入「因子加權組合後的邊際貢獻」檢驗（FactorMiner 未做，學術上更嚴謹但計算重）——建議 M4 後評估。
2. 產業內 vs 全池評估的權重——初版沿用前置專案的產業內框架，未來若擴產業需重新校準 MIN_STOCKS。
3. 週窗口參數（月頻下是否需要 1 個月以下粒度）——初版不做。

---

## 附錄 A：與 FactorMiner 的對照

| FactorMiner（論文） | 本系統 | 差異理由 |
|---|---|---|
| A 股高頻分鐘資料 | 台股日頻→月頻截面 | 資料可得性 + 前置專案框架 |
| 60+ GPU 運算子 | ~25 個月頻運算子 | 搜尋空間控制，月頻不需高頻算子 |
| 四階段漏斗（IC→ρ<0.5→批內去重→回測） | 相同結構 + 假設方向檢查 + validation 衰減上限 | 補上 pre-registration 與顯式防過擬合 |
| 經驗記憶（成功模式/禁忌方向） | 兩層檔案記憶 + 置信分級 + 整理回合 + 聚合審計 | 補上「垃圾經驗治理」——論文未處理歸因錯誤的累積問題 |
| 專用 agent 框架 | claude -p + 本地 orchestrator | 成本最小化（訂閱額度、零 API） |
| 未強調假設 | 假設先行、預測對照 | 使用者的核心設計要求，提升 lesson 品質 |

## 附錄 B：本規格書確立過程中的關鍵討論結論

1. DSL vs Python 片段 → DSL（前瞻偏差語言級封鎖優先）。
2. 假設先行 + 失敗歸因歸檔 → 採納並強化為 pre-registration（含 prediction 對照）。
3. 「模型知不知道自己錯在哪」→ 解法在診斷粒度（八項診斷指標）而非記憶架構。
4. 向量庫 → 初版不用，設定了明確的重估觸發條件。
5. 逐年 IC 需配合市場狀態解釋 → regime_table + 條件 IC。
6. test 回饋矛盾 → 雙層解法：validation 自由回饋（第一層）+ test 聚合審計（第二層）。
7. Pro 額度 → 15 輪/週、深夜排程、預算檔硬控制。

## 附錄 C：v1.1 實作期修訂（M0–M2）

1. **種子經驗改為空骨架**（使用者決策）：不預載 FinLab/前置專案結論，理由見 7.3 修訂。
2. **因子庫中繼資料擴充**（使用者要求）：每個入庫因子必含 `name_zh`（中文名稱）、`desc_zh`（一句話解釋）、`aspect`（技術面/基本面/籌碼面/混合，由公式欄位自動推導，`memory.ASPECT_MAP` 維護群組對映）。
3. **fwd_ret_1m 逐月 winsorize**（1%/99%，對齊前置專案）：因子欄位不 winsorize——評估全走 rank 不受影響，保留原值供人工檢視；目標變數必須 winsorize，否則減資假極端值扭曲多空腿統計。
4. **M1 驗收紀錄**：IC 實作與前置專案 `factor_eval.py` 六因子交叉驗證完全一致；低波動因子在 IC 口徑通過漏斗（反向 vol ICIR ~0.4），與「前 15 檔集中回測 CAGR 無效」口徑不同——留待 agent 自行發現。
5. **新增機械防線**：`test_funnel.py` 對評估輸出 JSON 做「不得出現 2020–2026 年份字串」的洩漏檢查。

## 附錄 D：v1.2 實作期修訂（M3–M4）

1. **運算子提案流程**（使用者提議，制度化 5.1 的安全閥）：Distill 增設 `dsl_limitation` 選填欄位收集表達力受限訊號（`memory/dsl_limitations.jsonl`）；整理回合中同類卡點 ≥2 次才允許正式提案；提案過三道機械驗證——名稱格式、純函數簽名白名單（只准 `(x)`/`(x,n)`/`(x,y,n)`，禁止任何新常數參數）、證據引用 ≥2 個真實 attempt——通過後仍為 `pending_review`，人工批准前絕不實作；實作時受 no-lookahead 守門測試強制覆蓋。
2. **整理回合安全機制**：learnings.md 重寫前自動備份至 `memory/learnings_history/`；新文本缺必要小節則拒用保留原文。
3. **人類報表**（`report.py`）：因子庫全表（含密封 test 指標）+ 漏斗存活率 + category/失因統計，標注「勿餵 agent」。
4. **Windows 相容**：`claude` CLI 以 `shutil.which` 解析，`.cmd` 包裝經 `cmd /c` 執行。
5. **運維教訓**：真實挖礦開始後，`memory/` 即為生產資料——任何自動化重置腳本一律禁止（曾誤刪一輪真實資料）。

## 附錄 E：v1.3 實作期修訂

> 資料擴張與版本管理的新提案見附錄 F；附錄 E 所稱「新增產業零改動」只適用候選評估，不代表既有因子 scope 已能安全自動擴張。

1. **籌碼面資料接入**：panel v2 新增 11 個籌碼/估值欄位，衍生 8 個白名單欄位（chips 群組，面向自動標「籌碼面」）：`frgn_net_21 / trust_net_21 / frgn_ratio / frgn_ratio_d63 / margin_d21 / short_margin_ratio / lend_vol_21 / div_yield`。正規化一律除以流通股數或成交量（rank 評估下純量無關，但跨股可比性必要）。
2. **build_base 分三階段**（`--stage prices/chips/final`）：降低單次記憶體與執行時間，各階段可獨立重跑。
3. **產業專屬入庫通道（Stage 4b）**（使用者要求，為未來擴產業鋪路）：整體 Stage 4 未達標時，逐產業檢查專屬門檻——train ICIR ≥ 0.60（比全池 0.45 嚴，理由：單產業樣本少 + 跨產業多重比較）、validation ICIR ≥ 0.35、衰減 ≤50%、產業內多頭腿 > 0、產業內覆蓋 ≥ 0.6、最少月數限制。通過 → verdict `passed_industry`，以 `industry_scope` 標籤入庫；其 Stage 2 去相關與密封 test 指標皆只在該產業範圍內計算；`library_summary` 與報表顯示「XX限定」。新增產業時通道自動生效，零改動。


## 附錄 F：資料擴張、自動產業範圍與參考因子換版設計（2026-09-18 提案）

**狀態：設計方案，尚未實作。** 本節中的命令、欄位、工作流程及 Web UI 均為擬議介面，不是目前已有的功能。這次已執行的資料重建另見評估報告第 10 節。

### F.1 使用者的一次執行流程

目標是資料擴張後執行一次工作，即可取得完整檢查結果；預先啟用自動套用時，合格項目自動更新，異常項目保留舊版本並說明原因。一般執行不需要逐檔手動挑產業。

擬議流程：

```text
更新上游資料與股票產業分類
  → 建立不可變資料版本及差異清單
  → 資料品質檢查
  → 暫存重建月頻資料及受影響因子
  → 依固定期間／規則評估新增產業
  → 建立變更計畫與檢查報告
  → check-only：結束，不啟用
     auto-apply：檢查全部通過後，備份並提交同一版本
  → 輸出成功／部分待處理／失敗原因與可回復版本
```

擬議命令（尚不存在）：

```powershell
python src/crosssec_oos.py --refresh --check-only
python src/crosssec_oos.py --refresh --auto-apply
python src/seed_reference.py --build-release --check-only
python src/seed_reference.py --build-release --auto-apply
```

參考池換版為獨立子工作：資料更新可以自動觸發其檢查，但只有政策明確啟用時才自動切換。不能每次擴資料都暗中改變 Stage 2 的參考標準。

### F.2 兩種「產業分類」分開處理

**股票分類：股票 → 產業。** 目前上游 config.py 已有 INDUSTRY_GROUPS、GROUP_ALIAS、STOCK_GROUP_OVERRIDE，應重用分類邏輯，不在本專案再維護一套矛盾規則。優先順序固定：有理由與生效日期的個股覆寫 → 明確類別對照 → 核准的關鍵字規則 → unknown。同時命中互斥群組時標記 conflict，不依不透明的 first-match 悄悄決定。unknown／conflict 列入異常清單，不送去搜尋哪個產業能產生最好績效。

分類版本須包含規則雜湊、資料來源、生效日期及股票的歷史歸屬。分類更名、拆組、合組及錯誤修正不等於獲得全新未見產業：使用穩定 group_id 與 lineage，防止把舊排除產業改名後當新產業加入。過往股票改分類也不應以最新分類直接覆寫整段歷史而不記錄。

**因子適用範圍：因子 → 核准產業集合。** 使用固定的 train／validation 標準判斷，test 不參與核准。這是資格評估，不是以 LLM 根據公司敘述猜適用產業。

### F.3 核心資料契約

因子 metadata 建議新增或明確化下列欄位：

| 欄位 | 用途 |
|---|---|
| groups_at_admission | 入庫時可見的產業 ID，建立後不可覆寫 |
| groups_seen | 已完成資格判定的產業集合；通過及拒絕都記錄 |
| excluded_at_admission | 入庫時已排除的產業，普通自動更新永不加回 |
| approved_groups | 實際獲准使用的產業集合，統一為 list |
| scope_mode | global／restricted；global 的自動擴張政策須明確，不把 None 當無限制的新產業授權 |
| scope_history | 每次新增產業、原因、run_id、規則版本與期間 |
| evaluation_history | 每次通過／拒絕／樣本不足的未取整指標與理由 |
| value_orientation / trading_use | 沿用既有定向與交易用途契約 |
| values_version / metrics_version | 區分現行因子值與歷史入庫績效所屬版本 |

現有字串 industry_scope 遷移為 list 時，Stage 2、admit、memory、factor_lab、audit、report 與 crosssec 都必須一起支援；不能只把 JSON 改成陣列。global 舊因子需凍結其原入庫可用產業，後來的新產業仍走資格判定。2026-09-18 的值重建是計算快取更新，不是新產業資格核准。

既有 26 個因子的原始 4 產業可根據入庫資料版本及歷史紀錄回填；不能對日後新因子一律硬填 4 產業。來源不明者先標 needs_provenance，不臆測其未見範圍。

### F.4 只擴張新產業的規則

```text
candidate_groups =
    current_groups
    - groups_seen
    - excluded_at_admission
    - historical_aliases_or_descendants_of_excluded_groups

new_approved_groups = old_approved_groups ∪ passed(candidate_groups)
```

- 舊範圍保留；新產業未通過，不擴張。
- 舊排除產業即使這次分數漂亮，仍不加入。
- 新產業沒有足夠樣本：記為 insufficient_data，保留等待條件，未完成有效判定前不當作永久拒絕。
- 已完成判定而失敗的新產業加入 groups_seen，不能每次重跑直到偶然通過。未來確需重新評估時，由明確的新評估版本／預定週期處理並記錄多次嘗試。
- 舊核准產業若重測衰退，列出警示與診斷，不在「只擴張」工作中偷偷刪除；停用應是獨立政策。
- 新增股票但產業不變：更新同產業的計算值與覆蓋率，不重新打開被排除產業。
- 重建所有產業的研究值，與把所有產業加入可用 scope，是不同操作；建議研究原值與正式使用遮罩分離。

同一段時間的新產業可能共受市場因子影響，不保證與原產業統計獨立；一旦反覆看結果並挑選，就不能再稱完全未使用的樣本外。

### F.5 判斷標準：沿用設定並補足用途一致性

以 config.yaml 為唯一門檻來源，沿用 industry_ic_series／industry_qualify，不複製一套公式。每個 plan 凍結門檻與期間，執行期間設定若變動即失效重算。

目前產業通道設定：

| 項目 | 現有值 |
|---|---:|
| sub-train ICIR | ≥ 0.60 |
| validation ICIR | ≥ 0.35 |
| validation 衰減 | ≤ 50% |
| 覆蓋率 | ≥ 60% |
| train 有效月份 | ≥ 36 |
| validation 有效月份 | ≥ 12 |
| 逐月產業 IC 最少股票 | 8 |
| 空頭用途換手上限 | 40%（目前引用 Stage 4） |

方向先按 metadata 定向，所有 gate 使用未取整值；可用腿依 trading_use 判定。正式自動擴張前應補齊：多頭／空頭／雙腿各自的換手口徑、用途能否在產業間不同、缺標籤時的可投資集合、有效月份定義。現行產業多頭通道沒有完整換手 gate，不能直接描述為已具備所有條件。

建議產業別保存用途，例如某因子在電子為 long_only、在營建為 short_only；不能只用一個全域 trading_use 讓多頭組合誤用新產業空頭訊號。若第一版不支援產業別用途，就限制新產業必須符合原用途。

既有 R02／R03 等標籤或單位問題未解決前，品質關卡須明確標出阻擋項；不能因通過 ICIR 門檻就跳過資料有效性。此次成長欄位重建不代表那些問題已修复。

### F.6 重用檔案與執行責任

| 現有檔案 | 擬議責任 |
|---|---|
| 上游分類程式與 config | 股票分類及規則版本，本專案讀取結果 |
| build_base.py | 可指定輸出版本／暫存位置，建構月頻資料及品質摘要 |
| eval_candidates.py | 原精度統計、方向、產業資格；提供純計算函式 |
| crosssec_oos.py | 擴充為 refresh／check／requalify 驅動；保留原橫斷面報表模式 |
| seed_reference.py | 參考因子 release 建立、比較與切換 |
| memory.py | 備份、版本、提交／回復與 metadata schema |
| report.py | 統一 JSON／人類可讀報告，供 CLI 與 UI 使用 |

先保留 crosssec_oos.py 名稱，避免馬上更名使既有命令失效；若未來改名 factor_scope.py，應保留相容入口並更新檔案對照表。不要把業務邏輯寫在 Web 頁面的按鈕事件中，也不要各功能各開一支重複腳本。

### F.7 參考因子換版

目前 seed_reference.select 使用 ICIR_test 與 test 衰減，dedupe_reference 未限制 pre-test，而且 --clear 會先移除現行參考池。這三件事必須改掉，不能把 clear＋apply 串起來就當安全換版。

擬議流程：

1. 凍結上游資料、生成程式及設定，生成全部合資格研究候選的原始值；不能只拿已經用 test 挑過的 survivors 作為起點。
2. 以與本專案一致的 sub-train／validation 重新評估。方向只由 train 決定；去重只看 pre-test。test 指標不進候選選取、排名或換版比較決策。
3. 在 staging 建立新 release：候選來源、公式／生成參數、資料版本、選取期間、原精度判斷、去重原因、因子值與 hash 一起保存。
4. 輸出新舊差異：增加、移除、公式相同但資料更新、方向／用途變更，以及 Stage 2 將使用的實際數量。
5. 保留舊 release 不動，驗證後切換 active reference release；原自有 F 因子與歷史 attempts 不重寫。
6. 回復只切回舊 release，不需重算或刪除歷史檔。

ID 建議使用永久識別碼，不在每次 release 從 R-001 重編。舊因子保存原 ID；新定義配置未使用 ID，附 supersedes／source_formula_hash。只有同一定義的資料重算可保留 ID 並提高 values_version。名稱相同不代表同一公式，公式相同也不代表同一資料版本。

新參考池只影響之後的 Stage 2 判斷；既有自有因子是否與新池重複可另產生報告，不能倒改當年通過紀錄。參考池成功換版也無法抹除已使用 test 的歷史選擇偏誤，舊成果仍需標註版本與限制。

### F.8 可回復提交、重跑與快取失效

每次工作使用 run_id，manifest 記錄輸入／輸出 hash、程式與設定版本、分類版本、執行狀態與檢查結果。流程狀態：

```text
created → building → validated → planned → committing → committed
                  ↘ failed / needs_review / rolled_back
```

- 快取鍵至少包含資料 hash、公式 hash、分類版本、定向、scope 與計算程式版本；不是只看檔案存在與否。
- 多檔提交以 generation 目錄加單一 current 指標切換；讀取端取得同一 generation，防止 monthly_base 已新、factor_values 還舊。
- 切換前檢查輸入 hash 未被其他工作改動，且取得互斥鎖；挖礦與換版不能同時寫 memory。
- 相同輸入與設定重跑為 no-op；失敗不動正式版本，重新執行從可驗證階段恢復。
- 已完成版本不可覆寫；rollback 也記錄新事件。
- 資料筆數減少、scope 違規、unknown 分類、來源不足等不能悄悄略過並顯示成功。

### F.9 驗收案例

1. 新產業通過則只加新產業；舊排除產業即使通過也不加入。
2. 新產業失敗、樣本不足及方向不符分別有明確狀態。
3. test 期任意污染不改變資格、方向或參考選取。
4. 分類更名／拆分不繞過舊排除規則；分類未知或衝突可被追查。
5. scope 外更改研究值不影響正式使用分數；僅做空的產業訊號不進多頭組合。
6. 相同版本重跑不重複入庫、不重複加 scope、不重複配置 ID。
7. 提交中斷後可回復，不留下 metadata／parquet 混版。
8. 舊因子 ID、原始入庫指標與歷史 attempts 保持不變。
9. 每個被跳過或停用的因子有原因；單一失敗不能偽裝整批成功。
10. UI 與 CLI 執行相同 plan，得到相同結果。

### F.10 Web UI 建議

可以建立本機 Web UI，但先把上述共用工作流程與版本契約做好，再把 UI 當成操作層。第一版以單使用者、localhost 為預設，不急著部署雲端或加入多人權限。

建議五個頁面：

- **資料狀態**：來源時間、資料版本、股票／產業數、快取是否過期、阻擋項。
- **更新與檢查**：按一次執行、工作進度、階段性紀錄；不得因重新整理頁面重複啟動。
- **因子產業範圍**：原範圍、新增候選、逐條門檻通過／失敗、預計新增範圍及用途。
- **參考池換版**：新舊 release 差異、現行版本、切換及回復。
- **執行歷史**：run_id、設定快照、報告下載與備份位置。

一般使用者只需選「檢查」或「依已設定規則自動套用」；專家設定集中管理。auto-apply 預先啟用後，不必每個產業反覆確認；遇到未配置的分類、輸入變更或品質阻擋時才停止該項。頁面明確區分「值已重算」「資格已核准」「正式版本已切換」，避免一個綠色成功燈掩蓋不同狀態。

長工作交給獨立工作程序，UI 查詢 job_id；提供取消與恢復，禁止在提交一半時直接取消。按鈕不能接受任意 shell 命令或檔案路徑；先呼叫受控服務函式／固定 CLI 參數。人類 test 報告與挖礦 prompt 資料出口分開，UI 不把 test 結果送回自動搜尋。

實作順序：共用計算與版本契約 → CLI 一鍵工作及回歸測試 → 本機 UI。此設計不需要先購買或部署外部服務。

### F.11 第一版實作狀態（2026-09-18）

已實作 `factor_scope.py` 共用核心與 `scope_ui.py`／`scope_ui.html` 本機介面，使用 Python 標準庫 HTTP 服務。包括分類一致性驗證、固定用途的新產業資格、舊排除保護、版本雜湊、備份／日誌與中斷回復。沿用現有正式 library／values 路徑，由互斥鎖與中斷標記防止讀取混版，尚非完整的 generation 指標架構。

本版輸入為已重建的 monthly_base；不提供資料下載、歷史產業轉換、自動推測未知分類、參考池 release 換版及工作取消。參考因子維持原版本。CLI 與 HTTP 測試已通過，瀏覽器工具工作階段錯誤導致畫面驗收未完成。實際操作及限制見 `SCOPE_資料擴張操作說明.md`。
