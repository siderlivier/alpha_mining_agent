# 代辦清單

> 更新日期：2026-09-17
> 涵蓋 `alpha_mining_agent`（本專案）與 `tw_alpha_strategy`（前置專案）

---

## 剛完成（2026-09-17）

### ✅ 資料重建：958 檔 / 4 產業 → 1,616 檔 / 6 產業

前置專案把逐檔抓的資料表補齊後（`chip_*` 五張、`event_capreduce`、`event_dividend`
的股票清單原本停在 7 月的舊 universe），重跑 `build_base.py`：

| | 舊（07-20） | 新（09-17） |
|---|---|---|
| 列數 | 140,564 | **239,919** |
| 股票數 | 958 | **1,616** |
| 產業 | 4 | **6**（+民生與製造 309、營建 89） |

品質檢查全數通過：**沒有任何欄位缺值率惡化超過 3pp**（全欄位平均 8.8% → 8.0%）、
新增 658 檔的缺值率 8.1% 比既有 958 檔的 8.8% 還低、每月每產業股票數
全部高於 `min_stocks_per_group = 8`、`pytest` 241 passed。
舊檔備份在 `data/_before_rebuild/`。

### ✅ 橫斷面樣本外檢定（新增 `src/crosssec_oos.py`）

26 個自有因子是在 4 個產業上挖出來的，**民生與製造與營建是 agent 從沒看過的產業**。
拿新資料測它們，得到第二種樣本外——與「往前走」互相獨立的「往旁邊走」：

| 檢定 | 通過 | 說明 |
|---|---|---|
| 新產業 × 全期 | **24 / 26** | 維持同向且 \|t\| ≥ 2 |
| 新產業 × test 期 | **19 / 26** | 兩個維度同時樣本外，最嚴格 |

最強的幾個（test 期、新產業）：F-018 低波動隱藏成長股 t 5.86、
F-010 金融資本穩健度 t 5.09、F-002 金融品質價值 t 4.69、F-024 成長無空頭疑慮 t 4.68。

**真正在新產業失效、但在原產業很強的只有 3 個**（其餘沒過的是在兩邊都弱，
屬於 test 期本來就不顯著，不是跨產業失敗）：

| 因子 | 原 4 產業 t | 新 2 產業 t | 判讀 |
|---|---|---|---|
| F-012 淨化ROE動能 | 5.29 | **0.25** | 最明顯的產業特定因子 |
| F-006 低應計品質股 | 3.87 | **0.44** | 應計品質訊號本來就受產業資本結構影響 |
| F-009 營收加速趨勢 | 3.56 | 1.76 | 邊緣 |

原始輸出存於 `logs/crosssec_oos.txt`、`logs/crosssec_oos_test.txt`。

---

## 🔴 立即：推上 GitHub

本機累積 **5 個 commit** 未推送（`origin/main` 停在 `0fdc622`）。
這一步必須在你自己的終端機做，這個 session 的 shell 沒有 GitHub 憑證。

```bash
cd C:\Users\User\Desktop\股匯操盤AI\alpha_mining_agent
git push -u origin main
```

- [ ] `git push -u origin main`
- [ ] 確認 GitHub 上看得到 `memory/attempts/` 的 1043 筆紀錄

---

## 🔴 因子庫的指標已經過時

`library.json` 裡每個因子的 `sub_train` / `validation` / `test_metrics_sealed`
都是在 **958 檔 / 4 產業**上算的。`memory/factor_values.parquet`（09-01 建）
也只有 958 檔。

**但這些是那次挖礦的歷史紀錄**——它們記錄了「這些因子當初是在什麼資料上被挑出來的」。
直接重算覆蓋掉，就抹掉了這個事實。

- [ ] **決定政策**：重算後要寫進 `library.json`（覆蓋歷史），
      還是另存一份 `library_1616.json` 並保留原檔？**建議後者**
- [ ] 重算 26 個 F-xxx 在新面板上的因子值
- [ ] 重新匯入參考因子：`data/dfs_snapshot.parquet` 建於 08-31，
      上游 `factors_monthly.parquet` 已是 09-14
      → `python src/seed_reference.py --clear && python src/seed_reference.py --apply`
      ⚠️ 參考因子的四道篩選會重跑，25 個的數目可能會變
- [ ] 重算後跑一次 `factor_lab.py --compare`，看 6 產業下的合成結果

---

## 🟠 8 個產業限定因子的 scope 要重新檢視

Stage 4b 產業通道入庫的 8 個因子，它們的 `industry_scope` **全部是在只有 4 個產業時決定的**：

| 因子 | 名稱 | 目前 scope |
|---|---|---|
| F-009 | 營收加速趨勢 | 電子 |
| F-013 | 冷門成長股 | 電子 |
| F-014 | 本業成長價值股 | 電子 |
| F-016 | 本業獲利創高股 | 電子 |
| F-017 | 外資認證營收動能 | 電子 |
| F-018 | 低波動隱藏成長股 | 電子 |
| F-019 | 現金含金價值股 | 電子 |
| F-025 | 半導體毛利拐點股 | **生技** |

橫斷面檢定顯示其中好幾個在新產業也有效（F-018 t 5.86、F-016 t 3.67、
F-025 t 3.83、F-014 t 3.03），**限定在單一產業可能已經沒有道理**。

- [ ] 用新面板重跑 `industry_qualify()`，看每個因子現在在哪些產業過關
- [ ] 決定是放寬 scope、還是維持原樣（維持的話要說得出理由）

### F-025 的命名問題

```
name_zh         半導體毛利拐點股
industry_scope  生技
formula         mul(cs_rank(delta(gross_margin,6)), cs_rank(neg(rev_mom)))
```

看 `A-0828` 的原始紀錄，這不是 bug 而是**誠實的化石**——agent 假設半導體，
實測半導體 industry_icir 只有 0.2 未達標，反而在生技意外通過。而且 agent
自己的 diagnosis 已經寫對了：

> 毛利率領先營收的機制假說在半導體本業反而較弱，但在生技產業卻成立，
> 顯示『毛利先行、營收落後確認』這個時間差機制**可能是跨產業的財報揭露延遲現象**，
> 而非半導體特有的庫存循環現象。

**而橫斷面檢定證實了 agent 當初的改寫是對的**（新產業 test 期 t = 3.83）。

三個選項：

- [ ] **(A) 改名為機制導向、產業中性的名字**（例如「毛利先行營收落後」），
      原始假設保留在 `desc_zh`。理由：`name_zh` 會進 Generate prompt，
      現在的名字會讓 agent 學到錯誤的產業聯想
- [ ] (B) 維持原樣。理由：名字記錄了當初的假設，`attempts/A-0828` 已完整保存
      「假設錯了但因子有效」這個過程，那是專案裡少數幾個真正有意思的紀錄
- [ ] (C) 改名並同時放寬 scope（配合上面那一項）

**我的建議是 (A) 或 (C)**——化石的價值在 `attempts/`，不必犧牲 prompt 的正確性來保存它。

---

## 🟠 所有文件的數字都建在舊資料上

`RESULTS.md`（17 節）與 `README.md` 的**每一個數字**都是 958 檔 / 4 產業的結果，
包含 IR 2.57、DSR 0.997、下跌月超額 13.08% 等等。

- [ ] 因子值重算後，決定要不要重跑六項診斷並更新文件
- [ ] 更新前先在 `RESULTS.md` 開頭加警語，標明現有數字的資料基礎
- [ ] `FILES_檔案對照表.md` 的 `data/` 那節要更新（現在寫的還是 958 檔 / 4 產業）

> 提醒：你說過因子測試是**為了證明因子有用，不是拚績效**。
> 所以重跑的重點應該放在「哪些因子撐得住」，而不是新的 IR 有沒有比 2.57 高。
> 橫斷面檢定那 19/26 才是這個目標下最有價值的數字。

---

## 🟡 前置專案 `tw_alpha_strategy` 的殘留項目

資料抓取的主要問題已修（09-07/09-08 全部重抓，涵蓋率從 51~58% 升到 87~99%），
以下是檢查時發現的次要項目：

- [ ] **OHLC 內部矛盾 1,497 筆**（342 檔，違反幅度 ≥ 5%）。
      全部 101,846 筆矛盾中 98.5% 幅度 < 5%，是還原精度造成的雜訊；
      但那 1,497 筆有明顯型態——`open` 停在還原前的價位，像是還原係數
      套用到 `open` 時差了一天：
      ```
      3597  2012-03-01  開 10.30  收 20.38      ← 當天調整
      3597  2012-03-02  開 20.38  高 10.92      ← 隔天 open 還是舊價位
      ```
      **不影響 mining agent**（`build_base.py` 只讀 close / close_raw / ret /
      volume / amount，不碰 OHLC），對上游的 `exec_prices.parquet` 影響也很小
      （`px_vwap / px_close` 的 1~99% 分位是 0.948~1.074，無離群）。低優先度。
- [ ] **`benchmarks.parquet` 停在 06-30**，是唯一沒重抓的檔。
      上游 README 的已知侷限提過「2026 上半年指數出現異常大波動（待核對）」。
      *（你說這個專案先不理市場指數，記在這裡備查。）*
- [ ] **`fetch_extra.py` 的守衛要改**：目前是「檔案存在就跳過」，
      這正是這次 10 個檔靜默沒重抓的原因。應改成
      **「比對表內股票集合 vs 現行 universe，缺的就補抓」**。
      不改的話，下次 universe 再擴會重演同一件事。
      這跟本專案對前瞻偏差的態度是同一個原則：**不該靜默失敗的地方要吵**。

---

## 🟢 挖礦本身：可以繼續，但要知道邊際報酬遞減

- [ ] 繼續 `python src/mining_loop.py --rounds 5`（一輪約 8~15 分鐘、$0.8~1.5）
- [ ] 定期 `python src/mining_stats.py` 看漏斗組成的變化

`mining_stats.py` 的分析顯示入庫率 4.2% → 2.6% → 0.9%，原因是**因子庫飽和
而非 agent 退步**（S1 訊號強度淘汰率 −9.0pp、S4 品質 −12.5pp，
但 S2 與既有因子重複 **+24.4pp**）。

**但現在多了 2 個產業、658 檔股票——Stage 2 的去相關基準等於換了一張牌桌。**
重算因子值之後值得再跑幾輪看入庫率會不會回升；若回升，那本身就是
「飽和是相對於股票池而言」的證據。

---

## ⚫ HMM 市場狀態模型：結案，不再投入

以 Sharpe 論達標（不切換 1.79 → 切換 1.88 → 完美預知上限 2.01，五個門檻全部勝過基準），
但配對自助法的 ΔSharpe 95% CI **[−0.347, +0.152] 跨越 0**，n = 77 分不出高下。
四種總經指標一個都沒幫上忙，反而稀釋了價格訊號。

完整證據見 `RESULTS.md` 第 16 節。第 11 節是用錯指標（IR）得出的舊結論，
原文保留供對照。

> ⚠️ 若未來重啟：**不要在 test 期上調門檻或狀態數去追 Sharpe**，那是在測試集選模型。
> 目前的 0.5 門檻與 2 狀態都是未調過的預設值，這是它現在唯一的可信之處。
> 真正能拉開差距的是擴大樣本（更多年份或週頻觀測），不是調參。

---

## 🟢 已完成的里程碑（存檔用）

<details>
<summary>展開</summary>

- 受限 DSL：33 個運算子、帶型別簽章、前瞻偏差語言級封鎖
- 四階段漏斗 + 產業專屬通道（Stage 4b）
- 兩層記憶：`attempts/` 只進不出（1043 筆）+ `learnings.md` 蒸餾（90 行）
- 整理回合：經驗壓縮、運算子提案、佇列消費機制
- 5 個運算子從提案到上線：`rank_nz`、`industry_demean`、`streak`、`streak_true`、`clip_std`
- 參考因子匯入 + 四道篩選（含彼此去重，86 → 31 → 25）
- 因子組合實驗室：`--compare` / `--loo` / `--greedy` / `--cost-scan`
- 過擬合診斷六項：régime / breadth / liquidity / importance / DSR+PBO / timing
- **四套前瞻偏差檢測**，每一套都做過變異驗證（故意寫錯確認測試會轉紅）
- 風險調整對照：`--riskadj` 三張表 + `threshold_sweep.py`
- **橫斷面樣本外檢定**：`crosssec_oos.py`（本次新增）
- 241 個測試通過
- 文件四件套：`SPEC`（為什麼）/ `GUIDE`（怎麼用）/ `RESULTS`（數字）/ `FILES`（哪個檔在幹嘛）

</details>
