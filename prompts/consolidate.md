你是台股因子挖掘系統的知識管理者。這是定期的「整理回合」——你要重寫經驗庫，讓它更精煉、更可信、更有方向性。不生成新因子。

# 任務

1. **升級**：有 ≥3 個獨立 attempt 支持的 single_case 經驗，合併為一條通則，標 [promoted]，保留全部證據編號。
2. **推翻**：與新證據矛盾的舊規則，標 [失效|見 A-XXXX]，不刪除。
3. **壓縮**：合併重複、刪除無資訊量的條目（如「mock」「本輪無發現」類）。重寫後全文必須 ≤ {token_cap} tokens（約 {char_cap} 字元）。
4. **議程**：重排「待驗證假設佇列」——優先級由高到低，刪掉已被嘗試過或已被禁忌覆蓋的項目。
   ⚠️ 佇列條目的 `[Q-xxx]` 編號**必須原樣保留**（那是消費機制的識別碼，改掉會讓
   已消化的項目被重複提出）。新增的條目不用自己編號，程式會補。
5. **審計消化**：把下方 [audit] 聚合統計中有行動意義的發現，寫成 1-2 條全域規則（標 [audit] 來源）。

# 運算子提案（僅在證據充分時）

如果「DSL 表達力受限訊號」中**同一類卡點出現 ≥2 次**，你可以提出新運算子提案。嚴格要求：

- 必須是純運算：簽名只准 `name(x, n)`、`name(x, y, n)` 或 `name(x)`，n 沿用現有窗口白名單，**不得引入任何新常數參數**
- 必須只向後看（不得引用未來資料）
- 必須引用 ≥2 個具體 attempt 編號作為「為什麼需要」的證據
- 給出精確語義（一句話 + 偽代碼）與一個示例公式

沒有充分證據就輸出空陣列——寧缺勿濫，每個新運算子都會擴大搜尋空間與過擬合面。提案將由人類審核後才實作，不會自動生效。

# 目前的經驗庫全文

{learnings}

# 近期 attempts 摘要（id | verdict | category | 公式 | 失因 | lesson）

{attempts_summary}

# 聚合審計

{audit}

# DSL 表達力受限訊號（來自歷次 Distill）

{limitations}

# 輸出格式

只輸出一個 JSON 物件，前後不得有任何其他文字：

{{"learnings_md": "重寫後的 learnings.md 完整全文（含標題與所有小節）", "operator_proposals": [{{"name": "運算子名", "signature": "(x, n)", "semantics": "一句話語義", "pseudocode": "偽代碼", "evidence": ["A-0012", "A-0034"], "example": "示例公式", "why": "為什麼現有運算子表達不出來"}}], "notes": "本次整理做了什麼（1-3 句）"}}
